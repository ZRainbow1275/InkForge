Xiumi Menu Pin Control Residue - 2026-06-29

Scope:
- Static publishability gate for copied or partially cleaned Xiumi v5 paper-editor operation menu
  pin controls.
- Applies to WeChat, Xiaohongshu, and Zhihu quality reports.
- No platform action, phone preview, sync, upload, schedule, or publish action was performed.

Live source:
- CloakBrowser reviewed the logged-in Xiumi v5 paper editor DOM.
- The editor exposed empty operation menu pin controls with source-specific class markers
  `op-cp-menu-pin` and `op-cp-menu-pin-tb`.
- The nodes appeared under Xiumi's operation-loader context, but the reduced rule is intentionally
  anchored to the pin markers themselves.

Implementation:
- Added the `Xiumi menu pin control residue` detector label.
- Added a reduced regression fixture containing only `op-cp-menu-pin` and `op-cp-menu-pin-tb`,
  without `op-loader`, operator dock controls, operator depot items, attribute context-menu host
  nodes, attribute-board controls, operation-bar menus, Angular runtime attributes, hosted media,
  sidebar controls, or meta panels.
- Kept the rule anchored to class/id attributes, not prose.
- Kept adjacent detector labels independent from `Xiumi operation panel loader residue`,
  `Xiumi operator dock control residue`, and `Xiumi attribute context menu host residue`.

TDD:
- Red:
  `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "menu pin controls" --reporter=default`
  failed before the detector update because no `*-market-editor-residue` issue was emitted.
- Green:
  `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "menu pin controls|operation panel loader classes|operator dock child controls|attribute context menu host controls" --reporter=default`
  passed with 4 selected tests and 298 skipped tests after the detector update.

Full verification:
- `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts --reporter=default --testTimeout=60000`
  passed with 1 file and 302 tests.
- `pnpm -C inkforge exec vitest run src/services/export --reporter=default --maxWorkers=1 --no-file-parallelism --testTimeout=60000`
  passed with 36 files and 1279 tests.
- `pnpm -C inkforge exec eslint src/services/export/quality-detector.ts src/services/export/platform-export-rendering.test.ts --quiet`
  passed.
- `pnpm -C inkforge exec vue-tsc --noEmit --pretty false` passed.
- `NODE_OPTIONS=--max-old-space-size=4096 pnpm -C inkforge build` passed with 4653 modules
  transformed and Vite build completed in 50.34s.
- `pnpm -C inkforge style-proof:release-preflight --json` exited 1 as expected with
  `status=blocked-by-external`, `canClaimComplete=false`,
  `blockerKinds=["phone-preview","external-dependency","unsafe-to-automate","mutating-platform"]`,
  `externalHandoffRows=18`, `safeExternalRows=0`, `actionableLocalRows=0`, `nextRowRefs=5`,
  and `uniqueNextRows=3`.

Boundary:
- This is static publishability protection only.
- It does not prove WeChat PC paste, phone preview, mobile SMIL/click interaction, mobile Dark
  Mode, cover thumbnail acceptance, credentialed sync, scheduled send, platform preview, public
  article rendering, public-host acceptance, XHS/Zhihu account upload, or publish success.
