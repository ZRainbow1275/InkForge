Xiumi Color Palette Panel Residue - 2026-06-29

Scope:
- Local static publishability protection for copied or partially cleaned Xiumi v5 paper-editor
  color palette popup and mask chrome.
- No platform action, no phone preview, no sync, no upload, no publish.

Live DOM source:
- CloakBrowser read the live Xiumi v5 paper editor DOM at the logged-in paper editor surface.
- The sampled DOM exposed source-specific editor-side markers:
  - tn-color-palette-panel
  - tn-color-palette-panel-mask
  - tnColorPaletteInst
  - tnColorPaletteMask
- These markers describe palette popup state and palette mask handling. They are not article body
  DOM, style semantics, or platform-safe color values.

Implemented:
- Added `Xiumi color palette panel residue` to `MARKET_EDITOR_RESIDUE_RULES`.
- The rule matches class/id attributes containing:
  - tn-color-palette-panel
  - tn-color-palette-panel-mask
  - tnColorPaletteInst
  - tnColorPaletteMask
- The rule is ordered before `Xiumi color selector control residue` so palette popup residue keeps
  an exact diagnostic instead of being conflated with ordinary text color selector controls.
- Added three-platform regression coverage for WeChat, Xiaohongshu, and Zhihu.

Negative scope:
- The rule does not block ordinary prose or article markup containing color, palette, panel, mask,
  picker, storage, popup, or theme wording by itself.
- The reduced fixture does not rely on color selector controls, operation-bar dropdown controls,
  panel-header controls, Angular runtime attributes, hosted media, sidebar controls, or meta
  panels.

Verification:
- Red:
  pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "color palette panels" --reporter=default
  Result: failed as expected before implementation, 1 failed / 310 skipped / 311 total, because
  no wechat-market-editor-residue issue was emitted.
- Green:
  pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "color palette panels|color selector controls|operation bar dropdown controls|panel header controls without panel parents" --reporter=default
  Result: passed after implementation, 1 file / 4 selected tests passed / 307 skipped / 311 total.
- Full validation:
  pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts --reporter=default --test-timeout=90000
  Result: passed, 1 file / 311 tests.
  pnpm -C inkforge exec vitest run src/services/export --reporter=default --maxWorkers=1 --no-file-parallelism --test-timeout=90000
  Result: passed, 36 files / 1288 tests.
  pnpm -C inkforge exec eslint src/services/export/quality-detector.ts src/services/export/platform-export-rendering.test.ts --quiet
  Result: passed.
  pnpm -C inkforge exec vue-tsc --noEmit --pretty false
  Result: passed.
  NODE_OPTIONS=--max-old-space-size=4096 pnpm -C inkforge build
  Result: passed, 4653 transformed modules, Vite build 42.45s.
- Release preflight:
  pnpm -C inkforge style-proof:release-preflight --json
  Result: exited 1 as expected. canClaimComplete=false, status=blocked-by-external,
  blockerKinds=phone-preview, external-dependency, unsafe-to-automate, mutating-platform.
  Summary: blockerCount=4, combinedIssueCount=11, cannotClaimSteps=29, phoneOpenSteps=4,
  externalDependencyOpenSteps=14, unsafeToAutomateOpenSteps=13, mutatingOpenSteps=13,
  externalHandoffRows=18, safeExternalRows=0, actionableLocalRows=0, nextRowRefs=5,
  uniqueNextRows=3.

Boundary:
- This proves local static publishability protection only.
- It does not prove WeChat PC paste, ordinary Ctrl+V, phone preview, mobile SMIL/click,
  mobile Dark Mode, cover thumbnail acceptance, credentialed sync, scheduled send, platform
  preview, public article rendering, public-host acceptance, XHS/Zhihu account upload, or publish
  success.
