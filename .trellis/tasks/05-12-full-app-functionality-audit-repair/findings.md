# Findings

This file records verified failures only. Do not add speculative findings without reproduction evidence.

| ID | Severity | Matrix Row(s) | Status | Reproduction | Root Cause | Fix Plan | Re-test Evidence |
| --- | --- | --- | --- | --- | --- | --- | --- |
| F-000 | info | all | open | Initial task setup only. No verified failures recorded yet. | Not applicable. | Build inventory and begin Web/Tauri audit. | pending |
| F-PUB-001 | medium | `PUB-CTRL-001` | fixed | Open `/publish` at 1440x900, run DOM query for buttons with no text, no `aria-label`, and no `title`. The header `.back-btn` matched: `<button class="back-btn"><svg ...></svg></button>`. | Icon-only button lacked accessible name and `type="button"`, violating component accessibility guidance. | Add `type="button"`, `aria-label="返回工作台"`, and `title="返回工作台"` to the back button. | `pnpm -C inkforge exec eslint src/views/PublishView.vue --ext .vue --quiet` passed; Playwright re-check found `unnamed: []` and `.back-btn` had `aria=返回工作台`, `title=返回工作台`, `type=button`. |
| F-THEMES-001 | medium | `THEMES-CTRL-001` | fixed | Open `/themes` at 1440x900 and run the no-text/no-aria/no-title button query. The header `.back-btn` matched with only an inline SVG. | Icon-only route-back button lacked an accessible name, and the route's non-form buttons did not declare explicit `type="button"`. | Add `type="button"`, `aria-label="返回上一页"`, and `title="返回上一页"` to the back button; add explicit button types to the Apply and Cancel buttons. | `pnpm -C inkforge exec eslint src/views/ThemesView.vue --ext .vue --quiet` passed; Playwright re-check `/themes?audit=themes-back-a11y` returned `unnamed: []`, `.back-btn` had `aria=返回上一页`, `title=返回上一页`, `type=button`, and all four route buttons declared `type=button`. |
| F-SETTINGS-001 | medium | `SETTINGS-CTRL-001` | fixed | Scan the visible active Settings tab controls across all 12 tabs. `appearance` had unnamed range inputs; `editor`, `ai`, and `data` had visible buttons without explicit `type`; `data` had unnamed number inputs; `extensions` and `advanced` had unnamed file inputs. | Shared Settings control patterns missed component accessibility requirements for explicit non-submit button type and accessible names on icon/file/range/number controls. | Add `aria-label` to range, number, and file inputs; add `type="button"` to visible action/toggle buttons that are not form submits; add aria names to swatch buttons. | `pnpm -C inkforge exec eslint src/views/SettingsView.vue --ext .vue --quiet` passed; Playwright all-tab re-scan returned no unnamed visible controls, no visible buttons without `type="button"`, no range without name, no console errors, and no horizontal overflow across all 12 tabs. |
| F-SETTINGS-002 | medium | `SETTINGS-NAV-001`, `SETTINGS-CTRL-001` | fixed | 复核 Settings 基础导航控件时，重点检查 `.sv-back-btn`、`.sv-nav-item` 和 `settings.appearance.reducedMotion`。这些控件必须有原生按钮或输入语义、可访问名称，并且按钮不能落入默认提交行为。 | Settings shell 的基础导航控件需要和全应用按钮规范保持一致；此前全 tab 扫描覆盖面较广，但没有把这组三个基础控件作为独立证据闭合。 | 为返回按钮补 `type="button"` 与 `aria-label="返回上一页"`；为侧栏 tab 按钮补 `type="button"`；为“减弱动效”复选框补 `aria-label="减弱动效"`。 | `pnpm -C inkforge exec eslint src/views/SettingsView.vue --ext .vue --quiet` 通过；Playwright 桌面 `/settings?tab=appearance&audit=settings-nav-controls-recheck` 与 390px 移动端复核均返回 `visibleButtonsWithoutType: []`、`unnamedVisibleControls: []`、控制台错误为空、页面级横向 overflow 为 0；移动截图保存到 `screenshots/settings-appearance-mobile-controls-2026-05-12-2026-05-12T06-19-13-322Z.png`。 |
| F-SETTINGS-003 | medium | `SETTINGS-CONFIRM-001`, `SETTINGS-CTRL-001` | fixed | 触发 `重置外观 Tab` 时，确认弹窗才出现在 DOM 中。检查弹窗模板发现取消/确认按钮没有显式 `type="button"`，需要输入 `RESET` 的校验输入框没有稳定可访问名称。 | Settings 危险操作确认弹窗是隐藏态控件，早前只扫活动 tab 可见控件时没有覆盖到该 overlay。 | 给校验输入框添加 `aria-label="确认操作校验文本"`；给取消和确认按钮添加 `type="button"`。 | GitNexus `SettingsView` impact 返回 LOW；`pnpm -C inkforge exec eslint src/views/SettingsView.vue --ext .vue --quiet` 通过；Playwright reset proof 打开弹窗后返回 `visibleButtonsWithoutType: []`、`unnamedVisibleControls: []`，输入 `RESET` 前确认按钮禁用、输入后启用，确认后弹窗关闭并通过 `settingsStore.resetTab('appearance')` 回到默认外观；控制台错误为空。 |
| F-SETTINGS-004 | high | `SET-EDITOR-003`, `CMP-EDITOR-001` | fixed | Settings Editor 真实 UI 能持久化 `editor.autoSave` 和 `editor.autoSaveInterval`，但代码审计发现 `EditorPanel.schedulePersist()` 没有读取这些设置，实际保存仍固定为 900ms。也就是说 Settings 层看似通过，编辑器真实行为没有跟随设置。 | Settings 持久化层和编辑器保存调度层之间断链；`autoSave` 和 `autoSaveInterval` 只被 SettingsView/settings store 使用，没有进入 `EditorPanel` 自动保存调度。 | 让 `EditorPanel` 读取 `settingsStore.settings.editor.autoSave` 和 `autoSaveInterval`；关闭自动保存时取消待保存计时器并显示 `未保存`，保留 `Ctrl+S`/路由切换等手动 flush；开启自动保存时按设置的秒级间隔调度真实 `editorStore.updateContent()`。 | GitNexus 未能映射 `EditorPanel`、`schedulePersist`、`TyporaSyncState`，`updateContent` impact 为 CRITICAL 因此未改 store API；补偿为 Serena 符号/文本检查、目标 ESLint、`vue-tsc --noEmit`、真实浏览器证据。Playwright 证明：关闭自动保存后编辑 2.5s，状态为 `未保存` 且 IndexedDB `contents` 未变；按 `Ctrl+S` 后才写入；开启自动保存并设为 10s 后，2.5s 未写入，约 11.9s 后写入并显示 `已同步 · 已保存`。 |
| F-WS-001 | medium | `WS-CTRL-001`, `CMP-FILE-001`, `CMP-VERSION-001` | fixed | Open a real article in `/workstation`, scan visible controls. Workstation header/actions, manager tabs, stage/inspector controls, FileManager menu/context buttons, and VersionPanel save/diff controls contained visible buttons without explicit `type="button"`. Version panel re-scan showed `保存版本` and diff toggle as missing type before the second repair pass. | Several mature workstation component families predated the component accessibility contract requiring every non-submit button to declare `type="button"`. | Add explicit `type="button"` to WorkstationView, FileManager, and VersionPanel non-submit buttons without changing handlers or store/service boundaries. | Static button scan returned `missingType: 0` for `WorkstationView.vue`, `FileManager.vue`, and `VersionPanel.vue`; targeted ESLint passed; Playwright `/workstation?id=...&audit=ws-version-button-type-recheck` found `visibleButtonsWithoutType: []`, `unnamedButtons: []`, no console errors, and no horizontal overflow. |
| F-WS-002 | high | `RTE-002`, `CMP-WORKSTATION-001` | fixed | Real CloakBrowser inspection showed `workstationTabsStore` and `WorkstationTabBar.vue` existed, but `/workstation` never rendered `.workstation-tabbar` because the component was imported nowhere. The first mounted version then exposed invalid nested interactive controls: Enter/Space on pin/close could be canceled or treated as tab activation. | The route shell omitted the existing production component, while the original tab item combined `role="tab"` behavior and nested native buttons in one interactive container. Stateful E2E also assumed an empty tab session, so it was invalid when an earlier real case retained a baseline tab. | Mount the existing TabBar from real store state; use a non-interactive draggable wrapper with separate native tab/pin/close sibling buttons; add ArrowLeft/ArrowRight/Home/End navigation and keyboard-close focus fallback; derive shortcuts from current DOM order and clean only run-created tabs. | GitNexus impact for the relevant TabBar/Workstation handlers was LOW. CloakBrowser proved pointer and focused keyboard close on real articles. Store/layout Vitest passed 18/18; targeted ESLint, CommonJS syntax, and `vue-tsc` passed; the current production build transformed 4663 modules and the production Tauri debug binary compiled. Focused native cases, the retained-baseline pair, and the final complete Tauri editor suite all passed; the final full replay was 12/12 in 5m 24.8s. |
| F-WS-003 | high | `RTE-002`, `CMP-WORKSTATION-001` | fixed | Route/query article changes could remount the whole Workstation, and tab selection/close/restore mutated Pinia/session state before Vue Router confirmed navigation. Concurrent repository loads and EditorPanel markdown hydration could then publish an older article or mark the active document saved before its matching editor body was rendered. The E2E also treated encrypted IndexedDB `body` as plaintext, so its durable readback was invalid. | `ViewTransition` keyed Workstation by `fullPath`; navigation and tab-store mutation were not one transaction; editor-store loads and EditorPanel hydration had no selection generation/promise boundary; raw persistence assertions ignored the production encrypted envelope. | Keep the `/workstation` route shell stable across query changes; flush, navigate, then commit tab/article selection; leave store/session/recently-closed state untouched on router rejection; gate editor-store completion and EditorPanel hydration with monotonically increasing generations; expose `pending` until hydration is complete; validate raw encrypted envelope shape and prove readable persistence only through production reload/store/editor paths. | GitNexus reports LOW impact for `replaceWorkstationRouteArticle` (1 direct caller), `hydrateEditorContent` (2 direct callers), and `createContent` (1 direct caller, one mapped `useEditorStore` flow). Real Vue Router guards rejected select, close, last-close, and restore without state drift; rapid switching and native reload preserved the correct decrypted markers. Targeted lint/type/unit gates passed, production build passed, full Tauri editor Settings passed 12/12, and the independent SVG/WeChat Tauri suite passed 10/10. |
| F-WS-004 | high | `RTE-002`, `CMP-WORKSTATION-001` | fixed | After the route transaction repair, FileManager and TagBrowser still selected articles directly, so pending source text could bypass the flush/route/tab transaction. A first same-document save result could also arrive while a newer local editor revision was waiting for debounce and be interpreted as fresh hydration. Settings tests separately interacted with a leaving or transparent route shell during the 240ms out-in transition. | Selection ownership remained duplicated in child components; save completion tracked only the latest scheduled sequence rather than an immutable content/revision request; E2E route readiness checked visibility but not transition completion. Empty sensitive bodies were also exempted from encryption, weakening strict native proof. | Pass one Workstation-owned async selection request into child selectors; capture save identity plus revision before scheduling and ignore persistence echoes while a newer local revision exists; focus moved tabs only after route activation; prevent defaults only for actionable shortcuts; make the collapsed manager recovery a named native button; encrypt every sensitive string including empty values; and make the E2E wait for the route shell transition to settle. | A real IndexedDB transaction barrier held the first production save while native keyboard input created a newer revision; the final editor and reload retained both in order with no cross-document leak. FileManager and TagBrowser source-to-target switches proved source flush plus target isolation. The fresh complete Tauri/WebView2 suite passed 12/12 in 5m12.7s. Non-mutating ESLint, `vue-tsc`, full Vitest 108 files/1825 tests, the 4663-module build, and Tauri debug build passed. |
| F-EXP-001 | medium | `EXP-MODAL-001`, `CMP-EXPORT-001` | fixed | From a real Workstation article, click `发布` to open `ExportModal`. The modal used real article content and honest unavailable direct-publish copy, but its close, platform, preset, native copy/download, and styled copy/download buttons lacked explicit `type="button"`. The modal panel also lacked dialog semantics. | Export modal interactive controls predated the shared component accessibility contract for explicit non-submit button type and modal semantics. | Add `type="button"` to ExportModal action buttons, add an accessible close label, and mark the panel as `role="dialog"` with `aria-modal` and a title reference. | Static scan returned `missingType: 0`; targeted ESLint passed; Playwright re-check across WeChat/Xiaohongshu/Zhihu found `visibleButtonsWithoutType: []`, `unnamedButtons: []`, `role=dialog`, real article marker visible, direct-publish unavailable text visible, no console errors, and no horizontal overflow. |
| F-SET-EXPORT-001 | medium | `SET-EXPORT-001` | fixed | Change Settings `export.defaultPlatform` to a non-WeChat platform, then open Workstation and ExportModal. Before repair, both Workstation Stage and ExportModal initialized from hardcoded `wechat`, so the persisted default platform did not drive the export surface. | `SettingsView` persisted `settings.export.defaultPlatform`, but `WorkstationView` and `ExportModal` each declared local `selectedPlatform = ref<Platform>('wechat')` and never consumed the persisted setting for initial platform selection. | Initialize Workstation Stage from `settingsStore.settings.export.defaultPlatform`; pass the same setting into `ExportModal` as `initialPlatform`; reset the modal selection from that prop on each open without overriding the user's in-modal manual selection while the modal stays open. | GitNexus impact for both `selectedPlatform` symbols returned LOW risk with 0 affected processes; `openExportModal` impact returned LOW. Targeted ESLint and `vue-tsc --noEmit --pretty false` passed. Production build passed. Targeted real Tauri/WebDriver `svg-render.spec.cjs` passed 7/7 and proved persisted `zhihu` default drives Workstation Stage and ExportModal initial platform, then restores the original setting. Full `pnpm -C inkforge test:e2e` passed 3 specs / 21 tests. |
| F-SET-EXPORT-002 | medium | `SET-EXPORT-002` | fixed | Enter CSS in Settings `export.customCss`, then open a real Workstation article and ExportModal for WeChat. Before repair, Settings preview appended a local `<style>` tag, but the real Workstation/ExportModal renderer did not receive Settings export CSS as a first-class `ExportOptions` value, so the copied WeChat HTML path could diverge from Settings. | `settings.export.customCss` was handled as a SettingsView-local preview helper rather than a renderer option. `ExportModal` kept modal-local export options and did not merge persisted export CSS; `convertToWechatWithStats` had no sanitized custom CSS input before the `juice` inline pass. | Add `ExportOptions.customCss`; pass `settingsStore.settings.export.customCss` into `ExportModal`; build effective export options for styled/native renders; sanitize export CSS and merge it before WeChat CSS inlining; keep runtime `settings.advanced.customCss` and `#inkforge-custom-css` untouched. | GitNexus impact for `convertToPlatform`, `convertToWechat`, `convertToWechatWithStats`, and `usePreviewRenderer` was LOW; `ExportOptions` was MEDIUM but 0 affected processes. Targeted ESLint, `vue-tsc`, and production build passed. Focused export Vitest passed 377/377; full export Vitest passed 41 files / 1366 tests. Targeted real Tauri/WebDriver `svg-render.spec.cjs` passed 8/8 and proved `export.customCss` is read from real Settings, applied inline in WeChat ExportModal, then restored without runtime CustomCSS leakage. Full Tauri/WebDriver E2E passed 3 specs / 22 tests. XHS/Zhihu publish automation remains manual/operator-owned. |
| F-SET-EXPORT-003 | high | `SET-EXPORT-003` | fixed | Perform a successful copy/download from Workstation, ExportModal, or Publish, then open Settings Export History. Before repair, those production actions did not append a history row; the existing Settings preview was the only caller. The copy helper could also fall back to `writeText` and still let a styled-copy action appear successful even though SVG/rich HTML was not on the clipboard. Independent review additionally reproduced that one overlong/invalid persisted title could invalidate the entire history array on reload. | Durable history existed in the Settings schema/store, but real export surfaces bypassed it. Rich and plain clipboard success semantics were conflated, history writes bypassed entry-level schema normalization, and bulk-array parsing discarded valid neighbors around one corrupt row. | Reuse the existing settings store from each production export surface; append only after successful copy/download trigger; accept only modern rich-copy success or a sanitized DOM-selection fallback returning true; normalize and validate every history write; salvage valid persisted rows independently; add typed clear confirmation, focused regressions, and real Tauri copy/reload/clear proof. | Focused Vitest passed 4 files / 19 tests; full export Vitest passed 41 files / 1368 tests; targeted ESLint, `vue-tsc`, and production build passed. Real `svg-render.spec.cjs` passed 10/10 and full Tauri/WebDriver E2E passed native 8/8, SVG 10/10, visual 11/11 (29 total). The history row survived reload and typed clear survived a second reload. No OS download landing, WeChat external paste/publish, or XHS/Zhihu publish success is claimed. |
| F-SET-ADV-001 | high | `SET-ADV-001` | fixed | Settings Advanced 写入 `advanced.customCss` 后，真实 Tauri/WebView2 中 `#inkforge-custom-css` 文本存在，但 `style.sheet.cssRules.length=0`，live `.tiptap-content .ProseMirror p` 的 computed color 与 letter-spacing 未变化。 | runtime CustomCSS 依赖动态 style text 编译和 legacy `.editor-content` scope；WebView2/Tauri 可能不生成 CSSOM 规则，live editor 也需要更高权重的具体 selector。 | 将 CustomCSS 编译到 legacy 与 live ProseMirror 双 effective scope；注入后验证 CSSOM，必要时使用 CSSOM insertRule 或 singleton constructed/adopted stylesheet；禁用时清理 tag 与 fallback；保持 `export.customCss` 与 runtime `advanced.customCss` 分离。 | GitNexus `injectCustomCssStyle` impact=HIGH，已最小化改动并补偿回归。CustomCSS Vitest PASS 16/16；targeted Tauri/WebDriver CustomCSS PASS 1/1；`svg-render.spec.cjs` PASS 9/9；targeted ESLint、vue-tsc、build PASS；`pnpm -C inkforge test:e2e` PASS 3 specs / 24 tests。未声明 XHS/Zhihu 发布、微信手机预览、同步或发布成功。 |
| F-SET-PROFILES-001 | high | `SET-PROFILES-001`, `SET-PROFILES-003`, `SET-SYNC-002` | fixed | Settings Profiles 在真实 Tauri 中只显示“原生边界不可用”，创建动作固定写入空文件根；路径正则误删加号且会破坏 Windows/UNC/POSIX 根路径。活动 Profile 改变后，SyncEngine 仍把真实审计行归到 `local-default`；把单例的 `profileId` 改为可变后，又会让旧 Profile 的 pending/provider/conflict/lock 状态被新 Profile 继承，并让失活引擎继续监听网络/自动同步；仅停用 listener 还会漏掉失活期间的在线状态变化。真实截图还显示通用横向卡片把操作按钮压成逐字竖排。 | Profile 表单未接入 Tauri dialog；路径规范化缺少根语义；SyncEngine 身份与其 ChangeTracker/provider/conflict/lock 生命周期不一致；Settings 使用跨 Profile 的局部异步消息；重激活时未读取当前 `navigator.onLine`；详情卡误用通用横向布局。 | 接入 typed Tauri directory boundary；保留 drive/UNC/POSIX roots 与 literal plus；由 Profile Store 的统一激活入口切换到按 Profile 缓存、身份不可变且可停用/恢复的 SyncEngine；隔离状态订阅与晚到结果，重激活时校正网络状态并在恢复在线时触发原 Profile recovery sync，Settings 只派生当前引擎结果；对 Profile 详情卡增加局部响应式 grid。 | Profile/Desktop/Sync Vitest PASS 5 files / 38 tests，新增 pending/outbox、late-result、listener、missed network transition 与 auto-sync lifecycle 回归；targeted ESLint、`vue-tsc`、production build PASS；真实 Tauri 的非默认 Profile 同步审计归属、生命周期和目录取消均通过，最终 native 8/8、SVG 9/9、visual 11/11。没有自动选择真实目录，也没有执行 XHS/Zhihu 登录、上传或发布。 |
| F-PUB-002 | medium | `PUB-CTRL-002` | fixed | Open `/publish` directly and scan visible controls. Platform cards, preset buttons, theme navigation, copy/download/source buttons, preview/code tabs, and code-copy button lacked explicit `type="button"`. | Publish route controls predated the component accessibility contract for explicit non-submit button types. | Add explicit `type="button"` to all Publish route non-submit buttons while preserving honest empty state and editor-store based selected-article behavior. | Static scan returned `missingType: 0`; targeted ESLint passed; direct `/publish` re-check found honest empty state with disabled copy/download and no missing button types; SPA navigation from a loaded Workstation article showed the real article marker, enabled copy/download, no missing button types, no unnamed buttons, no console errors, and no horizontal overflow. |
| F-APP-001 | medium | `APP-CTRL-001` | fixed | Static scan of `inkforge/src/App.vue` found two global error-boundary recovery buttons without explicit `type="button"`: refresh page and dismiss/recover. | The app-shell error boundary predated the component accessibility contract for non-submit buttons. | Add explicit `type="button"` to both error-boundary buttons without changing retry/dismiss handlers. | `App.vue` static scan returned `missingType: 0`; `pnpm -C inkforge exec eslint src/App.vue --ext .vue --quiet` passed. |
| F-CMD-001 | medium | `CMD-007` | fixed | From `/drafts`, open Command Palette, search `Open publish workspace`, and execute it with no selected article. Before repair, the palette stayed open and showed `permission_denied` instead of navigating to `/publish`. | `publish.open` is a navigation/preflight command but incorrectly required `publish.execute`, which is only granted when an active document exists. | Remove `requiredPermissions: ['publish.execute']` from the navigation command; keep real publish/export actions guarded elsewhere. | GitNexus impact for `createBuiltinCommands` returned LOW risk; targeted ESLint for `src/services/command/builtin.ts` passed; browser re-check routed to `/publish` and showed the honest empty state with no console errors. |
| F-FILE-001 | high | `CMP-FILE-001` | fixed | In FileManager, click `新建` -> `新建空白文章`. The created IndexedDB article was selected immediately, so `articleStore.selectArticle()` converted the default `new` status to `read`; the row appeared as `无标题文章已读` and did not stay in `草稿/写作`. The same missing status existed in the category context-menu path `在此分类新建文章`. | `FileManager.vue` called `articleStore.addArticle()` without an explicit lifecycle status. `addArticle()` defaults to `ARTICLE_STATUS.NEW`, and selecting a `NEW` article marks it `READ`, which is correct for imported/read articles but wrong for a blank document creation control. | Pass the same draft creation contract used by the Command Palette: `sourceName`, empty `rawContent`/`description`, and `status: ARTICLE_STATUS.DRAFT` in both FileManager creation paths. | GitNexus could not map `FileManager`, `createBlankArticle`, or `ctxNewArticleInCategory`; `addArticle` impact returned LOW. `pnpm -C inkforge exec eslint src/components/file/FileManager.vue --ext .vue --quiet` passed. Desktop re-check created draft `c1c237ad-784d-4713-bc8f-7208db8944ab` with `status=draft`; category context-menu creation created draft `486f0b01-a353-4640-93be-67ee19287ef5` in the real category; mobile 390px re-check created draft `abaaa633-0833-4c26-b2b7-d375cb239c9f` with no overflow or console errors. |

| F-HUB-001 | medium | `HUB-CTRL-001` | fixed | Open Hub `/` with real local article data and scan visible controls. Article filter tabs lacked explicit `type="button"`; the Settings icon and AI inspiration setup/refresh controls lacked complete accessible button contracts; the lower article search input and sort select had no accessible names. | Hub controls were built across several UI waves and some controls predated the shared component contract for explicit non-submit button type and accessible names. | Add explicit `type="button"` to Hub non-submit buttons, add `aria-label` to icon/AI controls, lower search input, and sort select, and keep the visible Chinese content unchanged. | GitNexus impact for `HubView` returned LOW risk. Targeted ESLint for `HubView.vue` passed. Robust static tag scan returned `missingButtonType=0`, `selectsWithoutName=0`, `inputsWithoutName=0`. Playwright desktop and 390px mobile re-checks both found real article stats, no visible buttons without type, no unnamed controls, no overflow, and no console errors. |
| F-HUB-002 | high | `HUB-TEMPLATE-001`, `RTE-001`, `RTE-002` | fixed | From Hub, click a template card. Before repair, IndexedDB gained a real template draft `8536c26c-747b-467d-b689-799f7cfc14fd`, but the app navigated to `/workstation?id=abaaa633-0833-4c26-b2b7-d375cb239c9f`, the previously active persisted tab, so the user did not land on the new template article. A 2026-07-11 real Tauri replay reproduced the same regression for the visible blank-draft control: the new empty draft was created and selected, then async layout restore switched back to the older active article. | `WorkstationView.applyPersistedLayoutRecord()` restored the persisted active tab after route navigation and selected it even when the route already carried an explicit `id`. The layout restore then replaced the route with the old active article. | Treat an explicit route article id as higher priority than persisted active-tab selection. Restore the remaining layout/tab metadata, then reopen and activate the explicit route article; only use the persisted active article when no valid route id exists. | Historical Playwright proof created and opened exact template draft `7039324f-00fe-46c2-b8eb-18969b7c3c3c`. For the 2026-07-11 recurrence, GitNexus impact on `applyPersistedLayoutRecord` was LOW: 1 direct caller, 2 total affected nodes, and 0 execution flows. Targeted ESLint and `vue-tsc` passed; production build passed with 4660 modules. After independent review, the real Tauri/WebView2 test now deterministically persists an older seed id in `layoutStates`, creates the target through the visible Hub control, waits until the old record has been hydrated, proves route/current-content/selection remain on the target, persists and reloads the target id, then restores Settings and cleans only its run-created drafts/tabs/layout references. Final strict replay passed 1/1 on WebView2 `150.0.4078.65`. |
| F-HUB-003 | high | `CMP-HUB-001`, partial `RTE-001` | fixed | CloakBrowser inspection found that Hub's empty Tag Cloud rendered invented labels (`草稿`, `已发布`, `灵感`, `笔记`, `待整理`), weekly bars/category actions/category cards/article cards used visual-only `div`/`span` controls, and the existing quick-action menu had no mounted header or floating trigger. After mounting the triggers, native Enter initially opened the menu, shifted focus, and let the same activation create a draft immediately. | Empty-state presentation fabricated insight data; mature visual surfaces were not native controls; existing menu state/handlers had been disconnected from their triggers; and trigger Enter/Space did not consume the native activation before moving focus to the first menu item. | Render only caller-supplied tag data, preserve a truthful text-only empty state, use native buttons/links for chart/category/article actions, mount the existing header/FAB triggers, and consume trigger Enter/Space before opening and focusing the menu. | CloakBrowser verified the visible 1440x900 state, native control semantics, no fake labels, three menu items, Enter/Space, ArrowDown/Home/End, Escape focus return, and zero console errors. Isolated Tauri/WebView2 created a real draft through the keyboard menu, saved 105 tokens, observed real article/heatmap/trend/distribution/timeline output, followed the exact article link by keyboard, and cleaned the created draft through production trash/tab/layout paths. Focused Hub passed 1/1; complete editor Settings passed 9/9; AI Settings passed 1/1. Full Vitest passed 108 files / 1825 tests, complete source ESLint and `vue-tsc` passed, and the current-round SVG/style gate remained ready. |
| F-E2E-001 | medium | `CMP-HUB-001` test infrastructure | fixed | After restart, `tauri-driver` attached to the visible splash WebView; the splash then closed before the spec could reach the application, producing `no such window: target window already closed / web view not found`. | The shared WDIO `before` hook verified the driver-owned data directory but assumed the current handle was the main application WebView. | After the existing isolation guard, enumerate surviving handles with a 15-second bound and select the one containing `.ink-titlebar`; fail closed if no main application WebView survives. | The focused Hub flow passed 1/1, complete editor Settings passed 9/9, and the independent AI Settings spec passed 1/1. Each run logged successful temporary WebView2 data-root verification before selecting the main WebView. No production profile or account state was used. |
| F-THEMES-002 | medium | `THEMES-CTRL-002`, `RTE-007` | fixed | Open `/themes` and inspect theme cards. Ten preset cards were clickable `div.theme-card` elements rather than real controls, so keyboard users and assistive technology had no button role, selected state, or focusable control for the primary theme selection action. | The card grid was implemented visually first and did not use a native interactive element for each preset. | Convert preset cards to `<button type="button">`, add `aria-pressed` and an accessible selection label, and keep the card visual style by resetting button appearance. | GitNexus impact for `ThemesView` returned LOW risk. Targeted ESLint passed. Static scan found 5 button tags and `missingButtonType=0`. Playwright selected `法学研讨`, applied it through `themeStore.applyPreset()`, observed `inkforge_theme_preset=legal`, reloaded Themes to see the legal card as current, then restored `论文翻译` / `thesis`; no unnamed buttons, missing button types, overflow, or console errors. |
| F-DESKTOP-001 | high | `DSK-001`, `DSK-002`, `DSK-004`, `DSK-005`, `SET-ABOUT-001` | fixed | In the real Tauri 1/WebView2 runtime, `utils/platform.ts` documents that `withGlobalTauri=false` can expose `__TAURI_INVOKE__` / `__TAURI_IPC__` without relying only on `window.__TAURI__`; `services/desktop/environment.ts` only recognized `__TAURI__` and `__TAURI_INTERNALS__`, so Settings About desktop diagnostics could classify a native shell as web and block native command calls. | Desktop runtime detection duplicated an older, narrower Tauri-global probe instead of matching the central platform detector's IPC-only Tauri 1 markers. | Extend `DesktopRuntimeSignal` and `detectTauriRuntimeSignal()` to recognize `__TAURI_INVOKE__`, `__TAURI_IPC__`, `__TAURI_METADATA__`, and `__TAURI_POST_MESSAGE__`; add unit and real Tauri/WebDriver regression coverage. | GitNexus impact: `detectTauriRuntimeSignal` LOW, `detectDesktopRuntime` MEDIUM with Desktop-only upstream callers and 0 affected processes. `pnpm -C inkforge exec vitest run src/services/desktop/environment.test.ts --reporter=default` passed 3 tests. `pnpm -C inkforge exec eslint src/services/desktop/environment.ts src/services/desktop/types.ts src/services/desktop/environment.test.ts tests/e2e/specs/native-runtime.spec.cjs --quiet` passed. `NODE_OPTIONS=--max-old-space-size=4096 pnpm -C inkforge build` passed. `pnpm -C inkforge test:e2e` passed 3 real Tauri/WebDriver specs / 19 tests; the new native spec proved Settings About reports `Tauri Desktop`, gets the Rust window list, and fails closed for empty/missing reveal paths and unsafe/malformed shell URLs. |
| F-DESKTOP-002 | medium | `DSK-006` | fixed | The Desktop capability matrix advertised `Clipboard Text: available` in Tauri, but the desktop service/store exposed no typed clipboard-text boundary and there was no real Tauri/WebView2 read/write proof. UI code elsewhere used raw `navigator.clipboard`, which is not enough to prove the native Tauri clipboard allowlist path. | Capability reporting and real command surface were disconnected for clipboard text: Tauri allowlist was configured, but `services/desktop/index.ts` did not wrap `@tauri-apps/api/clipboard` or provide a store action for safe native-shell verification. | Add typed `writeClipboardText()` and `readClipboardText()` desktop service functions with Tauri, degraded web, unavailable, and failed result branches; expose them through `useDesktopStore`; add unit tests and a real Tauri/WebDriver probe that restores the original text clipboard when text was present. | GitNexus impact: `buildDesktopCapabilityMatrix` LOW and `useDesktopStore` LOW; `DesktopCommandResult` type target was not found. Context7/Tauri docs plus local `@tauri-apps/api/clipboard.d.ts` confirmed Tauri 1 `writeText()` / `readText()` API. `pnpm -C inkforge exec vitest run src/services/desktop/environment.test.ts src/services/desktop/index.test.ts --reporter=default` passed 8 tests. Targeted ESLint and `vue-tsc --noEmit --pretty false` passed. `NODE_OPTIONS=--max-old-space-size=4096 pnpm -C inkforge build` passed. Targeted real Tauri/WebDriver native-runtime replay passed 3/3, including exact text write/read and exact restore readback when the original clipboard content was text; full `pnpm -C inkforge test:e2e` passed 3 specs / 20 tests. |
| F-CMD-005 | medium | `CMD-005`, `SET-ABOUT-001` | fixed | The built-in updater command was wired to `updaterStore.checkNow()` and `openSettings('about','updater')`, but the Command Palette icon registry did not include the declared `RefreshCw` icon and there was no executable proof that the real palette command reached the updater boundary, persisted Settings status, and wrote audit evidence. | Command metadata and Command Palette icon registration had drifted, and the updater acceptance row relied on code review instead of a real Tauri/WebDriver command-to-store-to-audit regression. | Register `RefreshCw` in `CommandPalette.vue`; add a native-runtime WebDriver test that opens the real Command Palette, searches/clicks `Updater: Check for Updates`, waits for Settings About updater, and reads real IndexedDB audit rows without seeding fake update metadata. | GitNexus impact for `createBuiltinCommands`, `checkNow`, and `useUpdaterStore` returned LOW risk with 0 affected processes. `pnpm -C inkforge exec vitest run src/services/updater/updater.test.ts --reporter=default` passed 12 tests. Targeted ESLint, `vue-tsc --noEmit --pretty false`, production build, and targeted `native-runtime.spec.cjs` passed 4/4; the new test proved `disabled/build-config`, persisted `lastCheckAt`/`lastStatus`/`lastDisabledReason`, `command.execute` audit, `updater.user-check` audit, and no install/download/relaunch claim. |
| F-SETTINGS-005 | medium | `SET-EDITOR-005` | fixed | Enable the Smart Punctuation master and all ten rules through visible Settings, wait for the real debounce/reload, then type each trigger in the mounted Workstation editor. Nine rules transformed, but `A - - B` remained the raw ASCII sequence in real WebView2 even though the direct unit helper passed. | `createSmartPunctuationTransaction()` assumed every ProseMirror `handleTextInput` payload had exactly one character. WebView2 can coalesce the adjacent space and hyphen into one DOM-change batch, and the attempted `appendTransaction` fallback repeated the same single-character arming assumption. Its unit test started from an already-inserted document, so it did not exercise the failing input lifecycle. | Keep the existing extension as the single behavior owner; allow only enabled `spacedDash` to consume a multi-character input batch, preserve any unmatched inserted prefix in the same transaction, and remove the unrelated post-transaction scan. Add direct batched-payload tests plus visible Settings and real-keyboard Tauri coverage. | GitNexus impacts for `SmartPunctuation`, `findRuleReplacement`, and `createSmartPunctuationTransaction` were LOW with 0 affected processes. Final SmartPunctuation tests passed 23/23; focused Settings tests passed 2 files / 26 tests; editor/layout regression passed 6 files / 61 tests; full serial Vitest passed 106 files / 1812 tests. Targeted ESLint, `node --check`, `vue-tsc`, and the 4660-module production build passed. Strict real Tauri/WebView2 replay passed 1/1, then review-hardened targeted 1/1 and complete editor Settings 2/2 passed on WebView2 `150.0.4078.65`, including code-block/Source/URL guards, fresh errors, and exact localStorage-state cleanup. |
| F-SETTINGS-006 | high | `SET-EDITOR-006` | fixed | Configure document/daily/weekly goals, create a real four-word draft, and compare Hub and Workstation totals. The first strict replay showed the active draft contributed eight words to Workstation daily/weekly totals instead of four. Independent review also showed that the first invalid-input implementation treated non-empty `-2` as explicit disable and overwrote the prior target. | `writingWindowEntries` mapped article records to id-less `WritingWindowEntry` objects, then searched those mapped entries for `id`; the check could never succeed and appended the active article a second time. Both Settings and Writing Assist also assigned normalization failure directly as `undefined`, conflating invalid input with an intentional empty value. | Check active-article presence against the real article collection; centralize safe positive-decimal normalization; keep local Settings drafts; commit explicit empty only on blur; preserve the last valid target for non-empty invalid input; show typed `aria-invalid` feedback in Settings and Writing Assist; add real baseline-delta and persistence/reload proof. | GitNexus impacts for writing-goal helpers and `writingWindowEntries` were LOW with 0 affected processes. Focused regression passed 3 files / 21 tests; full serial Vitest passed 106 files / 1814 tests; targeted ESLint, type/build, and the 4660-module build passed. Rebuilt Tauri/WebView2 writing-goal proof passed 1/1, complete editor Settings passed 3/3, and the remaining serial native/SVG/visual suites passed 8/8, 10/10, and 11/11. Independent re-review returned `P0-P3 none`. |
| F-SETTINGS-007 | high | `SET-AI-002` | fixed | Persist a non-empty `ai.systemPrompt`, then inspect both AI execution boundaries. `aiChat.buildMessages()` included it, but every outline, polish, title, summary, transcript, continuation, expansion, and condensation task flowed through `useAIStore.generate()` / `streamGenerate()` whose local `buildMessages()` used only the built-in task prompt. Chat emitted global prompt and document context as two system messages even though Anthropic consumes only the first; independent review then proved `regenerateLast()` dropped the first send's document context entirely. | The Settings field was wired only to the chat store, not to the shared writing-task message builder. Chat did not model the document context as conversation state, so separate messages and regeneration could both discard it. | Add one small provider-neutral composer; route writing tasks through one ordered system message; merge chat global prompt plus bounded document context; retain the last valid context for regeneration and clear it with the conversation. Preserve empty-prompt behavior and all provider/request boundaries. | GitNexus impacts for `useAIStore`, both `buildMessages` functions, `send`, `clear`, and `regenerateLast` were LOW. The first TDD test failed because the shared composer did not exist; the reviewer-driven regression then failed because the second real Ollama request contained only the global prompt. Final focused Vitest passed 4/4 by executing `generateOutline`, `send`, and `regenerateLast` through the real Ollama Provider and a pass-through native fetch to an unavailable local port; no response was fabricated. Targeted ESLint, `node --check`, `vue-tsc`, and the 4660-module build passed. Full serial Vitest passed 107 files / 1818 tests. Focused real Tauri/WebView2 passed 1/1, full desktop replay passed 5 specs / 33 tests, and independent re-review returned `P0-P3 none`. |
| F-SETTINGS-008 | high | `SET-DATA-001` | fixed | Persist `autoBackup=true`, `backupInterval=1`, and edit a real draft while the default Workstation manager panel remains collapsed. The strict/review loops found missing interval snapshots, pending-debounce loss on route leave, lifecycle/reentry gaps, same-document stale read-modify-write races, queued writes that could execute against a newly selected document, unexecuted failure paths, and weak timer-stop proof. | The only manager was conditionally mounted; watching the immutable content object reset the timer; route navigation did not own a current-flush boundary; asynchronous snapshots had no generation token or in-flight lock; content/version writes were not serialized or bound to enqueue-time content identity; and `EditorPanel` treated the store error state as permanently non-persistable. `createVersion()` also wrote the whole captured content object. | Keep one Workstation-owned manager; watch content id only; explicitly restart after version switch; block route leave only when the active document flush fails while allowing retry; add single-flight/generation guards; serialize body save/create/prune/switch in one rejection-safe queue; capture content id at the public action boundary and resolve detached queued writes from the real repository; persist only operation-owned fields; and expose real failure feedback. | GitNexus impacts remained LOW. Real Tauri proved the one-minute snapshot, route failure/retry, failed manual backup, same-document body/version/prune/switch equality, and a 65-second unmount window. A second real UI draft then provided cross-document proof: source saves/version were enqueued before production selection switched, production reload retained the exact source body/version once, while target body, complete persisted versions, and `currentVersionId` stayed unchanged and final live state exactly equaled IndexedDB. Strengthened focused Data passed 1/1; final independent re-review returned `P0-P3 none`. |
| F-SETTINGS-009 | medium | `SET-DATA-002` | fixed | Inspect the Data diagnostics failure paths. A failed Dexie read was logged and represented as zero records/tables; rejected refreshes were logged but the visible surface did not say that its supposedly complete snapshot was partial. Independent review also found no executable failure-state evidence. | Runtime diagnostics lacked explicit collection state/message and aggregate partial-failure status, and the first acceptance covered only the ready path. | Add typed `idle/ready/error` IndexedDB diagnostics plus visible `idle/ready/limited` aggregate state. Use reversible browser API fault injection only to exercise failure rendering, restore the original descriptors, then rerun direct real API comparisons; never seed data or fabricate success. | Real Tauri observed `storage=limited`, `IndexedDB=error`, `Cache=error`, and aggregate `部分诊断不可用` under reversible failures. After restoration, exact localStorage bytes/keys, all 34 object-store counts, Cache buckets, Service Worker summary, quota, and bounded adjacent usage matched independent reads. Targeted lint/type/build, full Vitest 108/1820, editor Settings 4/4, and full Tauri 34/34 passed. |

| F-CATEGORY-001 | high | `CMP-CATEGORY-001`, partial `RTE-001` | fixed | Real Hub/Workstation acceptance found category creation could accept normalized duplicates concurrently, expected conflicts cleared modal state, FileManager could omit empty category nodes, and no native end-to-end proof covered article migration after delete. | Validation, duplicate reservation, visible error state, route hydration, and category-tree rendering were split across UI/store boundaries without one enforced contract. | Reuse the category schema; normalize and reserve pending names in the store; retain input on expected conflict; load/render categories independently from article groups; keep delete migration authoritative. | Two consecutive real Tauri/WebView2 focused replays passed 1/1 using visible controls, native right-click, read-only Pinia/IndexedDB evidence, refresh, and production cleanup. Category create/duplicate/article relation/rename/Hub filter/delete-to-null all passed. |
| F-TAGS-001 | high | `CMP-TAGS-001` | fixed | Real Tauri/WebView2 reached the tag manager but WebDriver correctly rejected the native edit click: in a 1400px viewport the fixed modal was constrained by the Workstation side panel to about 231px, compressing each column to about 86px so a merge-source button covered the edit button. Async failures also cleared input/edit/merge state before persistence success. | The modal rendered inside a transformed/constraining panel instead of the existing body-Teleport modal boundary, and transient UI state was cleared at emit time rather than after the real store action resolved. | Follow the existing `<Teleport to="body">` pattern; keep one flex-column modal with scrolling/narrow stacking; use success callbacks so rejected actions preserve user input; keep native sibling controls and repository errors visible. | After repair the modal measured 860px with two non-overlapping about-400px sections. Two consecutive native focused replays passed 1/1 through create/assign/reload/OR-AND filter/Hub cloud/rename/color/duplicate/delete/merge/remove/cleanup. Tag repository tests passed 13/13; no synthetic click or direct persistence write was used. |

## Severity Scale

- `critical`: data loss, fake success for durable operations, native blocker, or route-breaking crash.
- `high`: primary user workflow broken or Settings persistence incorrect.
- `medium`: secondary workflow broken, accessibility/keyboard issue, or recoverable service error not surfaced.
- `low`: polish issue with no functional loss.
- `info`: planning or evidence note.

## F-AI-001 — AI chat textarea DOM type trips full lint

- Severity: low
- Status: fixed
- Area: Workstation / AIChatPanel
- File: `inkforge/src/components/ai/AIChatPanel.vue`
- Reproduction: `pnpm -C inkforge exec eslint src --ext .ts,.tsx,.vue --quiet`
- Failure: `HTMLTextAreaElement is not defined` from ESLint `no-undef`.
- Root cause: the component used the concrete DOM constructor type `HTMLTextAreaElement` in a ref annotation, while the current lint globals do not expose that name.
- Fix: use `HTMLElement | null`, which is sufficient for the actual operations (`style.height` and `scrollHeight`) and does not change runtime behavior.
- Verification: targeted lint, full lint, typecheck, full Vitest, web build, and Tauri package build all passed on 2026-07-05.

## F-SETTINGS-010 — WebView2 transient blur cancels shortcut recording

- Severity: high
- Status: fixed
- Area: Settings / Shortcuts
- Files: `inkforge/src/components/settings/ShortcutInput.vue`, `inkforge/src/views/SettingsView.vue`, `inkforge/tests/e2e/specs/editor-settings.spec.cjs`, `inkforge/tests/e2e/wdio.conf.cjs`
- Reproduction: open `/settings?tab=shortcuts` in the real Tauri/WebView2 runtime, click a shortcut trigger, wait for its recording label to render, and send a real modifier chord. The trigger briefly entered recording mode and then cleared it before the key event; WebView2 emitted `blur` followed by `focus` while the focused button's recording DOM changed.
- Root cause: `ShortcutInput.handleBlur()` synchronously stopped recording on every blur. That treated WebView2's transient same-element blur/refocus as a real focus departure, making native shortcut recording deterministic only by accident.
- Fix: defer element blur resolution by one animation frame and stop only when the document lost focus or `document.activeElement` is no longer the original button. A window-level blur listener preserves native app focus-loss behavior, and unmount removes the listener. Stable accessible action names were added while conflict/store/persistence logic remained unchanged. The WDIO harness now verifies each launched session's child WebView2 process uses a native driver-owned temporary `%TEMP%/scoped_dir*/EBWebView` data root and rejects every test unless that session guard passed. Teardown waits for the driver's `close` event before removing only the verified scope; timeout or process error fails closed and skips deletion.
- Verification: focused extension/shortcut proof passed 2/2; visible focus transfer and native minimize/restore both stopped recording; complete editor Settings passed 6/6; the memory-safe serial full desktop run passed 5 specs / 36 tests. All five native sessions passed the isolation guard, left zero verified scoped directories, and did not change the production data-root timestamp. The real Data timer proof passed 1/1 in 2 minutes 24.2 seconds under the 300-second Mocha ceiling. Targeted ESLint, `node --check`, the 4660-module production build, extension Vitest 6/6, and serial full Vitest 108 files / 1820 tests passed. Independent review found one cleanup-order P2 and two P3 wording/lint issues; after the driver-close fail-closed repair, regex cleanup, fresh 2/2 Tauri replay, and documentation correction, bounded re-review returned `P0-P3 none`.

## F-SETTINGS-011 — About registry drift and unproven runtime boundaries

- Severity: medium
- Status: fixed
- Area: Settings / About / AI
- Matrix rows: `SET-ABOUT-002`, `SET-ABOUT-004`, `SET-ABOUT-005`, `SET-ABOUT-006`, `SET-ABOUT-007`, `SET-ABOUT-008`
- Files: `inkforge/src/stores/settings.ts`, `inkforge/src/stores/settings.ai.test.ts`, `inkforge/src/views/SettingsView.vue`, `inkforge/tests/e2e/specs/editor-settings.spec.cjs`, `.trellis/spec/frontend/state-management.md`, task evidence
- Reproduction: search Settings for `Feature Flags` or `代理设置`. The registry declared both controls on About although their real sections are owned by the AI tab, so search navigated to a tab where the requested control was absent. Three reserved flags were described as if production consumers existed, proxy copy did not state that it was configuration/preview only, and FTUE reset copy described a later-entry behavior even though the real store opens Welcome immediately. The six rows also lacked one real Tauri path proving debounce/reload, performance IndexedDB samples, typed rollback, and FTUE persistence together.
- Root cause: the Settings registry, visible capability copy, reset ownership, and acceptance evidence evolved separately from the actual template/store boundaries. Feature flags and proxy were rendered on AI but reset by About. The generic route-ready helper also treated hidden `v-show` tab contents as ready, and the first E2E draft clicked hidden switch inputs, omitted the existing typed confirmations, used an unscoped skip selector, and lacked independently reproducible schema/unsupported/reset evidence.
- Fix: align registry and reset ownership so AI owns `ai + featureFlags + proxy` while About owns `advanced` and preserves CustomCSS; label unused flags as reserved and only `performance-metrics` as connected to the real SLO ledger; state that proxy is validated configuration plus masked preview, not a connected request path; correct FTUE reset copy to immediate Welcome behavior; add stable accessible/read-only evidence points; and exercise all six rows through visible controls plus read-only Pinia/localStorage/IndexedDB evidence. The E2E requires visible route targets and switch tracks, enters `RESET`/`RESTORE`, proves current schema after the first real write, compares real WebView2 support rows, waits for the disabled collector to stop, sets visible Developer Mode/updater/CustomCSS sentinels, proves full About reset ownership and snapshot retention, and never writes or deletes the Settings `localStorage` key directly.
- Verification: after the second independent review's two P1 and one P3 evidence findings were repaired, focused About Tauri/WebView2 passed 1/1 in 25.9 seconds. The complete `editor-settings.spec.cjs` passed 7/7 in 4 minutes 23.7 seconds on WebView2 `150.0.4078.65`. Focused Settings/migration/performance Vitest passed 6 files / 24 tests; final serial Vitest passed 108 files / 1820 tests. `node --check`, targeted ESLint, `vue-tsc --noEmit --pretty false`, the 4660-module production build, and `style-proof:current-round` passed. The style gate remained `application-acceptance-ready` with `canClaimCurrentRoundTarget=true`, 27 SVG modules, 108 rendered module/persona pairs, 17 WeChat choices, 13 selectable/rendered choices, zero local issues, `xhsZhihuPublishAutomationDeferred=true`, and `canClaimReleaseComplete=false`. Direct persistence writes remain absent; the redacted raw command record is `verification-about-20260713.txt`.

## F-CMD-006 — Command Palette permission, persistence, and keyboard boundaries drifted apart

- Severity: high
- Status: fixed
- Area: Global / Command Palette
- Matrix row: `CMP-COMMAND-001`
- Files: `inkforge/src/components/command-palette/CommandPalette.vue`, `inkforge/src/services/command/executor.ts`, `inkforge/src/stores/command-palette.ts`, `inkforge/src/services/command/fuzzy-search.test.ts`, `inkforge/tests/e2e/specs/native-runtime.spec.cjs`, `inkforge/vite.config.ts`, `.trellis/spec/frontend/state-management.md`, task evidence
- Reproduction: in a real isolated Tauri/WebView2 session, open the palette from `/drafts`, search `Open drafts`, traverse by native Tab, and try to favorite it by keyboard. Before repair, the star was only an SVG click target inside the command button, Favorites/Recent/Featured could repeat the same command, `requiredPermissions` accepted any one declared permission, and `dev.togglePanel` fell back to the generic icon because `Activity` was not registered. Strict failure-path replays then showed that a rejected async command unmounted its focused option, moved focus to `body`, left Escape unable to close the palette, and did not return focus to search. Independent review also found no modal focus wrap, keyboard navigation following raw relevance rather than grouped visual order, invalid listbox ownership around sibling favorite buttons, a five-item Favorites cap that could hide every sixth-or-later favorite after lower-priority exclusion, and insufficient proof that denial appended no successful command audit.
- Root cause: command execution, quick-section composition, selection order, accessibility semantics, and modal focus management had evolved independently. The executor used existential permission matching for a plural all-required contract; quick sections did not reserve command ids and later capped authoritative favorites; the favorite affordance lacked a native control; Tab was intercepted as execution; and the component assumed focus could never leave the overlay. The production minifier also inherited Terser's CPU-based worker count, which intermittently exceeded this Windows machine's stable build envelope.
- Fix: require every declared permission; keep every favorite authoritative while deduplicating lower-priority quick sections; expose each favorite as a sibling native button; render command results as a list/listitem surface with a polite active-command announcement; make Arrow/Home/End follow rendered group order; trap native Tab/Shift+Tab at modal boundaries; return failed execution focus to search; clear stale errors when the query changes so Enter performs an observable retry; preserve safe Enter targeting and command ids; register Lucide `Activity`; keep an open-only window Escape listener with unmount cleanup; and limit Terser to one worker without disabling minification. Denied execution now has separate unit and native evidence: the unit boundary proves all-required semantics plus `auditLogged=false` and zero handler/audit calls, while the native boundary proves the protected surface stays closed and the durable `command.execute` success count does not change across click or Enter retry.
- Verification: focused command regression passed 10/10, including seven overlapping favorites and explicit handler/audit spies. `node --check`, targeted ESLint including `vite.config.ts`, complete production-source ESLint, `vue-tsc --noEmit --pretty false`, and the final compressed 4660-module production build passed; the final Vite phase completed in 55.40 seconds with minification enabled and `maxWorkers=1`. The focused native flow passed 1/1 after strict repair loops and proved native Ctrl+K, Tab/Shift+Tab focus wrapping, list/listitem semantics, keyboard favorite activation, read-only IndexedDB persistence, grouped visual-order Home/End, route filtering/execution, typed runtime permission denial with zero `command.execute` count delta, stale-error clearing, Enter retry with search-focus restoration, failed-history exclusion, registered `Activity`, and Escape recovery. Its three audit reads are required to succeed, and its pre-denial success baseline is required to be non-zero, so an IndexedDB read failure cannot satisfy the zero-delta assertion. Complete `native-runtime.spec.cjs` passed 9/9; serial full Vitest passed 108 files / 1824 tests. `style-proof:current-round` remained `application-acceptance-ready` with 27 SVG modules, 108 module/persona pairs, 17 WeChat choices, 13 selectable/rendered choices, 45 sample SVG modules, and zero local issues while preserving `canClaimReleaseComplete=false` and manual Xiaohongshu/Zhihu publishing.

## F-SETTINGS-012 — Settings import discarded its promised local rollback point

- Severity: high
- Status: fixed
- Area: Settings / Data / Migration
- Matrix row: `RTE-005`
- Files: `inkforge/src/stores/settings.ts`, `inkforge/src/views/SettingsView.vue`, `inkforge/src/stores/settings.data.test.ts`, `inkforge/tests/e2e/specs/editor-settings.spec.cjs`, `.trellis/spec/frontend/state-management.md`, task evidence
- Reproduction: export Settings JSON from the visible Data tab, change a validated field, import that file, type `IMPORT`, and then try `恢复最近回滚点`. The UI promised that import would create a rollback point, but `importSettings()` created the snapshot and immediately replaced the complete Settings object with the imported candidate, including the candidate's older `advanced.migrationSnapshots`; the newly-created local snapshot was therefore lost.
- Root cause: rollback creation and whole-object candidate assignment were ordered correctly, but the local rollback ledger was not retained across the assignment. The Data tab also created an ephemeral detached file input, making the real file boundary inaccessible to stable UI automation and assistive technology.
- Fix: retain the bounded local `migrationSnapshots` array immediately after `createRollbackPoint()` and reattach it after applying the validated candidate. Reuse the existing visually-hidden input pattern for one persistent JSON file input; keep it out of the keyboard tab order and accessibility tree because the labelled visible Import button is the sole user entry. Keep preview, typed confirmation, validation, migration, persistence, and rollback store boundaries unchanged.
- Verification: the focused store regression passed 3/3 and proves import, snapshot increment, restore, and fresh-Pinia persistence. The broader Settings/migration regression passed 5 files / 20 tests, and full serial Vitest passed 108 files / 1825 tests. Targeted ESLint, `node --check`, `vue-tsc --noEmit --pretty false`, and the 4660-module production build passed. A rebuilt real Tauri/WebView2 replay passed 1/1 using one actual temporary JSON path twice through the production file input; it keyboard-activated the visible Import button both times, proved picker click count `1 → 2`, cleared input value after each selection, rejected invalid content without mutation, then accepted rewritten valid content from the same path with typed `IMPORT`. It also proved keyboard-activated visible export history with non-zero bytes, Pinia/localStorage/visible-control equality, reload persistence, typed restore, second reload persistence, preserved local snapshot ledger, and zero fresh runtime errors. The import case is registered last in the disposable isolated session because its rollback audit row is intentionally durable; a failure after successful import invokes the production restore action in `finally`. Earlier expanded native loops exposed two WebView2 interactability gaps in the writing-goal editor and Settings export control; both use scroll, focus, and real keyboard activation without bypassing production handlers. The dependent pair passed 2/2 and the final complete editor Settings suite passed 8/8 in 4 minutes 26 seconds. `style-proof:current-round` returned `current-round-ready` with 27 modules, 108 module/persona pairs, 17 WeChat choices, 13 selectable/rendered choices, and zero local issues. No direct Settings persistence write or external platform action was used.

## F-HUB-003 — Hub insights fabricated empty-state data and primary actions were not keyboard-native

- Severity: high
- Status: fixed
- Area: Hub / Insights / Quick actions
- Matrix rows: `CMP-HUB-001`, partial `RTE-001`
- Files: `inkforge/src/components/hub/insights/TagCloud.vue`, `inkforge/src/views/HubView.vue`, `inkforge/tests/e2e/specs/editor-settings.spec.cjs`, `inkforge/tests/e2e/wdio.conf.cjs`, `.trellis/spec/frontend/component-guidelines.md`, task evidence
- Reproduction: inspect Hub with no real tags and observe five invented labels in the Tag Cloud. Inspect the weekly bars, category actions/cards, and article cards: they are mouse-oriented visual elements rather than native controls. The quick-action menu implementation exists, but neither header nor floating trigger is mounted. After restoring the triggers, focus one and press Enter: before the final keyboard repair, the same native event opened the menu, moved focus to its first item, and immediately created a draft.
- Root cause: the empty state used fallback product-looking data; interactive styling had drifted away from native semantics; trigger markup was removed while menu machinery remained; and focus transfer happened inside an unconsumed Enter/Space activation.
- Fix: remove all fallback labels and render only `props.tags`; keep a truthful text-only empty hint; convert chart/category/article actions to native buttons or links while preserving the existing design; mount the existing header and FAB triggers with menu ARIA; and prevent the opening Enter/Space default before moving focus to the first menu item.
- Verification: CloakBrowser confirmed the 1440x900 visual state, native control tags, no fabricated cloud, correct three-item menu ownership, Enter/Space opening, ArrowDown/Home/End traversal, Escape focus restoration, and no console errors. The isolated Tauri/WebView2 flow created a real draft from the keyboard menu, persisted 105 tokens through the mounted editor and Ctrl+S, returned to Hub, required the exact article link plus real heatmap/trend/distribution/timeline surfaces, reopened that article by keyboard, and captured zero fresh runtime errors. Cleanup used existing production trash/tab/layout actions. Focused Hub passed 1/1; complete editor Settings passed 9/9; AI Settings passed 1/1; serial full Vitest passed 108 files / 1825 tests; source ESLint, `vue-tsc`, build, and the current-round SVG/style gate passed. The later `F-CATEGORY-001` closure leaves `RTE-001` pending only for native Hub file import.

## F-E2E-001 — Shared Tauri harness could remain attached to the closing splash window

- Severity: medium
- Status: fixed
- Area: Tauri/WebView2 E2E harness
- Matrix row: `CMP-HUB-001` test infrastructure
- File: `inkforge/tests/e2e/wdio.conf.cjs`
- Reproduction: start an isolated Tauri session after restart. `tauri-driver` can return the splash handle; once the splash closes, the first app query fails with `no such window: target window already closed / web view not found` before the spec runs.
- Root cause: the shared hook validated the native driver-owned temporary WebView2 data root but assumed the current window handle already belonged to the hidden-then-shown main application WebView.
- Fix: retain the existing fail-closed data-root guard, then enumerate surviving handles for at most 15 seconds and select only the WebView containing `.ink-titlebar`; include the final handle count/error if selection fails.
- Verification: the focused Hub case, complete nine-case editor Settings spec, and independent AI Settings spec all passed. Each run logged isolation verification first and main-WebView selection second. The helper adds no dependency, does not access production profiles, and does not weaken any test assertion or timeout.

## F-CATEGORY-001 — Category validation, hydration, and delete migration lacked one executable contract

- Severity: high
- Status: fixed
- Area: Hub / Workstation / FileManager / category store
- Matrix rows: `CMP-CATEGORY-001`, partial `RTE-001`
- Files: `inkforge/src/schemas/article.ts`, `inkforge/src/stores/category.ts`, `inkforge/src/components/category/AddCategoryModal.vue`, `inkforge/src/components/file/FileManager.vue`, `inkforge/src/views/HubView.vue`, `inkforge/src/views/WorkstationView.vue`, `inkforge/tests/e2e/specs/editor-settings.spec.cjs`
- Reproduction: create a category from Hub, submit its normalized duplicate, deep-link to Workstation category management, create an article in that category, rename from its native context menu, filter by the renamed category on Hub, delete it, and refresh. Before the repair pass, normalized duplicate/pending reservations and retained modal errors were incomplete, empty categories could disappear from FileManager, and this complete relation/migration path had no real native proof.
- Root cause: category input validation, duplicate protection, route hydration, tree rendering, and article migration existed at separate boundaries but were not enforced as one store/UI contract.
- Fix: reuse the existing category schema for trim/length validation; normalize and reserve names while writes are in flight; expose expected store errors without clearing modal input; load categories on FileManager mount and render them without requiring an article group; preserve every article by migrating `categoryId` to `null` on category deletion.
- Verification: two consecutive focused real Tauri/WebView2 replays passed 1/1. The flow uses visible Hub/Workstation controls, a native right-click context menu, real production store/repository actions, and read-only Pinia/IndexedDB evidence. It proves create, duplicate rejection, categorized article persistence, rename, Hub filtering, delete migration, refresh persistence, and production cleanup. No synthetic contextmenu event or direct proof-row mutation is used. `RTE-001` remains pending only for native Hub file import.

## F-TAGS-001 — Tag manager escaped its side panel and async failures erased actionable state

- Severity: high
- Status: fixed
- Area: Workstation / Hub / tag system
- Matrix row: `CMP-TAGS-001`
- Files: `inkforge/src/components/tag-system/TagBadge.vue`, `inkforge/src/components/tag-system/TagInput.vue`, `inkforge/src/components/tag-system/TagManagerModal.vue`, `inkforge/src/components/tag-system/TagBrowser.vue`, `inkforge/tests/e2e/specs/editor-settings.spec.cjs`
- Reproduction: assign two tags to a real article, open the manager from Workstation, and click the second tag's native edit control. The real 1400×900 WebView reported a modal only about 231px wide because the nominally fixed backdrop was contained by the Workstation panel; the two about-86px columns overflowed across each other, so the matching merge-source button received the edit coordinate. Rejected create/update/merge actions could also clear the user's value or mode before the repository result was known.
- Root cause: unlike other project dialogs, the tag manager was mounted under a constraining ancestor instead of teleported to `body`; UI emit handlers treated dispatch as success instead of waiting for the real store action.
- Fix: follow the existing body-Teleport modal pattern, retain viewport-level fixed positioning and internal scrolling, and keep stacked sections at narrow widths. Keep tag selection/removal as separate native sibling buttons. Route all actions through the existing tag store and invoke completion callbacks only after success so repository errors and rejected input remain visible.
- Verification: after repair the same WebView measured an 860px dialog with two about-400px non-overlapping sections, and the native click reached its Lucide edit control. Two consecutive focused native replays passed 1/1 through visible create/assign, live and persisted `docTags`, `Article.tags` mirror, refresh, OR/AND filtering, Hub cloud, rename/color, duplicate retention, delete, merge, remove, cleanup, and production cleanup. Repository tests passed 13/13. No JavaScript click, synthetic event, new dependency, or direct persistence write was added.

## F-HUB-004 — UTC serialization shifted local insight buckets across a calendar boundary

- Severity: high
- Status: fixed
- Area: Hub / Insights
- Matrix row: `CMP-HUB-001`
- Files: `inkforge/src/components/hub/insights/DataInsightsSection.vue`, `inkforge/tests/e2e/specs/editor-settings.spec.cjs`, `.trellis/spec/frontend/state-management.md`
- Reproduction: create and save a real current-day article, then return to Hub after local 08:00 in UTC+8. The article card, distribution, and timeline appeared, but heatmap and trend omitted the same article even though it met the real word threshold.
- Root cause: `dateKey()` called `toISOString().slice(0, 10)` for local-midnight iteration values. UTC conversion moved that midnight into the previous date while the article's current timestamp retained the current UTC date, so equality depended on local time and timezone.
- Fix: build the calendar key from local year, month, and day fields. Keep the existing real-article thresholds and insight computations unchanged.
- Verification: the focused real Tauri Hub flow passed 1/1 after a fresh build; the five-case prefix passed 5/5; the final complete native Settings/Hub session passed 11/11. Serial Vitest passed 108 files / 1825 tests, and lint, typecheck, and production build passed. No analytics record was inserted or fabricated.

## F-WEBVIEW-FOCUS-001 — Transient WebView blur committed or cancelled destructive UI state

- Severity: high
- Status: fixed
- Area: Settings / Workstation / WebView2 focus lifecycle
- Matrix rows: `SET-SHORTCUTS-001`, `CMP-CATEGORY-001`
- Files: `inkforge/src/components/settings/ShortcutInput.vue`, `inkforge/src/components/file/FileManager.vue`, `inkforge/tests/e2e/specs/editor-settings.spec.cjs`, `.trellis/spec/frontend/state-management.md`
- Reproduction: activate Shortcut recording or a FileManager inline rename through the visible native control. WebView2 emitted element blur plus window blur, restored focus a few milliseconds later, and the synchronous handlers either stopped recording before key input or submitted the unchanged rename and unmounted the field.
- Root cause: both controls interpreted the first blur event as settled focus departure. A prior one-animation-frame shortcut delay was still earlier than some native blur/focus pairs, and rename had no stabilization at all.
- Fix: use one bounded stable-state check per affected component. Shortcut recording stops only after stable trigger departure or persistent native-window focus loss. Inline rename commits only after focus is stably outside the input while the document is focused; native-window loss preserves the draft. Explicit Enter/Escape behavior and timer cleanup remain immediate and deterministic.
- Verification: temporary event probes established the native event order and were removed. Focused shortcut, category, and tag replays passed. The complete real Tauri/WebView2 suite passed 11/11, including visible focus departure, production title-bar minimize/restore, native category context-menu rename, and full persistence checks. Static checks, serial Vitest 108/1825, and the 4660-module production build passed.

## F-WS-005 — Persisted layout restoration bypassed the Workstation route transaction

- Severity: high
- Status: fixed
- Area: Workstation / Profile / layout persistence
- Matrix rows: `RTE-002`, `CMP-WORKSTATION-001`
- Files: `inkforge/src/views/WorkstationView.vue`, `inkforge/tests/e2e/specs/editor-settings.spec.cjs`, frontend state/type/quality specs
- Reproduction: save a real layout record whose active article differs from the current source article, remove the route `id`, install a real Vue Router guard that rejects the persisted target, and switch the active Profile so the production layout watcher restores that record. Before repair, `applyPersistedLayoutTabs()` hydrated tabs and called `articleStore.selectArticle()` directly, so Profile/layout restoration could mutate the active article and editor without first flushing the source or obtaining router acceptance.
- Root cause: the earlier flush/navigate/commit contract covered direct tab, close, restore, FileManager, and TagBrowser actions, but persisted layout restoration still combined record resolution and state mutation in one helper outside the shared Workstation transition boundary.
- Fix: resolve and validate the persisted tab plan without mutation, choose explicit route article before persisted/selected fallbacks, call the existing `transitionToWorkstationArticle()` transaction, and apply layout/tab state only after accepted navigation. A rejected transition returns `false`, logs the reason, and leaves route, article, editor, tabs, session, and layout state unchanged.
- Verification: the native regression creates an isolated layout row through the real `layoutPersistenceStore.save()` boundary, types a source marker until the real active tab reports `saving`, removes the route id, installs a real router guard, and triggers the production Profile watcher. It requires exactly one guarded navigation, zero changed route/store/DOM/session fields, and encrypted repository readback of the source marker before exact fixture cleanup. The focused Workstation case and complete editor Settings suite passed; final complete native replay passed 12/12 in 5 minutes 15 seconds.

## F-WS-006 — Dark active-tab color targeted the wrapper but not the native tab button

- Severity: low
- Status: fixed
- Area: Workstation / TabBar visual state
- Matrix rows: `RTE-002`, `CMP-WORKSTATION-001`
- Files: `inkforge/src/components/workstation/WorkstationTabBar.vue`, `inkforge/src/components/workstation/WorkstationTabBar.style.test.ts`
- Reproduction: in dark color-scheme CSS, `.workstation-tabbar__tab--active` set the intended active color on the non-interactive wrapper, while `.workstation-tabbar__tab` set its own inherited-breaking foreground on the actual native tab button. The active wrapper therefore did not reliably color the visible label.
- Root cause: the TabBar accessibility repair correctly separated wrapper/tab/pin/close controls, but the dark selector still assumed the wrapper owned the rendered text color.
- Fix: target the direct native tab descendant with `.workstation-tabbar__tab--active .workstation-tabbar__tab` while preserving the existing palette and sibling control structure.
- Verification: focused `happy-dom` source-contract Vitest passed 1/1 and the full serial suite passed 109 files / 1826 tests. Targeted ESLint, typecheck, production build, and the complete native Workstation/Settings replay passed.

## F-E2E-002 — Native sessions could expose a collapsed viewport or remain unfocused after minimize

- Severity: medium
- Status: fixed
- Area: Tauri/WebView2 E2E harness
- Matrix rows: native acceptance infrastructure for `RTE-002`, Settings, Hub, tags, and categories
- Files: `inkforge/tests/e2e/wdio.conf.cjs`, `inkforge/tests/e2e/specs/editor-settings.spec.cjs`
- Reproduction: after a machine restart, an isolated Tauri session selected the correct main WebView but reported a real `158×26` viewport. The Data backup button rect was above the viewport and its center hit a title-bar button, so EdgeDriver truthfully rejected the click. In the full sequence, the production minimize action correctly removed focus, but `maximizeWindow()` restored dimensions without bringing the borderless Tauri window to the foreground; every later Settings/Hub control then became non-interactable.
- Root cause: the shared harness selected the correct WebView but did not normalize the native desktop viewport, and the shortcut focus-loss cleanup assumed WebDriver maximize implied foreground focus for a decorations-disabled Tauri window.
- Fix: select the real main WebView; use the visible production restore control only when startup is already maximized; after real minimize, restore and foreground the native process through Win32, reselect the original handle, and require document focus. Fail closed unless the WebView is desktop-interactable and its client bounds align with the native host. Cleanup initializes the real Workstation stores before removing run-created articles so focused single-case runs are independently clean.
- Verification: the fresh current-source complete suite passed all 13 cases in 5m49.6s. Every session logged host `1401x900`, client `1400x900`, gap `1x0`; the shortcut case retained the real minimize/focus-loss assertion and the SVG asset case retained production cleanup. No JavaScript click replaced business-control interaction, no viewport threshold was lowered, and no assertion or product timeout was weakened.


### 2026-07-16 native-window follow-up

The earlier viewport repair still used WebDriver window maximization. On the borderless Tauri host this could resize or maximize the WebView surface without an equivalent host-window transition, producing the large black surround shown in the operator screenshot. The final harness no longer calls maximizeWindow() or setWindowSize(). It selects the real main WebView, uses InkForge's production title-bar restore control when the native window is maximized, clicks the production title bar to foreground the host, and then fails closed unless the real viewport is at least 800×500 and document.hasFocus() is true. This supersedes the maximize wording above; business controls remain real WebDriver interactions.

The final remediation also removes the residual minimize-recovery maximize call: it invokes Win32 `SW_RESTORE`, foregrounds the real process, reselects the original WebDriver handle, and requires the normal-size control plus document focus. Shared startup compares the native host rectangle with the WebView client and now fails closed when either absolute axis gap exceeds 8 px. A deterministic Node regression accepts the 8 px DPI/native-border boundary and rejects positive or negative 9 px drift. Fresh focused SVG and complete 13-case replays recorded host `1401x900`, client `1400x900`, gap `1x0`; after independent review tightened the gate, another real SVG replay passed 10/10 in 33.3s with the same aligned geometry.

## F-ASSET-001 — Native SVG assets were not a complete durable editor workflow

- Severity: high
- Status: fixed
- Area: Workstation / asset pipeline / editor insertion
- Matrix rows: CMP-ASSET-001
- Files: inkforge/src-tauri/tauri.conf.json, inkforge/src/components/asset/**, inkforge/src/services/asset-pipeline/**, inkforge/src/stores/asset.ts, inkforge/src/components/editor/EditorPanel.vue, inkforge/src/extensions/ImageV2/imageMarkdown.ts, inkforge/src/views/WorkstationView.vue, and native regression tests
- Reproduction: upload a real SVG in the Tauri application, insert it in visual and source modes, reload the article, upload the same binary from another article, delete references, and attempt to insert a non-image file. Before repair, the desktop CSP blocked the Blob URL; list requests could race and truncate implicitly; legacy ownership, duplicate/shared-reference deletion, object-URL revocation, source-mode serialization, keyboard controls, and invalid-MIME feedback did not form one complete contract.
- Root cause: the repository, Pinia store, asset components, editor modes, CSP, and URL lifecycle each implemented only part of the workflow. Ownership and request ordering were not expressed as durable invariants.
- Fix: permit blob: only for image sources in the Tauri CSP; make the latest list request authoritative; remove the implicit 100-item truncation; migrate legacy article ownership once; retain shared binaries until their final reference is removed; bound and revoke object URLs; serialize source-mode insertion through the shared Markdown image helper; insert a real image node in visual mode; and visibly reject unsupported MIME without creating a node.
- Verification: real Tauri/WebView2 uploaded a genuine SVG through the visible file input, decoded its Blob URL, inserted and reloaded it through production IndexedDB, deduplicated it across two real drafts, retained and then removed references in order, observed bounded URL revocation, exercised keyboard/tag/search controls, and rejected a genuine text/plain file with no image node. The final native editor suite passed 13/13 and the independent native SVG renderer suite passed 10/10. Unit regressions cover pipeline ownership, deduplication, deletion, request ordering, and Markdown image serialization.

## F-CRYPTO-001 — Tauri debug builds could persist sensitive content without desktop encryption

- Severity: high
- Status: fixed
- Area: native runtime / IndexedDB sensitive fields / Windows credential store
- Matrix rows: DSK-001, CMP-ASSET-001, and native persistence acceptance infrastructure
- Files: inkforge/src/utils/crypto/config.ts, inkforge/src/utils/crypto/key-management.ts, inkforge/src/utils/platform.ts, and native regression tests
- Reproduction: build with tauri build --debug --bundles none, create and save a real article, and inspect only the raw IndexedDB envelope shape. Before repair, the Tauri pre-build exposed import.meta.env.PROD=false; Rollup therefore removed the desktop encryption path and contents.body could remain plain. In a long-running native session, expiry of the short-lived in-memory key cache could also make later saves fail despite a valid OS-stored key.
- Root cause: encryption availability was incorrectly coupled to the Vite release flag rather than the authenticated Tauri runtime, and getMasterKey() read only the in-memory cache instead of rehydrating it at the persistence boundary.
- Fix: enable sensitive-field encryption for Tauri release, debug, and development runtimes while keeping Web preview on its existing password/UI boundary. Recognize the reserved Tauri origin before injected globals are available. On cache expiry, `getMasterKey()` restores only the persisted OS key and rejects a missing entry instead of generating a replacement. First-run generation is cached only after persistence succeeds. WebDriver uses the dedicated `inkforge_e2e_*_v3` namespace rather than the normal application credential.
- Verification: focused crypto tests cover first-run persistence failure, cache-expiry restore-only behavior, and missing persisted-key failure. The rebuilt native application persisted the real article body as an encrypted-v2 envelope. The fresh complete 13-case native editor suite ran for 5m49.6s, crossed the five-minute key-cache window, retained encrypted save/reload behavior, verified the dedicated E2E credential existed, and removed it through the real Tauri command. No credential value, browser profile, runtime data path, or diagnostic probe is committed.
