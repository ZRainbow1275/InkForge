135 SVG Work Tool Quick-Entry Residue - 2026-06-27

Scope:
- Static publishability protection for copied 135 SVG editor work-tool quick-entry chrome.
- Applies to WeChat, Xiaohongshu, and Zhihu quality reports.
- No platform action, phone preview, upload, sync, scheduled send, public host check, or publish
  action was performed by this slice.

Observed live-editor markers converted into executable rules:
- work-tool
- work-tool-signature fixed
- ant_btn_panel
- idea-entry-quick
- entry-popover
- btn-entry ant-btn

Implemented:
- Added detector label: 135 SVG work tool quick-entry residue.
- Added a reduced regression fixture containing only source-specific work-tool quick-entry markers.
- The fixture intentionally excludes header/logo/menu chrome, user/header chrome, work-title
  controls, toolbar controls, sidebar/navigation controls, material-list cards, material component
  paths, material search controls, material purchase controls, shell wrappers, layout controls,
  material-panel controls, known 135 data-name values, hosted media, trigger overlays, Ant switch
  controls, svg:135 styles, and background-size:100.1% background shells.

False-positive boundary:
- Ordinary prose containing work, tool, entry, history, signature, quick, panel, button, or editor
  wording is not enough.
- Generic entry-list, entry-item, history, button, or ant-btn markers are not standalone triggers.
- btn-entry only counts when paired with ant-btn.

TDD:
- Red:
  pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "work tool quick-entry" --reporter=default
  Result: failed with 1 selected failing test because no market-editor-residue issue was emitted.
- Green:
  pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "work tool quick-entry" --reporter=default
  Result: PASS, 1 selected test / 252 skipped tests.

Verification:
- pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "routes WeChat markdown Mermaid" --reporter=default
  PASS: 1 selected test / 252 skipped tests after an unrelated full-file Mermaid timeout.
- pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts --reporter=default
  PASS: 1 file / 253 tests.
- pnpm -C inkforge exec vitest run src/services/export --reporter=default --maxWorkers=1 --no-file-parallelism
  PASS: 36 files / 1230 tests.
- pnpm -C inkforge exec eslint src/services/export/quality-detector.ts src/services/export/platform-export-rendering.test.ts --quiet
  PASS.
- pnpm -C inkforge exec vue-tsc --noEmit --pretty false
  PASS.
- NODE_OPTIONS=--max-old-space-size=4096 pnpm -C inkforge build
  PASS: 4653 modules transformed, Vite build completed in 50.64s.

Release preflight:
- pnpm -C inkforge style-proof:release-preflight --json
  Expected exit 1.
  status=blocked-by-external
  canClaimComplete=false
  externalHandoffRows=18
  safeExternalRows=0
  actionableLocalRows=0
  nextRowRefs=5
  uniqueNextRows=3

Boundary:
- This proves local static residue detection only.
- It does not prove WeChat PC paste, phone preview, mobile interaction, mobile Dark Mode, cover
  thumbnail acceptance, credentialed sync, scheduled send, platform preview, public article
  rendering, public-host acceptance, Xiaohongshu account upload, Zhihu account upload, or publish
  success.
