Xiumi UIB Tab Content Transclude Residue - 2026-06-29

Scope:
- Local static publishability protection for the Angular UI Bootstrap tab-body transclusion
  directive observed on the live Xiumi v5 paper editor surface.
- Covered attribute: uib-tab-content-transclude.
- No platform action, no phone preview, no sync, no upload, no publish, no account artifact, no
  browser runtime artifact, no capture-location reference, no QR artifact, and no credential
  material.

Implemented:
- Extended the existing Xiumi UI Bootstrap control directive detector from
  uib-tab-heading-transclude to uib-tab-(heading|content)-transclude.
- Added a reduced regression fixture that contains only uib-tab-content-transclude.

Behavior:
- WeChat, Xiaohongshu, and Zhihu quality checks report Xiumi UI Bootstrap control directive
  residue for the reduced fixture.
- Xiumi dropdown directive residue stays absent for the reduced fixture.
- The rule does not treat ordinary tab prose, content prose, heading prose, transclusion wording,
  or Xiumi prose as residue by itself.

Regression coverage:
- Class-cleaned UI Bootstrap tab-content residue cannot pass publishability checks.
- Adjacent UI Bootstrap dropdown, heading-transclude, and button-state diagnostics remain
  independent.
- The change reuses the existing UI Bootstrap label instead of adding a broad any-uib-* detector.

Verification:
- pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "tab content directives" --reporter=default
  RED: 1 selected failing test before the detector update; no *-market-editor-residue issue was emitted.
- pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "tab content directives|UI Bootstrap directives|button directives|dropdown directives" --reporter=default
  PASS: 1 file / 4 selected tests / 352 skipped tests after the detector update.
- pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts --reporter=default --testTimeout=90000
  PASS: 1 file / 356 tests.
- pnpm -C inkforge exec vitest run src/services/export --reporter=default --maxWorkers=1 --no-file-parallelism --testTimeout=90000
  PASS: 36 files / 1333 tests.
- pnpm -C inkforge exec eslint src/services/export/quality-detector.ts src/services/export/platform-export-rendering.test.ts --quiet
  PASS.
- pnpm -C inkforge exec vue-tsc --noEmit --pretty false
  PASS.
- NODE_OPTIONS=--max-old-space-size=4096 pnpm -C inkforge build
  PASS: transformed 4653 modules and completed in 36.56s.
- pnpm -C inkforge style-proof:release-preflight --json
  EXPECTED BLOCKED: canClaimComplete=false, status=blocked-by-external, blocker kinds
  phone-preview, external-dependency, unsafe-to-automate, mutating-platform; blockerCount=4,
  combinedIssueCount=11, cannotClaimSteps=29, phoneOpenSteps=4, externalDependencyOpenSteps=14,
  unsafeToAutomateOpenSteps=13, mutatingOpenSteps=13, externalHandoffRows=18, safeExternalRows=0,
  actionableLocalRows=0, nextRowRefs=5, uniqueNextRows=3.

Boundary:
- This proves local static publishability protection and diagnostic coverage only.
- It does not prove WeChat PC paste, phone preview, tab interaction fidelity, upload, cover
  thumbnail acceptance, mobile Dark Mode, mobile SMIL/click interaction, credentialed sync,
  scheduled send, public rendering, public-host acceptance, XHS/Zhihu account upload, or publish
  success.
