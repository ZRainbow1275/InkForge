Style Proof Manifest Draft Source Guidance - 2026-07-03

Scope:
- Local `style-proof:external-handoff --manifest-drafts` enhancement.
- No platform action, no phone preview, no sync, no upload, no publish, and no proof artifact
  creation.

Implemented:
- Added `sourceRows[]` to the manifest draft pack.
- Kept existing `sourceRowIds[]` for stable id-only consumers.
- Each source row carries the row id, platform, choice ids, requirement id/label, gate, boundary,
  status, blocker kinds, issue ids, cannot-claim reason, next operator action, the committed
  artifact template, and the row-specific artifact guidance.
- Row-specific artifact guidance includes the same draft-only `artifactDraftTemplate` used by
  `--template`, with nullable base artifact fields, required/forbidden field checklist rows,
  accepted values, redaction boundary, success criteria, failure signals, and the do-not-include
  list.
- Manifest drafts themselves still keep `claimedEvidence: []` and `artifacts: []`.

Observed local behavior:
- `pnpm --silent -C inkforge style-proof:external-handoff --manifest-drafts --next-only`
  still exits 1 with `draftOnly:true`, `notProof:true`, `canClaimComplete:false`,
  `sourceRowIds=3`, `sourceRows=3`, `manifestCount=21`, and all generated manifests
  artifact-empty.
- The credentialed WeChat source row exposes nullable `externalAccountAuthenticated` guidance
  and forbidden `externalAccountLoginBlocked` guidance.
- The Zhihu public-host source row exposes nullable `hostStatus` guidance and accepted statuses
  `public-https` and `platform-hosted`.

Regression coverage:
- `sourceRows.map(row => row.id)` must equal `sourceRowIds`.
- Credentialed-channel source row must keep the artifact draft template as draft-only and not
  proof.
- Public-host source row must preserve host-status accepted values.
- The manifest-drafts output must remain ingestible as an incomplete draft pack with
  `canClaimComplete:false`.

Verification:
- `npx gitnexus impact buildManifestDraftPack -r InkForge --depth 3`
  returned target not found, risk UNKNOWN, impactedCount 0. The helper is script-local and not
  indexed by the current GitNexus graph.
- `pnpm -C inkforge exec vitest run scripts/style-proof-external-handoff.test.ts --reporter=default --test-timeout=90000`
  passed with 1 file and 14 tests.
- `pnpm -C inkforge exec eslint scripts/style-proof-external-handoff.ts scripts/style-proof-external-handoff.test.ts --quiet`
  passed.
- `pnpm -C inkforge exec vitest run scripts --reporter=default --test-timeout=90000 --maxWorkers=1 --no-file-parallelism`
  passed with 4 files and 39 tests.
- `pnpm -C inkforge exec vue-tsc --noEmit --pretty false`
  passed.
- `NODE_OPTIONS=--max-old-space-size=4096 pnpm -C inkforge build`
  passed; 4653 modules transformed, Vite built in 40.10s, and `inkforge/tsconfig.tsbuildinfo`
  was restored afterward.
- `pnpm --silent -C inkforge style-proof:release-preflight --json`
  still exits 1 with `status=blocked-by-external`, `canClaimComplete=false`,
  `nextRowRefs=5`, `uniqueNextRows=3`, and `nextRows=3`.
- `git diff --cached --check`
  passed.
- `git diff --cached -U0 | rg ... sensitive fragments`
  returned no matches for local profile paths, auth header markers, QR/code markers, HAR markers,
  account-image markers, local capture-path markers, or platform route fragments.
- `npx gitnexus detect-changes -r InkForge --scope staged`
  returned `No changes detected.` for the staged patch.

Boundary:
- This is local manifest-draft operator guidance only.
- It does not prove WeChat phone preview, phone screenshot, mobile interaction, mobile Dark Mode,
  cover thumbnail acceptance, credentialed sync, scheduled send, public rendering, Zhihu
  public-host acceptance, XHS/Zhihu account upload, or publish success.
