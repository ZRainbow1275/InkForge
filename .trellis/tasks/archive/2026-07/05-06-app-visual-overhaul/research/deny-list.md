# 文案净化 Deny-List

> 用途：CI 级 grep 反向校验；任何匹配项视为"开发注释前端化"，必须替换或删除
> 原则（0506 T09-13、L1-50、L1-52）：所有功能第一天可见，不在 UI 解释"为什么这么设计"

## 完全禁用词组（精确匹配）

```
真实创建链路
保留真实创建链路
不再绕路
不新增任何空壳
不新增任何空壳入口
不再复制第四条路径
这里展示的所有动作都已经在 Hub 内跑通
减少重复入口
开发链路
真实快速操作
创作工具信号
直接复用现有模板创建真链路
```

## 语义模式禁用（regex）

```
保留.*真实
直接.*不再.*绕路
不再.*第.*条路径
都已经.*跑通
真实创建.*链路
```

## 校验命令

```bash
grep -rE "(真实创建链路|不再绕路|不新增任何空壳|这里展示的所有动作都已经在 Hub|不再复制第四条路径|开发链路|创作工具信号|真实快速操作|直接复用现有模板创建真链路|减少重复入口)" inkforge/src --include="*.vue" --include="*.ts"
```

期望输出：**0 行**（即 grep 返回退出码 1）

## 替换/删除策略

| 原文案位置 | 原文 | 处理 |
|----------|-----|------|
| HubView.vue:1323 | 减少重复入口，保留真实创建链路。模板创建、文档导入与素材积累都可以从这里接续。 | **删除整段** |
| HubView.vue:1830 | 第二屏直接挑模板，进入工作站不再绕路。 | **删除** |
| HubView.vue:1838 | 这里直接复用现有模板创建真链路。点击模板后会写入草稿并跳转工作站，不新增任何空壳入口。 | **删除** |
| HubView.vue:1893 | 真实创建链路、当前进度和工具信号在这里聚合。 | **删除** |
| HubView.vue:1898 | 保持空白 / 模板 / 导入三入口，不再复制第四条路径。这里展示的所有动作都已经在 Hub 内跑通。 | **删除** |
| HubView.vue:2026 ARIA | 创作工具信号 | **改为**「创作动态」 |
| 任何 ARIA / aria-label 含「真实快速操作」 | 真实快速操作 | **改为**「快速操作」 |

## 允许的产品语（白名单参考）

- 主标题：「模板」「创作流」「创作动态」「最近文章」「数据洞察」「我的分类」「每日灵感」
- 操作 label：「新建」「导入」「使用模板」「打开」「编辑」「导出」「设置」
- 状态文案：「已保存」「同步中」「未保存」「已发布」「草稿」「待审阅」「已归档」
- 计量单位：「篇」「字」「分钟」「天」

## 维护规则

- 任何 PR 引入"开发解释"语 → CI 拒绝
- 新增禁用词时同步更新此文件 + 更新 grep 命令
- 此文件即 SSOT（Single Source of Truth）
