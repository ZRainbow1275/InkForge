XHS Cover Hook Local Evidence - 2026-06-21

Scope:
- Repository-committed local StyleProofManifest evidence for `xhs-cover-hook`.
- No Xiaohongshu account login, account upload, mobile/platform preview, sync, scheduled send,
  public URL acceptance, or publish action was executed or claimed.
- No external platform page or account/session/runtime artifact is recorded.

Generated raster artifact:
- Browser route: CloakBrowser opened the local Vite app and dynamically imported the source-owned
  `svg-modules` package from the running InkForge development server.
- Rendering path:
  `coverModules.find(id === 'cover-title').render(...)` -> `renderXhsPosterCard(svgHtml, '3:4', '#fff7ed')`.
- Target: `xhs`; `allowMotion=false`.
- Source module: `cover-title`.
- Generated artifact:
  `prompts/0601/evidence/xhs-raster/xhs-raster-cover-hook-browser-2026-06-21.png`.
- Metadata artifact:
  `prompts/0601/evidence/xhs-raster/xhs-raster-cover-hook-browser-2026-06-21.json`.
- Visual QA adjusted the subtitle until no ellipsis/truncation remained.
- Final PNG dimensions: 1080 x 1440.
- Final PNG bytes: 92316.
- Final PNG hash:
  `sha256:c7200947079cda16ccafc51b5c56bfd840355da199da48b790b6725233af2d32`.

Validated XHS image artifact manifest:

```json
{
  "kind": "image-page",
  "bodyReferences": [1],
  "pages": [
    {
      "page": 1,
      "fileName": "xhs-raster-cover-hook-browser-2026-06-21.png",
      "src": "inkforge-asset://xhs-cover-hook-browser-2026-06-21",
      "exists": true,
      "width": 1080,
      "height": 1440,
      "ratio": "3:4",
      "format": "png",
      "bytes": 92316,
      "cover": true,
      "referencedByBody": true,
      "cropStatus": "ok"
    }
  ]
}
```

Validator result:
- Command:
  `pnpm -C inkforge exec tsx -e "... validateXhsImageArtifactManifest(manifest) ..."`
- Result: `issues=[]`.

Committed manifest behavior:
- `getCommittedStyleProofLocalEvidenceManifests()` now includes one `xhs-cover-hook` manifest.
- The manifest satisfies only local rows for the exact raster artifact:
  - `unit-test-coverage`
  - `local-browser-rendering`
  - `exact-artifact`
  - `xhs-artifact-manifest`
  - `no-sensitive-artifact`
- `published-url-or-platform-preview` remains missing and unsafe-to-automate.

Verification:
- Focused regression passed:
  `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "committed local evidence|release claims|execution runbooks" --reporter=default`
  passed 1 file / 4 selected tests.
- Full platform-export regression passed 1 file / 153 tests.
- 4-file cross-platform export regression passed 4 files / 192 tests.
- Full export service serial regression passed 35 files / 1126 tests.
- Targeted ESLint passed for `style-catalog.ts` and `platform-export-rendering.test.ts`.
- `pnpm -C inkforge exec vue-tsc --noEmit --pretty false` passed.
- `NODE_OPTIONS=--max-old-space-size=4096 pnpm -C inkforge build` passed; Vite transformed 4652
  modules and built in 30.25s.
- Release-gate readout after this slice:
  `status=blocked-by-local-conflict`, `canClaimComplete=false`, `localManifestCount=6`,
  `combinedManifestCount=8`, `combinedIssueCount=13`,
  `hasExactArtifactFingerprintConflicts=false`.

Boundary:
- This proves local XHS image-page raster and manifest accounting only.
- It does not prove Xiaohongshu account upload, mobile/platform preview, public URL acceptance,
  scheduled send, public article rendering, or publish success.
