Xiumi User Profile Menu Residue - 2026-06-29

Scope:
- Static publishability gate for copied or partially cleaned Xiumi v5 paper-editor user profile
  dropdown controls.
- Applies to WeChat, Xiaohongshu, and Zhihu quality reports.
- No platform action, phone preview, sync, upload, schedule, or publish action was performed.

Live source:
- CloakBrowser reviewed the logged-in Xiumi v5 paper editor DOM.
- The editor exposed a user profile dropdown marker `usr-info-desc-frame`.
- The node appeared around account settings, invoice, and sign-out controls. It is editor chrome,
  not article content.

Implementation:
- Added the `Xiumi user profile menu residue` detector label.
- Added a reduced regression fixture containing only `usr-info-desc-frame` plus generic dropdown
  context, without `wx-user-panel`, `usr-message-box`, `message-box-toggle`, top operation
  buttons, dropdown directives, operation-bar dropdowns, Angular runtime attributes, hosted media,
  sidebar controls, or meta panels.
- Kept the rule anchored to class/id attributes, not prose.
- Kept adjacent detector labels independent from `Xiumi account sync panel residue`,
  `Xiumi message panel residue`, and `Xiumi top operation button residue`.

TDD:
- Red:
  `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "user profile menu" --reporter=default`
  failed before the detector update because no `*-market-editor-residue` issue was emitted.
- Green:
  `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "user profile menu|message panel controls|account sync panel controls|top operation buttons" --reporter=default`
  passed with 4 selected tests and 302 skipped tests after the detector update.

Full verification:
- Platform regression suite:
  `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts --reporter=default --test-timeout=90000`
  passed with 1 file and 306 tests.
- Export service suite:
  `pnpm -C inkforge exec vitest run src/services/export --reporter=default --maxWorkers=1 --no-file-parallelism --test-timeout=90000`
  passed with 36 files and 1283 tests.
- ESLint:
  `pnpm -C inkforge exec eslint src/services/export/quality-detector.ts src/services/export/platform-export-rendering.test.ts --quiet`
  passed.
- Type check:
  `pnpm -C inkforge exec vue-tsc --noEmit --pretty false`
  passed.
- Production build:
  `NODE_OPTIONS=--max-old-space-size=4096 pnpm -C inkforge build`
  passed after transforming 4653 modules in 36.35s.
- Release preflight:
  `pnpm -C inkforge style-proof:release-preflight --json`
  exited 1 as expected with `canClaimComplete=false`, `status=blocked-by-external`,
  blockerKinds `phone-preview`, `external-dependency`, `unsafe-to-automate`, and
  `mutating-platform`; summary included `externalHandoffRows=18`, `safeExternalRows=0`,
  `actionableLocalRows=0`, `nextRowRefs=5`, and `uniqueNextRows=3`.

Boundary:
- This is static publishability protection only.
- It does not prove WeChat PC paste, phone preview, mobile SMIL/click interaction, mobile Dark
  Mode, cover thumbnail acceptance, credentialed sync, scheduled send, platform preview, public
  article rendering, public-host acceptance, XHS/Zhihu account upload, or publish success.
