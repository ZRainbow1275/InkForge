Xiumi Sidebar Tab Control Residue - 2026-06-26

Scope:
- Static publishability protection for copied or partially cleaned Xiumi editor sidebar/tab DOM.
- No platform action, phone preview, sync, upload, scheduled send, or publish operation.

Live DOM source:
- CloakBrowser reviewed the live Xiumi v5 paper editor.
- Observed editor-side left material/template sidebar and tab controls:
  - sidebar-panel
  - sidebar-style-normal
  - x3-tab-item
  - uib-tab
  - tn-tab-ctrl-pin
- Adjacent observed icon classes:
  - icon templates
  - icon material-img
  - icon fragments
  - icon clipboard
  - icon images
  - icon team-images
  - icon music
- The adjacent icon classes are documented as context only and are not standalone detector triggers.

Implemented:
- Added detector label: Xiumi sidebar tab control residue
- Added reduced three-platform regression fixture:
  MARKET_EDITOR_XIUMI_SIDEBAR_TAB_CONTROL_RESIDUE_HTML

Behavior:
- WeChat, Xiaohongshu, and Zhihu quality reports now fail when Xiumi sidebar/tab editor chrome
  appears in publishable output.
- The reduced fixture intentionally keeps only non-tn-* sidebar/tab controls plus readable
  template/material labels.
- The reduced fixture avoids right-toolbar controls, text-toolbar controls, font-family menus,
  font-size skimmer attributes, color-selector controls, operation-bar controls, UI Bootstrap
  directives, top operation classes, broad Angular ng-* markers, paper auxiliary tree controls,
  selection overlays, crop/worker controls, operator-dock parents, operator depot items,
  op-loader, broad tn-* markers, opera-tn-* markers, contenteditable, hosted media, SVG
  content-layer, ui-slider, ui-sortable, touch-action, and user-select markers.
- The detector stays anchored to Xiumi-specific sidebar/tab class/id markers and does not block
  ordinary prose containing sidebar, template, material, image, music, clipboard, tab, or icon
  wording by itself.

Verification:
- pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "sidebar tab controls" --reporter=default
  RED: failed before detector update after the broad tn-* marker was removed; no market-editor-
  residue issue was emitted.
- pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "sidebar tab controls" --reporter=default
  PASS: 1 selected test / 238 skipped tests after detector update.
- pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts --reporter=default
  PASS: 1 file / 239 tests.
- pnpm -C inkforge exec vitest run src/services/export --reporter=default --maxWorkers=1 --no-file-parallelism
  PASS: 36 files / 1216 tests.
- pnpm -C inkforge exec eslint src/services/export/quality-detector.ts src/services/export/platform-export-rendering.test.ts --quiet
  PASS.
- pnpm -C inkforge exec vue-tsc --noEmit --pretty false
  PASS.
- NODE_OPTIONS=--max-old-space-size=4096 pnpm -C inkforge build
  PASS: 4653 modules transformed; Vite built in 41.99s.
- pnpm -C inkforge style-proof:release-preflight --json
  EXPECTED BLOCK: exit 1, status=blocked-by-external, canClaimComplete=false,
  externalHandoffRows=18, safeExternalRows=0, actionableLocalRows=0, nextRowRefs=5,
  uniqueNextRows=3.

Boundary:
- This proves local static publishability protection only.
- It does not prove WeChat PC paste, phone preview, mobile SMIL/click interaction, mobile Dark
  Mode, cover thumbnail acceptance, credentialed sync, scheduled send, platform preview, public
  article rendering, public-host acceptance, Xiaohongshu/Zhihu account upload, or publish success.
