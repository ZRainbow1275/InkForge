Xiumi Image Enhancement Crop Control Residue - 2026-06-29

Scope:
- Local static publishability protection for copied or partially cleaned Xiumi v5 paper-editor
  image enhancement, image popup, and thumbnail crop child controls.
- No platform action, no phone preview, no sync, no upload, no publish.

Live DOM source:
- CloakBrowser reviewed the live Xiumi v5 paper editor DOM.
- The page exposed `op-cp-image-enhancement`, `op-ce-image-enhancement`, `op-ce-image-popup`,
  `enhance-attr-menu`, and `thumb-crop-img` around image batch processing, click-to-zoom,
  blur/brightness/contrast/sharpen settings, image popup sizing, and article thumbnail crop panels.
- Minimal local probing showed those single-token class values did not emit a market-editor
  hard-block before this slice.

Implemented:
- Added the `Xiumi image enhancement crop control residue` quality detector for class/id tokens
  `op-cp-image-enhancement`, `op-ce-image-enhancement`, `op-ce-image-popup`,
  `enhance-attr-menu`, and `thumb-crop-img`.
- Added a reduced regression fixture that keeps only those source-specific single class tokens,
  without `op-worker-*`, `crop-*`, `cover-menu`, `tn-meta-*`, `op-dock`, `op-loader`, broad
  `tn-*`, broad Angular runtime attributes, hosted media, sidebar controls, or template markers.

Regression coverage:
- WeChat, Xiaohongshu, and Zhihu all report the precise
  `Xiumi image enhancement crop control residue` market-editor issue.
- The WeChat assertion verifies the reduced fixture does not fall through to
  `Xiumi worker surface crop control residue`, `Xiumi crop panel child control residue`,
  `Xiumi WeChat cover control residue`, `Xiumi meta panel control residue`, or
  `Xiumi operator dock control residue`.

Verification:
- pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "image enhancement and crop child controls" --reporter=default
  RED: 1 selected test failed before the detector because no market-editor-residue issue was emitted.
- pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "image enhancement and crop child controls|worker surface crop controls|crop panel child controls|WeChat cover controls|meta panel controls|operator dock child controls" --reporter=default
  PASS: 6 selected tests / 286 skipped tests.
- pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts --reporter=default --test-timeout=60000
  PASS: 1 file / 292 tests.
- pnpm -C inkforge exec vitest run src/services/export --reporter=default --maxWorkers=1 --no-file-parallelism --test-timeout=60000
  PASS: 36 files / 1269 tests.
- pnpm -C inkforge exec eslint src/services/export/quality-detector.ts src/services/export/platform-export-rendering.test.ts --quiet
  PASS.
- pnpm -C inkforge exec vue-tsc --noEmit --pretty false
  PASS.
- $env:NODE_OPTIONS='--max-old-space-size=4096'; pnpm -C inkforge build
  PASS.
- pnpm -C inkforge style-proof:release-preflight --json
  EXPECTED BLOCK: exit code 1, status=blocked-by-external, canClaimComplete=false,
  blockerKinds=phone-preview,external-dependency,unsafe-to-automate,mutating-platform,
  actionableLocalRows=0.

Boundary:
- This proves local static publishability protection only.
- It does not prove WeChat paste, phone preview, mobile SMIL/click interaction, mobile Dark Mode,
  cover thumbnail acceptance, credentialed sync, scheduled send, platform preview, public article
  rendering, XHS/Zhihu account upload, public host acceptance, or publish success.
