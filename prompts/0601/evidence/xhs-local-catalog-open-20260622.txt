XHS Local Catalog Open - 2026-06-22

Scope:
- Local catalog availability update for committed Xiaohongshu raster proof.
- No account upload, no platform preview, no scheduled send, no public article readback, no publish action.

Choices opened to local-browser available:
- xhs-data-card
- xhs-long-report
- xhs-market-rich-card-fallback

Evidence basis:
- Existing committed CloakBrowser/local-browser raster packs under prompts/0601/evidence/xhs-raster/.
- Each pack uses the source-owned path:
  sliceMarkdownToXhsCards -> renderXhsMarkdownCardSliceSvg -> renderXhsPosterCard.
- The packs record 1080 x 1440 PNG pages, positive byte sizes, cropStatus=ok, body references,
  overflow=false, and validationIssueIds=[].
- The committed style proof manifests already satisfy unit-test-coverage, local-browser-rendering,
  exact-artifact, xhs-artifact-manifest, and no-sensitive-artifact for these choices.

Runtime result after this slice:
- getPlatformStyleAvailabilityReport('xiaohongshu').stats:
  total=8, usable=7, blocked=0, unavailable=1.
- getPlatformStyleApplicationReport('xiaohongshu'):
  xhs-data-card, xhs-long-report, and xhs-market-rich-card-fallback remain selectable=false because
  no STYLE_CHOICE_APPLICATIONS mapping points to a real InkForge preset/export option yet.
- getCommittedStyleProofEvidenceReleaseGateReport():
  status=blocked-by-external
  canClaimComplete=false
  localManifestCount=20
  wechatPcManifestCount=2
  combinedManifestCount=22
  combinedIssueCount=13
  cannotClaimSteps=29
  phoneOpenSteps=4
  externalDependencyOpenSteps=14
  unsafeToAutomateOpenSteps=13
  mutatingOpenSteps=13
  blockerCount=4
  blockers=phone-preview, external-dependency, unsafe-to-automate, mutating-platform
  local-conflict=absent

Verification:
- pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts --reporter=default
  PASS: 1 file / 155 tests
- pnpm -C inkforge exec vitest run src/services/export --reporter=default --maxWorkers=1 --no-file-parallelism
  PASS: 36 files / 1132 tests
- pnpm -C inkforge exec eslint src/services/export/style-catalog.ts src/services/export/platform-export-rendering.test.ts --quiet
  PASS
- node --check inkforge/tests/e2e/specs/svg-render.spec.cjs
  PASS
- pnpm -C inkforge exec vue-tsc --noEmit --pretty false
  PASS
- NODE_OPTIONS='--max-old-space-size=4096' pnpm -C inkforge build
  PASS: 4653 modules transformed, built in 31.91s
- pnpm -C inkforge exec wdio run tests/e2e/wdio.conf.cjs --spec tests/e2e/specs/svg-render.spec.cjs
  PASS: 1 spec / 6 passing
- CloakBrowser local visual/DOM readback at 390 x 844:
  PASS: XHS summary shows available 7/8, cards total=8, available=7, blocked=0,
  unavailable=1, overflowingCards=0, document scrollWidth=390/clientWidth=390, panel
  scrollWidth=374/clientWidth=374, release gate is blocked-by-external with blockers=4 and
  canClaimComplete=false, and no local-conflict blocker appears.
  The three opened choices render as available but disabled/unmapped:
  Data and table image card, Long report image artifact, Market rich card image fallback.
- npx gitnexus detect-changes -r InkForge --scope all
  PASS: low risk, 41 dirty files across the whole working tree, 27 changed symbols, and
  0 affected processes. The dirty-file count includes unrelated pre-existing local changes and
  does not define the staged boundary.

Boundary:
- This proves local catalog availability and release-gate accounting only.
- It does not prove Xiaohongshu account upload, mobile/platform preview, scheduled send, public
  article rendering, public URL acceptance, or publish success.
- It does not add a UI selectable action; that requires a separate real preset/export-option mapping.
