# InkForge v2.1 完整需求沟通表（双层增强版）

> **创建日期**: 2026-04-08  
> **本次重构日期**: 2026-04-10  
> **文档目的**: 在重新开发前，先把产品边界、内容权威模型、同步/恢复/导出一致性、权限审计、编辑范式、命令系统等上位问题彻底说清，再进入 Task 级实现细问。  
> **本轮文档目标**: 先产出一份“极其详细、可填写、可直接生成后续 PRD/Spec”的需求问卷；你填写完成后，我再围绕这份问卷拆解正式 PRD、Spec、实施清单和验收矩阵。  
> **已确认的沟通前提**:
>
> - 必须基于现有设计风格、语言、哲学、功能大框架推进，不做大重构
> - 必须保持 Markdown-first / 纸张式 / 原始文本可见的编辑器气质
> - 严禁 Emoji 图标，统一使用已有图标库
> - 必须冗余开发、防呆开发，不能只做“刚好能跑”的空壳能力
> - 本轮重点是分析与问卷，不是直接进入功能开发
> - KaTeX + Mermaid → v2.1 全部包含
> - Typora 行内 Mark 感知 → Mark 级精确粒度
> - 多账户 → 需要完整多账户功能

---

## 如何使用这份问卷

### 一、填写顺序

1. **必须先填写第一层：前置总纲需求问卷**
2. **再填写第二层：Task 级实现问卷**
3. 如果第一层某题已经把原则锁死，第二层遇到重复问题时请直接引用第一层答案，不要重复发散

### 二、填写规则

- 每个问题提供 A/B/C/D 选项时，请在你的选择后标记 `✓`
- 如果不确定，请标记 `?`
- 如果所有选项都不满意，请在“补充”中直接写你真正想要的规则
- 如果某题你希望“严格禁止”“必须保留”“未来再做”，请在“补充”里写死，不要留模糊空间
- 如果某题涉及多个场景，请按“默认策略 / 例外策略 / 禁区”三段式填写

### 三、状态标记说明

后续 Task 章节前会附带状态标记，表示当前代码与 0327 文档的真实关系：

- `[已有实现，需核边界]`
- `[已有部分实现，需补全]`
- `[仅文档存在，代码未落地]`
- `[本轮候选增强]`
- `[远期预留]`

### 四、本轮已核验事实（2026-04-10）

以下事实已经过代码与文档复核，本问卷默认基于这些事实继续展开：

1. 当前真正活跃的前端实现落点在 `D:\Desktop\Inkforge\inkforge`
2. `prompts/0327/*` 仍是本轮最重要的开发约束来源，尤其是 `00-master-plan.md`
3. 当前代码里**已经存在**导出、同步、版本历史、设置、Hub/Workstation/Publish 基础能力
4. 当前代码里**尚未看到成熟落地**的能力包括：完整 Typora 模式、完整多账户页、评论审阅系统、完整 10 Tab Settings
5. 当前 `SettingsView.vue` 实际只有 7 个 Tab，不是 0327 文档里的完整 10 Tab
6. 当前内容流更接近 `TipTap HTML` 持久化，而不是已经完全切到 Markdown 作为唯一权威源
7. 当前问卷原稿的主要问题不是“不够长”，而是“过早进入实现细节，缺少上位边界层”

### 五、本轮为什么必须增加“第一层”

根据已核验的代码与 0327 文档，本轮最需要先锁定的不是按钮颜色、菜单层级、某个组件用哪个库，而是这些问题：

- Markdown 到底是不是唯一权威源
- Typora / Source / Preview / Export 到底谁对谁负责、谁不能反向污染谁
- 评论 / 批注 / 审阅到底是 v2.1 主线、附加层还是远期
- 同步 / 冲突 / 多账户 / 版本恢复各自要做到什么级别
- 权限 / 审计 / 高风险命令 / 删除恢复是否要正式留痕
- Hub / Workstation / Settings / Publish 各自承担什么职责，不允许彼此越界

如果这些问题不先定，后面的 PRD 和 Spec 仍会再次漂移。

---

# 第一层：前置总纲需求问卷（必须先填）

> **目的**: 在进入 Task 级实现讨论前，先锁定产品级不可逆决策，包括产品主战场、Markdown 权威模型、编辑范式、评论审阅、历史恢复、同步冲突、多账户隔离、知识增强、命令系统、导出发布一致性、权限审计、性能规模、扩展边界、竞品借鉴边界与冗余开发强度。  
> **原则**: 第一层只问“原则、边界、默认策略、验收门槛”，不提前下沉到某个按钮、某个组件、某个具体库的实现细节。  
> **特别说明**: Tiptap 官方文档已明确 Markdown 支持本质上是“Markdown 与内部 JSON 之间的双向 parse / serialize”，因此如果 InkForge 要坚持 Markdown-first，本层必须先明确“真正的权威内容模型”是什么。

---

## A. 产品定位与交付边界

## L1-01 | 产品第一性目标
InkForge 第一版真正要先赢在哪个核心场景？这会决定编辑器、知识增强、评论审阅、发布链路谁是主轴。

| 选项 | 描述 |
|------|------|
| A | 单人深度写作工具优先 —— 核心是 Markdown 编辑、版本恢复、导出发布 |
| B | 小团队协作写作优先 —— 核心是评论审阅、权限、同步冲突、审计 |
| C | 知识增强写作工作台优先 —— 核心是资料摄取、引用溯源、AI/命令增强 |
| D | 多渠道出版中枢优先 —— 核心是一次编辑、多平台高保真输出 |

**你的选择**: _A+D__  
**补充**: __必须以A和D选项为核心，C选项为近期，B选项为远期。四个选项都是必须要完成的内容。_

## L1-02 | 目标用户与部署形态
你希望 InkForge 首先服务哪类用户与部署方式？

| 选项 | 描述 |
|------|------|
| A | 个人本地优先 —— 单机、本地数据、本地导出、本地 AI |
| B | 小团队优先 —— 多账户、共享审阅、同步、权限分工 |
| C | 企业/组织优先 —— 审计、权限、知识库、发布治理必须完整 |
| D | 个人 + 团队双场景同等优先 —— 从第一版就同时兼顾 |

**你的选择**: _A__  
**补充**: _用户规模为5-10人；有管理员角色，以及非作者角色。但这些是远期后的选项。__

## L1-03 | 本轮 Scope 的硬边界
v2.1 本轮的硬边界到底是什么？

| 选项 | 描述 |
|------|------|
| A | 只要求 P0 真正做完，P1/P2 先写清楚不承诺同轮完成 |
| B | P0 + 核心 P1 必须同轮推进，P2 允许延后 |
| C | P0/P1/P2 全部进入同轮 scope，但允许阶段性交付 |
| D | 不按 0327 的 P0/P1/P2 走，需要重新分期 |

**你的选择**: __B_  
**补充**: __必须要将编辑以及渲染完成_

## L1-04 | 允许的交付形态
为避免再次出现“文档很美、实现很空”的情况，本轮对交付形态的要求是什么？

| 选项 | 描述 |
|------|------|
| A | 允许部分占位 UI，但必须把路径和状态说明白 |
| B | 允许只打通主路径，复杂边界先不做 |
| C | 任何进入 scope 的能力都必须做成完整垂直切片，拒绝空壳 |
| D | C + 所有高风险能力都必须有恢复/校验/错误处理链路 |

**你的选择**: __D_  
**补充**: __文档所约束的内容必须要全部落地，必须全部真实。_

---

## B. Markdown 权威模型与内容真相层

## L1-05 | 内容的唯一权威源
InkForge 内部最终应以什么作为“权威内容模型”？

| 选项 | 描述 |
|------|------|
| A | Markdown 文本是唯一权威源，编辑器/预览/导出都从 Markdown 派生 |
| B | 内部 AST/JSON 是权威源，Markdown 只是输入输出视图 |
| C | 双模型并存 —— AST/JSON 为主，Markdown 为受控副本，并定义一致性规则 |
| D | 不设统一权威模型，各模块按自身格式工作，靠转换适配 |

**你的选择**: _A__  
**补充**: _以Markdown文本作为唯一权威源的核心是它支持html+css渲染，这是我们发布到平台的优先关键；同时方便后期AI集成使用__

## L1-06 | 当前 HTML 持久化的处理态度
当前代码更接近 `TipTap HTML` 持久化。面对这个现实，本轮应该怎么处理？

| 选项 | 描述 |
|------|------|
| A | 接受 HTML 继续作为权威源，Markdown-first 只体现在编辑体验上 |
| B | 逐步迁移到 Markdown 或 JSON 权威模型，但允许过渡期并存 |
| C | 立刻切换到新的权威模型，并提供迁移策略 |
| D | 不切换权威模型，但强制为 HTML 定义更严格的导出/版本/搜索契约 |

**你的选择**: __D_  
**补充**: __必须参考市面上优秀的微信公众号、小红书编辑器_

## L1-07 | 非标准 Markdown 能力的态度
对于无法无损回写到标准 Markdown 的能力，产品应该采取什么原则？

| 选项 | 描述 |
|------|------|
| A | 一律禁止 —— 任何内容都必须可无损回写到标准 Markdown |
| B | 允许增强语法，但必须有明确 fallback 文本表示 |
| C | 允许富文本专属能力，但要显式标记“不可移植/不可纯 Markdown 导出” |
| D | 按文档级策略切换 —— 某些文档严格 Markdown，某些文档允许增强能力 |

**你的选择**: __C_  
**补充**: __这里必须参考typora的策略_

## L1-08 | Round-trip Fidelity 的最低要求
Typora / Source / Preview / Export 之间最少要保证到什么程度的一致性？

| 选项 | 描述 |
|------|------|
| A | 只保语义一致，不保源码细节 |
| B | 保常见 Markdown 结构与格式一致，复杂增强能力允许降级 |
| C | 保大多数 Markdown 与核心增强语法无损 round-trip |
| D | 只要用户能看起来差不多即可，不强求严格 round-trip |

**你的选择**: __C_  
**补充**: __所有元素必须无损_

## L1-09 | Frontmatter / Metadata / Sidecar 的策略
文档元数据应该如何组织？

| 选项 | 描述 |
|------|------|
| A | 全部内置在主内容模型中，不暴露 frontmatter |
| B | 支持 YAML frontmatter，作为 Markdown 文档的一部分 |
| C | 主体内容与元数据分离，使用 sidecar / 数据库字段管理 |
| D | C + 对导出 / 发布 / 搜索定义明确映射规则 |

**你的选择**: _D__  
**补充**: __必须在源文件中进行设置，同时使用数据库字段管理。_

---

## C. 编辑范式与纸张体验

## L1-10 | 默认编辑范式
用户默认进入哪种编辑范式？

| 选项 | 描述 |
|------|------|
| A | Markdown 优先 —— 所见即源码，富文本只是辅助 |
| B | Typora 式混合编辑优先 —— 平时所见即内容，光标进入才暴露语法 |
| C | 结构化块编辑优先 —— Markdown 是导出层，不是主交互层 |
| D | 多范式并存 —— 用户可在文档/工作区级别指定默认范式 |

**你的选择**: _B__  
**补充**: ___

## L1-11 | 模式切换的一致性要求
不同编辑模式之间切换时，系统应保证到什么程度的一致性？

| 选项 | 描述 |
|------|------|
| A | 只保证内容语义基本一致，不保证格式/光标/历史完全继承 |
| B | 保证内容与常见格式一致，历史记录可局部重置 |
| C | 保证内容、选区、撤销栈、版本点、评论锚点尽量连续 |
| D | 不允许“自由切换”，一篇文档创建时就固定编辑范式 |

**你的选择**: _C__  
**补充**: _所有状态必须继承__

## L1-12 | 纸张式体验的冻结项
以下“现有编辑器气质”中，哪些必须被锁定为不可破坏？

| 选项 | 描述 |
|------|------|
| A | 只冻结纸张居中 + 宽度档位，其他可灵活改 |
| B | 冻结纸张视觉、颜色体系、行距/字体基调，不破坏整体书写感 |
| C | B + 冻结工作台的安静感，限制过重工具栏、花哨动效、Notion 化块编辑 |
| D | C + 明确“任何新功能都必须优先服从纸张式写作体验” |

**你的选择**: _B__  
**补充**: __附上流程交互元素是可以允许的。必须提供选项允许用户使用过重工具栏或简化；动效是欢迎的_

## L1-13 | 块级能力与 Markdown 原文的关系
对表格、代码块、公式、Mermaid、图片、Callout 这类“重块级元素”，你希望 InkForge 更偏哪种取向？

| 选项 | 描述 |
|------|------|
| A | 始终优先看到 Markdown / 源码结构 |
| B | 默认看到渲染结果，进入时再暴露源码 |
| C | 不同元素不同策略，由元素类型决定 |
| D | 由用户在设置中决定“源码优先”或“渲染优先” |

**你的选择**: _B__  
**补充**: __参照typora进行优化完善_

---

## D. 评论、批注与审阅

## L1-14 | 评论能力是否进入 v2.1 主线
评论/批注/审阅是否属于 v2.1 的正式范围？

| 选项 | 描述 |
|------|------|
| A | 不进入 v2.1，本轮只关注写作与导出 |
| B | 进入，但只做轻量批注，不做正式审阅流程 |
| C | 进入，并提供基础审阅闭环 |
| D | 进入，并作为团队协作主功能之一重点建设 |

**你的选择**: _C__  
**补充**: __进入v2.1，但不作为团队协作功能。因为团队协作是远期功能_

## L1-15 | 评论锚点粒度
评论/批注最小要锚定到什么粒度？

| 选项 | 描述 |
|------|------|
| A | 仅文档级评论 —— 不做正文锚点 |
| B | 块级评论 —— 对段落/标题/列表项/代码块挂评论 |
| C | 行内范围评论 —— 可精确到一段文本范围 |
| D | C + 结构评论 —— 既可锚定行内，也可锚定块与元数据 |

**你的选择**: __C_  
**补充**: __必须允许跨版本漂移。最大程度保证锚点不会失效__

## L1-16 | 审阅状态机

评论与审阅应形成什么样的正式流程？

| 选项 | 描述 |
|------|------|
| A | 仅讨论线程 —— 评论可回复/关闭，不做正式审阅状态 |
| B | 轻量审阅 —— 待处理 / 已解决 / 已忽略 |
| C | 正式审阅 —— Comment / Request Changes / Approve 三态 |
| D | C + 审阅责任制 —— 明确指派、截止时间、强制通过条件 |

**你的选择**: __C_  
**补充**: __配合远期团队协作版本发布后细化_

---

## E. 历史、恢复与自动保存

## L1-17 | 版本快照生成策略
历史版本应按什么机制产生？

| 选项 | 描述 |
|------|------|
| A | 仅手动保存版本 —— 自动保存不进入历史 |
| B | 自动保存覆盖当前稿 + 用户可手动打版本点 |
| C | 自动快照 + 手动版本并存 —— 按时间/关键动作生成恢复点 |
| D | C + 里程碑语义版本 —— 如“审阅前/发布前/导出前”强制留痕 |

**你的选择**: _C__  
**补充**: __快照触发条件以修改为截止；保存数量不设上限；清理策略以内存占比以及向用户警告为主；显示diff_

## L1-18 | 恢复操作的语义
用户点击“恢复某版本”时，系统应如何执行？

| 选项 | 描述 |
|------|------|
| A | 直接覆盖当前文档 |
| B | 先生成当前版本备份，再覆盖 |
| C | 恢复为一个新分支/副本，用户自行比较合并 |
| D | 打开双栏 diff/merge 视图，让用户选择性恢复 |

**你的选择**: __D_  
**补充**: __所有状态跟随恢复__

## L1-19 | 自动保存失败时的默认策略
自动保存失败时，系统默认如何保护用户？

| 选项 | 描述 |
|------|------|
| A | Toast 提示失败，用户自行重试 |
| B | 本地临时缓冲 + 后台重试 + 显示未保存状态 |
| C | B + 强制创建本地恢复点，防止页面刷新或崩溃丢稿 |
| D | C + 提供“查看保存失败明细 / 手动导出应急副本”能力 |

**你的选择**: __D_  
**补充**: __失败重试一次；需要用户可见性完全；需要日志_

---

## F. 同步、冲突与多端边界

## L1-20 | Sync 在 v2.1 的真实定位
同步能力在 v2.1 中的真实定位是什么？

| 选项 | 描述 |
|------|------|
| A | 只保留本地接口与 UI 预留，不做真实同步功能 |
| B | 提供最小可用同步能力，但不做复杂多人并发 |
| C | 提供稳定的本地优先 + 多端同步能力 |
| D | 提供同步 + 冲突解决 + 审计留痕的正式协作基础设施 |

**你的选择**: __D_  
**补充**: __作为远期的基础，但不完全为远期服务，是v2.1必须落地的内容。必须落地WebDAV/Git/自有服务_

## L1-21 | 冲突检测粒度与时机
同步冲突应该在什么粒度、什么时机被识别？

| 选项 | 描述 |
|------|------|
| A | 文档级 —— 保存/同步时发现整篇冲突 |
| B | 块级 —— 同步时按段落/块检测冲突 |
| C | 行内/标记级 —— 尽量细粒度检测与合并 |
| D | 第一版不做多人并发写作，默认单写者，无需复杂冲突模型 |

**你的选择**: _D__  
**补充**: __远期开发时必须调整细化。但在这里需要注意：我需要的是行内/标记级_

## L1-22 | 冲突解决默认策略
发生冲突时，产品默认怎么处理？

| 选项 | 描述 |
|------|------|
| A | Last Write Wins —— 最新写入覆盖旧版本 |
| B | 本地优先 / 远端优先 二选一，由设置决定 |
| C | 强制进入人工合并界面，用户必须显式决策 |
| D | 尝试三方合并，只有失败时才进入人工处理 |

**你的选择**: _D__  
**补充**: __一律以用户解决为主。写入审计日志_

---

## G. 多账户与工作区隔离

## L1-23 | 多账户隔离对象范围
“账户隔离”到底要隔离哪些对象？

| 选项 | 描述 |
|------|------|
| A | 只隔离账户资料，文章/设置/缓存共用 |
| B | 隔离文章、分类、设置，资源与日志共用 |
| C | 隔离文章、分类、设置、资源、历史、同步状态、密钥、命令记录 |
| D | 直接按“本地工作区/资料库”隔离，每个账户对应独立数据库与文件根 |

**你的选择**: __D_  
**补充**: __允许共享共享模板、共享导出预设、共享 AI 配置__

## L1-24 | 账户切换的身份边界
切换账户时，系统边界应该有多硬？

| 选项 | 描述 |
|------|------|
| A | 同窗口热切换，尽量保留 UI 状态 |
| B | 切换时清空当前会话态并重新初始化 Store |
| C | 切换账户等同切换 Profile，需完整重新加载数据库/缓存/密钥 |
| D | 支持多账户并行打开，不同窗口/工作区完全独立 |

**你的选择**: _D__  
**补充**: __支持多账户并行打开，等同切换profile，不必要自动关闭文档_

---

## H. 知识增强与引用溯源

## L1-25 | 知识增强的来源边界
InkForge 的知识增强第一版允许接入哪些来源？

| 选项 | 描述 |
|------|------|
| A | 仅用户手动粘贴/导入的本地内容 |
| B | A + URL 抓取 / 文件导入 / 网页解析 |
| C | B + 项目知识库 / 本地资料库 / 结构化数据源 |
| D | C + 外部连接器（云文档、代码仓、数据库、第三方知识库） |

**你的选择**: _D__  
**补充**: ___

## L1-26 | 引用与溯源的强制程度
知识增强输出是否必须带来源与可信度标记？

| 选项 | 描述 |
|------|------|
| A | 不强制，用户自行判断 |
| B | 推荐带来源，但不作为强制门槛 |
| C | 生成摘要/建议时必须附带引用卡片与原文跳转 |
| D | C + 明确区分“原始事实 / 模型推断 / 用户手写内容”三层来源 |

**你的选择**: _D__  
**补充**: _必须做到引用显示位置，导出时保留，发布时可隐藏_

---

## I. 命令系统、快捷键与轻工具栏分工

## L1-27 | 命令系统的统一入口
参考当前优秀编辑器与 IDE 的做法，命令系统应通过哪些入口统一暴露？

| 选项 | 描述 |
|------|------|
| A | 仅斜杠命令 `/` |
| B | 斜杠命令 + Command Palette |
| C | B + 工具栏/右键菜单/快捷键统一映射到同一命令注册表 |
| D | C + 支持“可组合命令流/工作流模板” |

**你的选择**: _D__  
**补充**: _做到区分编辑命令 / 系统命令 / AI 命令 / 发布命令__

## L1-28 | 命令执行的权限与回滚要求
命令系统能直接改数据吗？是否需要确认与回滚？

| 选项 | 描述 |
|------|------|
| A | 可直接执行，用户自己负责后果 |
| B | 只对高风险命令弹确认 |
| C | 所有会改数据的命令都必须确认，并自动生成可回滚点 |
| D | C + 命令执行要受权限策略约束，并写入审计日志 |

**你的选择**: ___  
**补充**: ___（建议写明：哪些命令属于高风险、是否允许批量命令、失败后是否部分回滚）

## L1-29 | 快捷键、斜杠命令、浮动工具栏的职责分工
为了避免功能发现路径混乱，你希望三者如何分工？

| 选项 | 描述 |
|------|------|
| A | 快捷键给熟练用户，斜杠命令给插入/创建，浮动工具栏只做选区格式化 |
| B | A + 右键菜单负责上下文补充命令 |
| C | 所有入口都尽量等价，只是触达方式不同 |
| D | 根据模式区分 —— Typora / Source / Preview / Hub 各有不同命令策略 |

**你的选择**: _A__  
**补充**: ___

---

## J. 导出、预览与发布一致性

## L1-30 | 预览 / 导出 / 发布是否必须共享同一渲染链路
“本地预览 / 导出 HTML / 渠道发布”是否必须来自同一条内容转换链路？

| 选项 | 描述 |
|------|------|
| A | 不要求一致，各模块独立实现即可 |
| B | 尽量一致，但渠道可各自补丁式处理 |
| C | 必须共享同一 canonical render pipeline，渠道只做尾部适配 |
| D | C + 明确规定任何平台适配都不能反向污染编辑权威模型 |

**你的选择**: __D_  
**补充**: __必须以markdown编辑器文本内容作为唯一权威，渲染结果根据各家渲染规则进行适配，由此得知不可能来自同一条内容转换链路，各个平台各作为一个链路_

## L1-31 | 视觉保真 vs 平台合规的优先级
当“编辑器里看起来一样”和“平台发布能通过”冲突时，优先哪边？

| 选项 | 描述 |
|------|------|
| A | 优先视觉保真 —— 渠道失败由用户承担 |
| B | 优先平台合规 —— 必要时牺牲部分样式 |
| C | 由导出预设决定，不同渠道有不同优先级 |
| D | 先保语义与结构，再做可接受范围内的视觉保真 |

**你的选择**: __C_  
**补充**: __微信公众号 / 小红书 / 知乎 / HTML / PDF由于各平台支持规则不一，一定是有不同策略，不可能存在统一策略。_

## L1-32 | 数学公式 / Mermaid / 代码高亮的导出契约
公式、Mermaid、代码高亮在导出与发布时要遵守什么契约？

| 选项 | 描述 |
|------|------|
| A | 编辑器内可用即可，导出降级无所谓 |
| B | 编辑器与导出都要可见，但允许不同渲染方式 |
| C | 编辑器、预览、导出三者必须在语义和视觉上高度一致 |
| D | C + 必须定义失败降级策略（源码保留 / 占位提示 / 回退图像） |

**你的选择**: _C__  
**补充**: __以微信为例，支持CSS和SVG规则诡异，必须按照微信渲染规则进行适配_

---

## K. 权限、分享与审计

## L1-33 | 权限模型应细到什么程度
InkForge 是否需要正式权限模型？如果需要，应细到什么层级？

| 选项 | 描述 |
|------|------|
| A | 第一版不做正式权限模型，默认本地单用户 |
| B | 做基础角色划分 —— 管理员 / 作者 / 审阅人 / 访客 |
| C | 做资源级权限 —— 文档 / 文件夹 / 评论 / 版本 / 发布动作均可授权 |
| D | 做关系型权限（ReBAC）—— 主体、对象、关系、继承规则都可配置 |

**你的选择**: __C_  
**补充**: __结合本地多账户多profile进行设计_

## L1-34 | 审计日志的覆盖范围
哪些行为必须进入不可抵赖的审计轨迹？

| 选项 | 描述 |
|------|------|
| A | 仅安全相关 —— 登录、切换账户、删除、恢复、导入导出 |
| B | A + 编辑生命周期 —— 创建、修改、恢复、发布、同步解决冲突 |
| C | B + 审阅与权限 —— 评论、关闭线程、审批、授权、撤权 |
| D | C + AI/命令系统 —— 任何自动化修改与批量操作都必须留痕 |

**你的选择**: ___A+B+C  
**补充**: __全范围审计，留存期限为3个月，可以导出，必须由用户本身可以查看_

---

## L. 性能、规模与降级策略

## L1-35 | 第一版性能与规模假设
你预期 InkForge 第一版面对的真实规模是怎样的？

| 选项 | 描述 |
|------|------|
| A | 单用户、小文档优先 —— 只要普通文章流畅即可 |
| B | 大文档优先 —— 10 万字、长表格、长代码块、较多图片也要可用 |
| C | 多文档/多项目优先 —— 大量文件、分类、搜索、版本历史要稳定 |
| D | B + C + 团队并发 —— 大文档、多资料库、多账户、多端同步并存 |

**你的选择**: _C__  
**补充**: _最大文档长度900000+字符；最大附件数量2000+；最大版本数999；最大账户数50+。需要注意，先版本多账户并非为团队服务__

## L1-36 | 性能 SLO 与降级策略
当性能接近极限时，系统应如何降级？

| 选项 | 描述 |
|------|------|
| A | 不设明确 SLO，能用即可 |
| B | 设关键阈值 —— 首屏、输入延迟、保存、预览、导出都要有目标 |
| C | B + 超阈值时自动降级 —— 关闭某些实时能力、延迟某些计算 |
| D | C + 对每个能力定义“可关闭 / 必须保真 / 可后台处理”的等级 |

**你的选择**: _C__  
**补充**: _必须保证输入无延迟；保存耗时不超过1秒；冲突检测耗时不超过10秒；导出耗时不超过3分钟__（建议写明：输入延迟、保存耗时、冲突检测耗时、导出耗时的可接受范围）

---

## M. 扩展、插件与外部集成边界

## L1-37 | 扩展能力开放到哪一层
你希望 InkForge 最终允许扩展什么？

| 选项 | 描述 |
|------|------|
| A | 仅主题 / 样式 / 导出预设级扩展 |
| B | A + 编辑器节点 / 命令 / 工具栏扩展 |
| C | B + 同步 Provider / 发布渠道 / 知识源连接器 |
| D | C + 完整插件 SDK —— 生命周期、权限、事件、UI 注入点都开放 |

**你的选择**: _D__  
**补充**: _本项目完全开源，全部开放。但保证外部集成不作恶__

## L1-38 | 外部集成的安全边界
外部扩展或连接器可以拥有多大权限？

| 选项 | 描述 |
|------|------|
| A | 只允许官方内置扩展，不开放第三方 |
| B | 允许本地安装第三方扩展，但默认高信任 |
| C | 允许第三方扩展，但必须声明权限并受沙箱限制 |
| D | C + 扩展签名、权限审查、审计留痕、版本兼容承诺都必须存在 |

**你的选择**: _C__  
**补充**: __服务于写作的集成_

---

## N. 竞品借鉴、风格冻结与冗余开发

## L1-39 | 竞品借鉴优先级
你希望 InkForge 主要借鉴哪类产品的“方法”，而不是照搬其全部形态？

| 选项 | 描述 |
|------|------|
| A | Typora / MarkText / iA Writer —— 以 Markdown-first、纸张式、安静写作为核心 |
| B | Obsidian / Logseq —— 以知识增强、链接、资料组织为核心 |
| C | Notion / Craft / Heptabase —— 以块编辑、数据库、协作审阅为核心 |
| D | A 为主，吸收 B/C 的少量能力，但不允许整体产品气质被带偏 |

**你的选择**: _A__  
**补充**: ___

## L1-40 | 冗余开发与防呆强度
考虑到你明确要求“冗余开发、杜绝傻瓜使用”，本轮默认要把防呆做到什么程度？

| 选项 | 描述 |
|------|------|
| A | 只做基础校验，不做重度防呆 |
| B | 常见错误有提示，危险操作需二次确认 |
| C | C 级防呆 —— 删除/恢复/导入/导出/切账户/批量命令都要确认或备份 |
| D | 高强度防呆 —— 高风险操作必须可回滚、可审计、可追踪，并尽量默认安全 |

**你的选择**: _C__  
**补充**: _设置高危险操作__

---

# 第二层：Task 级实现问卷（在第一层完成后填写）

> **目的**: 这一层保留并扩展原有 Task 级细问，用来把具体页面、组件、交互、数据表现、技术路线说细。  
> **填写原则**:
> 1. 如果某个问题已经被第一层锁死，请直接引用第一层答案  
> 2. 如果某题明显属于“实现治理 / 项目排期 / 分支策略”，可作为可选题填写  
> 3. 每个 Task 前都先看“当前现状 / 依赖前提 / 本轮目标”，避免脱离当前代码现实发散

---

## 第一组：实施治理与全局配套（10 题，可选填写）

## G-01 | 任务执行顺序
P0 的 4 个 task 之间的串并行关系。

| 选项 | 描述 |
|------|------|
| A | 严格串行 01→02→03→04，每个做完验证后再开始下一个 |
| B | 01(Typora)+02(Hub) 并行 → 03(快捷键)→04(渲染引擎) |
| C | 01+02+03 三并行 → 04(因为04依赖01的编辑器基础) |
| D | 全部尽量并行，通过 git 分支隔离 |

**你的选择**: _C__
**补充**: ___

## G-02 | 分支策略
| 选项 | 描述 |
|------|------|
| A | 在当前 `dev/visual-fixes` 继续，用 commit 区分 task |
| B | 从 main 新建 `dev/v2.1`，每个 task 一个子分支 merge 回 v2.1 |
| C | 从 main 为每个 task 建独立 feature 分支，逐个 PR 合回 main |

**你的选择**: _C__
**补充**: ___

## G-03 | 测试策略
| 选项 | 描述 |
|------|------|
| A | 不写自动化测试，纯手动验收 + `pnpm build` + `vue-tsc --noEmit` |
| B | 关键 Store/Utils 写 Vitest 单测，UI 手动验收 |
| C | Vitest 单测 + Playwright E2E 覆盖核心流程 |

**你的选择**: _C__
**补充**: ___

## G-04 | 打包体积策略
KaTeX ~300KB, Mermaid ~1MB+, highlight.js all ~800KB

| 选项 | 描述 |
|------|------|
| A | 全部打入主包，不做 lazy load |
| B | KaTeX/Mermaid 做 dynamic import，首次使用时加载 |
| C | B + highlight.js 也按需加载语言包 |

**你的选择**: __C_
**补充**: ___

## G-05 | 原型参考文件状态
多个 spec 引用 `prototype/inkforge_hub.html`。

| 选项 | 描述 |
|------|------|
| A | 仍可用，作为视觉参考 |
| B | 已过期但部分可参考 |
| C | 不存在/已丢失，需要重新描述视觉 |

**你的选择**: _A__
**补充**: ___

## G-06 | InkForge 运行环境
影响快捷键冲突处理、API 权限、文件系统访问等核心决策。

| 选项 | 描述 |
|------|------|
| A | 纯 Tauri 桌面应用，不需要在浏览器中运行 |
| B | 主要 Tauri，但需要浏览器兼容 (开发调试用) |
| C | Tauri + Web 双端发布 |

**你的选择**: __B_
**补充**: ___

## G-07 | 错误处理与用户反馈策略
spec 中没有提到全局错误处理。

| 选项 | 描述 |
|------|------|
| A | 使用 Toast 通知 (右下角弹出，自动消失) |
| B | 使用 Snackbar (底部横条，需手动关闭) |
| C | 使用 shadcn-vue 的 Sonner/Toast 组件 |
| D | 当前系统已有 Toast，沿用 |

**你的选择**: _D__
**补充**: ___

## G-08 | 国际化 (i18n) 是否考虑？
| 选项 | 描述 |
|------|------|
| A | v2.1 不考虑，所有 UI 文案硬编码中文 |
| B | 用 vue-i18n 做基础架构，v2.1 只出中文 |
| C | 中英双语 |

**你的选择**: __C_
**补充**: ___

## G-09 | 无障碍 (a11y) 标准
| 选项 | 描述 |
|------|------|
| A | 不做特别处理 |
| B | 基础 aria-label + 键盘可访问 + 足够对比度 |
| C | WCAG AA 级合规 |

**你的选择**: __A_
**补充**: ___

## G-10 | 从 v2.0 到 v2.1 的数据迁移
如果用户已有 v2.0 的 IndexedDB 数据，v2.1 的 schema 变更如何处理？

| 选项 | 描述 |
|------|------|
| A | Dexie 的版本升级机制自动迁移 |
| B | 首次启动时跑迁移脚本 + 用户提示 |
| C | v2.1 是全新安装，不兼容 v2.0 数据 |

**你的选择**: _A__
**补充**: ___

### 第一组深挖补充题（建议在本组基础题后继续填写）

## G-11 | 原型 / 文档 / 代码三者的权威顺序

当 `prototype`、`prompts/0327`、当前代码三者对同一功能描述不一致时，后续 PRD / Spec 应优先以什么为准？

| 选项 | 描述 |
|------|------|
| A | 原型优先，代码和文档都向视觉稿靠拢 |
| B | 0327 文档优先，代码与原型都按 spec 修正 |
| C | 运行时代码优先，再把文档和原型回写对齐 |
| D | 先逐项判定权威来源，并在后续 PRD 中显式记录每一项的真值来源 |

**你的选择**: _?__
**补充**: _以最新日期文档优先，但需要逐项判断权威来源__

## G-12 | Web 调试态与 Tauri 发布态的兼容矩阵
同一功能在 Web 调试态与 Tauri 发布态之间，你希望后续文档如何定义兼容等级？

| 选项 | 描述 |
|------|------|
| A | 只要求 Tauri 最终可用，Web 仅供开发 |
| B | 主要保证 Web 与 Tauri 主路径一致，平台特定功能允许降级 |
| C | Web / Tauri 双端都要求完整主路径可用 |
| D | 为每个功能定义兼容矩阵：Web 完整 / Web 降级 / Tauri 独占 / 双端禁用 |

**你的选择**: __A_
**补充**: ___文件系统、剪贴板、导入导出、快捷键、系统菜单均属于Tauri发布态，web只是方便开发

## G-13 | 全局错误分级标准
为了避免后续每个 Task 各写各的错误处理，错误等级需要怎样的统一标准？

| 选项 | 描述 |
|------|------|
| A | 不做统一分级，按各模块自己处理 |
| B | 分为信息 / 警告 / 错误 三层 |
| C | 分为提示 / 可恢复错误 / 阻断错误 / 数据风险错误 四层 |
| D | C + 明确每一层对应的 UI 呈现、日志级别、是否自动备份、是否阻止继续操作 |

**你的选择**: _D__
**补充**: ___保存失败、迁移失败必须视为“数据风险错误”___

## G-14 | 验收证据的留存形式

后续每个 Task 做完后，验收证据需要保留到什么程度？

| 选项 | 描述 |
|------|------|
| A | 不强制留痕，只要人工确认过即可 |
| B | 每个 Task 至少保留文字验收结论 |
| C | 每个 Task 保留“步骤 + 结果 + 截图/日志/导出样本” |
| D | C + 关键链路保留失败样本、恢复样本、边界样本，形成正式验收矩阵 |

**你的选择**: __D_
**补充**: ___

## G-15 | 冗余开发的默认落点
你要求“冗余开发”，那么后续默认应优先把冗余放在哪些维度？

| 选项 | 描述 |
|------|------|
| A | 优先放在 UI 引导与空状态，减少用户迷路 |
| B | 优先放在保存 / 恢复 / 回滚 / 导出，减少事故风险 |
| C | 优先放在命令系统、快捷键、帮助提示，提高可达性 |
| D | B + C + 首次引导，凡是高频与高风险路径都默认多一层防呆和兜底 |

**你的选择**: _B__
**补充**: ___

---

# 第二组：Task 01 — Typora 模式编辑器 [仅文档存在，代码未落地] (15 题)

> **当前现状**: 当前代码仍是单一路径 TipTap 编辑流，更接近 HTML 持久化；未见成熟的 `TyporaMode` / `MarkdownHints` / `KeyboardShortcuts` 核心扩展真正注册落地。  
> **依赖第一层**: `L1-05` `L1-06` `L1-07` `L1-08` `L1-10` `L1-11` `L1-12` `L1-13`  
> **本组目标**: 锁定 Typora / Source 双模式的真实契约、模式切换损耗边界、元素覆盖优先级与纸张体验。

## T01-01 | 实现路径
| 选项 | 描述 |
|------|------|
| A | 增强 MarkdownHints.ts 加 `cursorAware` 模式 (复用现有代码) |
| B | 新建 TyporaMode.ts 独立扩展 (职责清晰，不影响原有) |
| C | TyporaMode.ts 处理块级 + 增强 MarkdownHints.ts 处理行内 Mark |

**你的选择**: __A_
**补充**: ___

## T01-02 | 嵌套 Mark 的语法标记显示

当粗体和斜体嵌套时 `***bold italic***`，光标进入后：

| 选项 | 描述 |
|------|------|
| A | 显示所有嵌套 mark 的标记: `***bold italic***` |
| B | 只显示最外层 mark 的标记 |
| C | 分别显示: `**` 在粗体边界 + `*` 在斜体边界 |

**你的选择**: _C__
**补充**: ___

## T01-03 | 语法标记的出现/消失动画
光标进入/离开时，`**` `#` 等标记的显隐：

| 选项 | 描述 |
|------|------|
| A | 无动画，瞬间显示/隐藏 |
| B | 淡入淡出 (opacity transition, 150ms) |
| C | 淡入淡出 + 轻微横向滑动 (mark 标记从左侧滑入) |

**你的选择**: _A__
**补充**: ___

## T01-04 | 语法标记的视觉样式
`#` `**` `` ` `` 等标记的颜色和透明度：

| 选项 | 描述 |
|------|------|
| A | 品牌红 (#D32F2F) 低透明度 (opacity 0.35) |
| B | 灰色 (#90A4AE) 低透明度 |
| C | 与文字同色但 opacity 0.25 |
| D | 自定义颜色 (请在补充中说明) |

**你的选择**: __C_
**补充**: ___

## T01-05 | 模式切换时的内容转换
Typora→Source 需要 HTML→Markdown。

| 选项 | 描述 |
|------|------|
| A | 可接受少量丢失 (textAlign/color 等在 Markdown 中丢失) |
| B | 内部始终存 TipTap JSON，Source 模式只是显示序列化的 Markdown 文本 |
| C | 两种模式共享同一个 TipTap Editor 实例，Source 模式用 CodeMirror 编辑序列化的 MD |

**你的选择**: _B__
**补充**: ___

## T01-06 | Source 模式编辑器技术栈
| 选项 | 描述 |
|------|------|
| A | vue-codemirror (CodeMirror 6) — 功能丰富但重 |
| B | Monaco Editor — VS Code 内核，更重 |
| C | 简单 `<textarea>` + 自定义语法高亮 — 轻量但功能少 |
| D | @codemirror/lang-markdown 最小化配置 |

**你的选择**: __A_
**补充**: ___

## T01-07 | 表格在 Typora 模式下的行为
| 选项 | 描述 |
|------|------|
| A | 始终显示为富文本表格，不做 Typora 切换 |
| B | 光标进入时加视觉提示 (虚线边框)，不显示 pipe 语法 |
| C | 完整 Typora 行为: 光标进入切换到文本编辑，离开渲染为表格 |

**你的选择**: _C__
**补充**: ___

## T01-08 | 代码块在 Typora 模式下的行为
| 选项 | 描述 |
|------|------|
| A | 光标进入时显示 ``` 围栏标记 + 语言标签，离开时隐藏围栏 |
| B | 始终显示围栏标记 (代码块特殊处理，不做 Typora 切换) |
| C | 光标进入时展开为可编辑文本，离开时折叠为只显示前几行的预览 |

**你的选择**: _A__
**补充**: ___

## T01-09 | 链接在 Typora 模式下的交互
光标离开链接后的行为：

| 选项 | 描述 |
|------|------|
| A | 渲染为蓝色可点击文字，单击跟随链接 |
| B | 渲染为蓝色文字，单击将光标置入 (进入编辑)，Ctrl+Click 跟随链接 |
| C | 渲染为蓝色文字 + hover 时显示 URL tooltip，Ctrl+Click 跟随 |

**你的选择**: __C_
**补充**: ___

## T01-10 | 图片在 Typora 模式下的行为
| 选项 | 描述 |
|------|------|
| A | 始终渲染为图片，光标进入时在图片下方显示 `![alt](url)` 编辑栏 |
| B | 光标进入时显示 `![alt](url)` 语法，离开时渲染图片 |
| C | 始终渲染图片，双击进入编辑模式 |

**你的选择**: __C_
**补充**: ___

## T01-11 | 纸张宽度切换的交互方式
4 档 (narrow/medium/wide/full)。

| 选项 | 描述 |
|------|------|
| A | 仅通过 Settings 设置，无快捷切换 |
| B | Settings + StatusBar 下拉选择器 |
| C | Settings + `Ctrl+=` 循环切换 + StatusBar 显示当前档位 |
| D | Settings + 拖拽纸张边缘调整 (Typora 原版行为) |

**你的选择**: _C__
**补充**: ___

## T01-12 | 专注模式下的编辑体验
进入专注模式后：

| 选项 | 描述 |
|------|------|
| A | 仅隐藏 UI 面板，编辑器行为不变 |
| B | 隐藏 UI + 当前段落高亮 (其他段落降低透明度) |
| C | 隐藏 UI + 当前段落高亮 + 打字机模式自动激活 |

**你的选择**: _B__
**补充**: ___

## T01-13 | 模式切换的触发反馈
用户按 Ctrl+\ 切换模式时：

| 选项 | 描述 |
|------|------|
| A | 直接切换，无过渡效果 |
| B | 淡入淡出过渡 (200ms) |
| C | 过渡 + StatusBar 短暂闪烁提示当前模式 |
| D | 过渡 + Toast 通知 "已切换到 Typora/Source 模式" |

**你的选择**: __D_
**补充**: ___

## T01-14 | Undo/Redo 跨模式行为
在 Typora 模式编辑后切换到 Source，Ctrl+Z 的行为：

| 选项 | 描述 |
|------|------|
| A | 共享撤销栈 (切换模式不清空 history) |
| B | 切换模式时清空撤销栈 (各模式独立 history) |
| C | 切换模式时保存检查点，Ctrl+Z 先回退当前模式内的操作 |

**你的选择**: __C_
**补充**: ___

## T01-15 | 15+ 元素覆盖策略
| 选项 | 描述 |
|------|------|
| A | v2.1 一次性全部覆盖 19 种元素 |
| B | 先做 12 种核心 (标题x4 + 粗体/斜体/删除线/行内代码/链接 + 列表x3)，其余后续 |
| C | 块级全覆盖 + 行内先做 5 种 (粗体/斜体/行内代码/链接/删除线) |

**你的选择**: __A_
**补充**: ___

### Task 01 深挖补充题（建议在本组基础题后继续填写）

## T01-16 | 中文输入法与语法显隐的边界
当用户处于中文输入法组合态（`isComposing=true`）时，Typora 模式的语法标记显隐应如何处理？

| 选项 | 描述 |
|------|------|
| A | 组合输入期间完全冻结语法显隐，直到 `compositionend` 后再恢复 |
| B | 仅冻结当前光标附近的 mark / block，其余区域仍正常响应 |
| C | 不做特殊冻结，依赖编辑器默认行为 |
| D | A + 在组合输入期间临时关闭相关动画，优先稳定性 |

**你的选择**: _C__
**补充**: ___

## T01-17 | 外部粘贴内容进入 Typora 模式的清洗策略
用户从网页、微信、知乎、Notion、VS Code 等外部来源粘贴内容时，Typora 模式默认应如何处理？

| 选项 | 描述 |
|------|------|
| A | 一律按纯文本接收，尽量转成 Markdown 语义 |
| B | 优先保留结构语义（标题/列表/代码块/链接/表格），丢弃大部分样式 |
| C | 优先保留 HTML 结构与可见样式，再由内部模型兜底转换 |
| D | 根据粘贴来源或用户设置决定“纯文本模式 / 语义保留模式 / 样式保留模式” |

**你的选择**: __A_
**补充**: ___（建议写明：哪些来源必须强制清洗、哪些来源允许最大限度保留结构）

## T01-18 | 重块元素的光标进入与退出统一规则
对代码块、表格、公式、Mermaid、图片等“重块元素”，你希望采用怎样统一的进入/退出编辑规则？

| 选项 | 描述 |
|------|------|
| A | 单击选中块，双击进入内部编辑，Esc/点击外部退出 |
| B | 光标移动到块内即自动进入编辑，移出块时自动恢复渲染 |
| C | 不强求统一，不同块级元素采用不同进入规则 |
| D | 统一使用“块外选择态 / 块内编辑态”双层状态机，并为所有重块提供一致的退出手势 |

**你的选择**: _B__
**补充**: ___（建议写明：Enter、Tab、Esc、鼠标单击/双击在各类块上的具体语义）

## T01-19 | 非法或未闭合 Markdown 的临时态策略
当用户输入半截语法，如未闭合的 `````、`**`、`$`、链接括号不完整时，系统应如何处理？

| 选项 | 描述 |
|------|------|
| A | 保持原始文本，不尝试提前纠正或渲染 |
| B | 尝试轻量自动补全，但失败时回退为原始文本 |
| C | 原始文本保留 + 明确错误提示（下划线/边框/状态栏提示） |
| D | 在编辑态保持原始文本，失焦或切模式时再统一校正/报错 |

**你的选择**: _B__
**补充**: _无论任何语法都以最大程度尊重用户选择__

## T01-20 | Typora 模式下复制内容的语义
用户在 Typora 渲染态选中内容并复制时，默认应该复制什么？

| 选项 | 描述 |
|------|------|
| A | 复制当前可见文本（最接近用户眼前看到的结果） |
| B | 复制对应 Markdown 源码（保留 Markdown 语义） |
| C | 同时写入多个剪贴板格式：纯文本 + HTML + 尽可能保留 Markdown |
| D | 根据内容类型决定：普通文本复制可见文本，块级结构优先复制 Markdown / 结构化内容 |

**你的选择**: __D_
**补充**: ___

---

# 第三组：Task 02 — Hub 首页布局 [已有实现，需核边界] (12 题)

> **当前现状**: Hub 已有 Hero、Stats、Recent、Inspiration、Categories、QuickActionFab 等基础卡片，但创建入口偏多、职责边界仍不够干净。  
> **依赖第一层**: `L1-01` `L1-02` `L1-03` `L1-12` `L1-39` `L1-40`  
> **本组目标**: 重新确定 Hub 作为“入口页/仪表盘/导航页/空状态承载页”的真正职责，避免功能与 Workstation/Settings 混淆。

## T02-01 | Hero 卡片内容
| 选项 | 描述 |
|------|------|
| A | 直接搬 WritingFlowCard (7 根柱状图 + 周频分布) |
| B | 综合仪表盘: 本周字数进度环 + 连续创作天数 + 创作趋势迷你图 |
| C | 创作流图表 + "继续创作"按钮 + 最近文章快速入口 |
| D | 自定义设计 (请在补充中描述) |

**你的选择**: _A__
**补充**: __在 WritingFlowCard的基础上进行改善，使之更加美观_

## T02-02 | 删除 card-new 后 Section 2 内容
| 选项 | 描述 |
|------|------|
| A | 只保留 TemplateMarketCard + "快速开始"引导 |
| B | TemplateMarketCard + 精选文章 |
| C | 直接删除 Section 2，首屏后直接数据洞察区 |
| D | "创作工具"区: 模板 + 草稿箱 + 素材管理入口 |

**你的选择**: _D__
**补充**: ___

## T02-03 | Bento Grid 高度
| 选项 | 描述 |
|------|------|
| A | `min-height: calc(100vh - 160px)` + 内容超出时自然撑高 |
| B | 纯 auto 高度，不做视口撑满 |
| C | 保持视口撑满 + `grid-template-rows: auto` |

**你的选择**: _B__
**补充**: ___

## T02-04 | InspirationCard 引言来源
| 选项 | 描述 |
|------|------|
| A | 本地硬编码 50-100 条引言，按日期轮换 |
| B | 调用外部 API (需联网) |
| C | 从用户文章随机提取精彩句子 |
| D | A+C 混合: 有足够文章时用用户句子，否则用内置引言 |

**你的选择**: _D__
**补充**: __本地硬编码加上AI生成_

## T02-05 | 首屏各卡片的空状态设计
当用户是全新安装 (0 篇文章、0 个分类) 时：

| 选项 | 描述 |
|------|------|
| A | 每个卡片显示友好的空状态插图 + "开始创作"引导文案 |
| B | 卡片显示虚线占位 + 简单文字提示 |
| C | 空状态时首屏切换为新手引导 (Onboarding) 模式 |

**你的选择**: _C__
**补充**: ___

## T02-06 | card-recent (最近编辑) 显示几条？
合并 card-new 后 card-recent 扩展为 1x2。

| 选项 | 描述 |
|------|------|
| A | 最多 3 条最近编辑 + "从模板创建"和"空白草稿"按钮 |
| B | 最多 5 条最近编辑 + 底部"新建"按钮 |
| C | 动态: 空间允许时显示更多，最少 2 条 |

**你的选择**: _C__
**补充**: ___

## T02-07 | Hub 页面数据刷新策略
用户从 Workstation 返回 Hub 后：

| 选项 | 描述 |
|------|------|
| A | 每次进入 Hub 时完全重新加载数据 |
| B | Store 响应式自动更新，无需手动刷新 |
| C | 后台定时刷新 (每 30s) + 切换页面时检查脏数据 |

**你的选择**: _B__
**补充**: ___

## T02-08 | Hub 响应式断点
当视口宽度变化时：

| 选项 | 描述 |
|------|------|
| A | 1440px: 4列 → 1024px: 2列 → 768px: 1列 |
| B | 不做响应式，固定 4 列 + 最小宽度限制 |
| C | 4列 → 3列 → 2列 → 1列 四级响应 |

**你的选择**: __C_
**补充**: ___

## T02-09 | scroll-snap 行为
Hub 页面的分区滚动：

| 选项 | 描述 |
|------|------|
| A | 保持 scroll-snap (分区对齐滚动) |
| B | 移除 scroll-snap，改为自由滚动 |
| C | 可选: Settings 中可以开关 scroll-snap |

**你的选择**: _A__
**补充**: ___

## T02-10 | SectionNav 导航栏
Hub 右侧的分区导航小圆点：

| 选项 | 描述 |
|------|------|
| A | 保持当前设计 |
| B | 移除 SectionNav |
| C | 改为左侧垂直导航文字标签 |

**你的选择**: _A__
**补充**: ___

## T02-11 | QuickActionFab 浮动按钮的功能
右下角浮动按钮：

| 选项 | 描述 |
|------|------|
| A | 只有"新建文章"一个功能 |
| B | 展开菜单: 新建文章 + 从模板创建 + 导入 Markdown |
| C | 展开菜单: 新建文章 + 快速笔记 (轻量记录) + 导入 |

**你的选择**: _B__
**补充**: ___

## T02-12 | card-categories 点击行为
分类卡片点击后：

| 选项 | 描述 |
|------|------|
| A | 跳转到 Workstation 并打开该分类下的文件管理器 |
| B | 展开显示该分类下的最近文章列表 |
| C | 无点击行为，纯展示 |

**你的选择**: _B__
**补充**: ___

### Task 02 深挖补充题（建议在本组基础题后继续填写）

## T02-13 | Hub 页面“新建入口”的预算上限
考虑到当前创建入口偏多，Hub 页面上最多允许保留多少个“新建 / 开始创作”入口？

| 选项 | 描述 |
|------|------|
| A | 只保留 1 个主入口 |
| B | 最多 2 个：一个主按钮 + 一个上下文入口 |
| C | 最多 3 个，但必须严格分工（新建 / 模板 / 导入） |
| D | 不限制数量，只要视觉上不乱即可 |

**你的选择**: _C__
**补充**: ___

## T02-14 | Hero 卡片的“继续创作”对象选择规则
如果 Hero 区承担“继续创作”职责，它默认应该指向哪篇文章？

| 选项 | 描述 |
|------|------|
| A | 最近打开的文章 |
| B | 最近编辑且未完成的文章 |
| C | 用户手动置顶的“当前主稿” |
| D | 由系统综合判断（最近编辑、字数目标、未完成状态、收藏标记） |

**你的选择**: __B_
**补充**: ___

## T02-15 | Hub 的空状态与首启引导的关系
当用户是全新安装且没有任何内容时，Hub 应更像“仪表盘”还是“引导页”？

| 选项 | 描述 |
|------|------|
| A | 仍保持完整仪表盘，只是每个卡片显示空状态 |
| B | 首次进入显示完整引导版 Hub，做完首个动作后再恢复常规 Hub |
| C | 用单独 Onboarding Flow 取代空 Hub |
| D | B + 支持随时重新打开新手引导，不与主 Hub 彻底分离 |

**你的选择**: _B__
**补充**: ___

## T02-16 | Hub 卡片的优先级与裁剪规则
当屏幕空间不足或信息过多时，Hub 上哪些卡片应最先保留，哪些应最先折叠/隐藏？

| 选项 | 描述 |
|------|------|
| A | Hero、Recent、Stats 必保留，其余可折叠 |
| B | Hero、Recent、Categories 必保留，其余可折叠 |
| C | Hero 必保留，其他全部按用户自定义排序和显隐 |
| D | 建立卡片优先级系统：核心卡片固定，其余卡片按屏幕和数据状态动态裁剪 |

**你的选择**: _A__
**补充**: ___

## T02-17 | Hub 与 Workstation 的导航职责边界
从 Hub 进入 Workstation 时，你希望导航体验更偏哪一种？

| 选项 | 描述 |
|------|------|
| A | Hub 只是入口，一旦进入 Workstation 就不再打断 |
| B | Hub 与 Workstation 是强耦合关系，频繁往返是正常路径 |
| C | Hub 更像项目首页，Workstation 更像文档级工作区，二者要有明显层级区分 |
| D | C + 需要明确返回策略、保留状态策略、最近上下文策略 |

**你的选择**: __C_
**补充**: ___

---

# 第四组：Task 03 — 键盘快捷键 [已有部分实现，需补全] (8 题)

> **当前现状**: 当前已有部分快捷键与浮动工具栏 title 提示，但与 0327 文档存在明显漂移，`Ctrl+\\` 还与左栏折叠冲突。  
> **依赖第一层**: `L1-27` `L1-28` `L1-29` `L1-40`  
> **本组目标**: 锁定命令发现路径、快捷键热更新、冲突处理、输入法兼容与帮助系统。

## T03-01 | FindReplace.vue 搜索能力
| 选项 | 描述 |
|------|------|
| A | 纯文本搜索 + 替换，无正则 |
| B | 支持正则搜索 + 大小写敏感切换 + 全词匹配 |
| C | B + 在结果间跳转 (上一个/下一个) + 匹配计数 |

**你的选择**: _C__
**补充**: ___

## T03-02 | FindReplace 面板位置
| 选项 | 描述 |
|------|------|
| A | 编辑器右上角浮动面板 (VS Code 风格) |
| B | 编辑区域顶部内嵌条 |
| C | 底部工具栏上方 |

**你的选择**: _A__
**补充**: ___

## T03-03 | 快捷键自定义后即时生效？
| 选项 | 描述 |
|------|------|
| A | 修改后即时生效 (watch settings 动态更新) |
| B | 修改后需要刷新页面 |
| C | 初始化时读取，不支持运行时变更 |

**你的选择**: _A__
**补充**: ___

## T03-04 | 快捷键冲突检测
| 选项 | 描述 |
|------|------|
| A | 录制时检测冲突，弹出警告但允许覆盖 |
| B | 录制时检测冲突，阻止保存直到解决 |
| C | 不做冲突检测，用户自行管理 |

**你的选择**: _A__
**补充**: ___

## T03-05 | 是否需要快捷键帮助面板？
类似 VS Code 的 `Ctrl+K Ctrl+S` 或 Notion 的 `Ctrl+/`。

| 选项 | 描述 |
|------|------|
| A | 需要 — 一个可搜索的快捷键速查面板 |
| B | 不需要 — Settings 的 Shortcuts Tab 已经是速查 |
| C | 需要 — 但做成 Tooltip 弹窗而非独立面板 |

**你的选择**: __C_
**补充**: ___

## T03-06 | Ctrl+\ 双重用途
| 选项 | 描述 |
|------|------|
| A | 接受双重用途 (有选区=清除格式，无选区=切换模式) |
| B | 分开: `Ctrl+\` 仅清除格式，模式切换改用 `Ctrl+Shift+M` |
| C | 分开: `Ctrl+\` 仅切换模式，清除格式改用 `Ctrl+Shift+N` |

**你的选择**: _C__
**补充**: ___

## T03-07 | Tab 键在编辑器中的行为
| 选项 | 描述 |
|------|------|
| A | 在列表中缩进/减少缩进，其他位置插入 Tab 字符 |
| B | 始终缩进/减少缩进，不插入 Tab 字符 |
| C | 列表中缩进，代码块中插入 Tab，其他位置切换焦点 |

**你的选择**: _C__
**补充**: ___

## T03-08 | 中文输入法兼容策略
| 选项 | 描述 |
|------|------|
| A | `event.isComposing` 时跳过所有快捷键 |
| B | 仅跳过 Ctrl+数字 (标题快捷键)，其他保持 |
| C | 不做特殊处理，依赖 TipTap 默认行为 |

**你的选择**: _B__
**补充**: ___

### Task 03 深挖补充题（建议在本组基础题后继续填写）

## T03-09 | 快捷键作用域
同一个快捷键是否允许在 Hub、Workstation、Source 模式、Typora 模式、Settings 中含义不同？

| 选项 | 描述 |
|------|------|
| A | 不允许，能全局统一就全局统一 |
| B | 允许少量上下文差异，但核心编辑快捷键必须统一 |
| C | 大量上下文差异是可接受的，只要帮助系统能解释清楚 |
| D | 建立明确的作用域层级：全局 > 页面级 > 编辑器模式级 > 组件级 |

**你的选择**: __A_
**补充**: _后续出问题再改，就先这样__

## T03-10 | 多段组合快捷键的支持程度
是否需要像 VS Code 一样支持“组合快捷键序列”？

| 选项 | 描述 |
|------|------|
| A | 不需要，全部用单步快捷键 |
| B | 只给少数高级命令保留组合快捷键能力 |
| C | 支持组合快捷键，并允许用户自定义 |
| D | C + 组合快捷键要可视化提示当前已进入的按键序列状态 |

**你的选择**: _D__
**补充**: _我对这件事情没概念，我不用快捷键。但我希望能适配这部分高技术用户__

## T03-11 | 与系统 / 浏览器保留快捷键的冲突策略
当自定义快捷键与浏览器、Tauri、系统层保留快捷键冲突时，默认如何处理？

| 选项 | 描述 |
|------|------|
| A | 用户可强行覆盖，系统自己承担结果 |
| B | 默认禁止覆盖常见保留快捷键 |
| C | 允许覆盖部分冲突键，但必须明确警告不可移植风险 |
| D | 建立冲突黑名单 / 灰名单 / 白名单，并区分 Web 与 Tauri 两套规则 |

**你的选择**: __？_
**补充**: __不可能与浏览器发生冲突，因为不做web版，只做app_

## T03-12 | 快捷键帮助系统的触达方式
如果做快捷键帮助系统，你希望它更偏“文档页”还是“交互式面板”？

| 选项 | 描述 |
|------|------|
| A | 只要静态列表页即可 |
| B | 可搜索的快捷键面板，支持按分组浏览 |
| C | B + 能直接在面板内录制/修改快捷键 |
| D | C + 支持按最近使用、冲突项、未绑定项、推荐项筛选 |

**你的选择**: _C__
**补充**: ___

## T03-13 | 快捷键失败时的提示策略
当用户按下一个快捷键但当前上下文不可执行时，应如何反馈？

| 选项 | 描述 |
|------|------|
| A | 静默无响应 |
| B | 状态栏短提示“当前不可用” |
| C | Toast / 气泡提示具体原因 |
| D | 提示原因 + 告诉用户在哪个上下文下该快捷键才可用 |

**你的选择**: _B__
**补充**: ___

---

# 第五组：Task 04 — 渲染引擎 [已有部分实现，需补全] (10 题)

> **当前现状**: 当前已经有导出服务、预览链路与部分渲染基础，但公式、Mermaid、统一 canonical render pipeline 仍未完全锁死。  
> **依赖第一层**: `L1-05` `L1-08` `L1-13` `L1-30` `L1-31` `L1-32` `L1-35` `L1-36`  
> **本组目标**: 锁定编辑态、预览态、导出态、发布态之间的一致性契约与降级策略。

## T04-01 | KaTeX 编辑体验
| 选项 | 描述 |
|------|------|
| A | 完整 WYSIWYG: 输入 `$...$` 时实时渲染，光标进入时显示源码 (Typora 风格) |
| B | 代码块风格: 输入完成后渲染，双击进入编辑 |
| C | 仅渲染: 从 Markdown 导入时渲染，不支持 WYSIWYG 编辑 |

**你的选择**: _A__
**补充**: ___

## T04-02 | KaTeX 错误处理
当用户输入无效 LaTeX 时：

| 选项 | 描述 |
|------|------|
| A | 显示 KaTeX 的红色错误信息 |
| B | 显示原始 LaTeX 源码 (不渲染) + 红色下波浪线 |
| C | 静默失败，显示原始文本 |

**你的选择**: __A_
**补充**: ___

## T04-03 | Mermaid 渲染方式
| 选项 | 描述 |
|------|------|
| A | 代码块 lang=mermaid 时，光标离开自动渲染图表 (Typora 风格) |
| B | 代码块旁边显示"预览"按钮，点击渲染 |
| C | 代码块始终显示源码，右侧 Stage 面板渲染 |

**你的选择**: _C__
**补充**: ___

## T04-04 | Mermaid 错误处理
| 选项 | 描述 |
|------|------|
| A | 显示 Mermaid 的错误信息 |
| B | 显示"图表语法错误"通用提示 |
| C | 保持代码块显示，不尝试渲染 |

**你的选择**: _A__
**补充**: ___

## T04-05 | 代码块语法高亮主题
| 选项 | 描述 |
|------|------|
| A | 亮色: github, 暗色: github-dark (与 InkForge 色系一致) |
| B | 亮色: one-light, 暗色: one-dark-pro |
| C | 使用自定义主题 (匹配 Ethereal Constructivism 色系) |
| D | 用户可在 Settings 中选择高亮主题 |

**你的选择**: _A__
**补充**: ___

## T04-06 | 代码块复制按钮行为
| 选项 | 描述 |
|------|------|
| A | 复制纯文本，按钮变为"已复制"图标 1.5s 后恢复 |
| B | A + 复制时包含语言标识注释 (如 `// JavaScript`) |
| C | A + 可选复制为富文本 (带语法高亮颜色) |

**你的选择**: _C__
**补充**: ___

## T04-07 | 代码块语言覆盖范围
| 选项 | 描述 |
|------|------|
| A | common (35 种，当前) |
| B | 扩展到 50 种常用语言 (按需注册) |
| C | 加载全部 (all, ~180 种，增加 ~400KB) |

**你的选择**: _C__
**补充**: ___

## T04-08 | 导出平台优先级
| 选项 | 描述 |
|------|------|
| A | 微信公众号优先，确保完美渲染 |
| B | 微信 + 知乎 + 小红书三平台同等优先 |
| C | 所有平台 + 纯 HTML + Markdown 导出 |
| D | 加上 PDF 导出 |

**你的选择**: _C__
**补充**: ___

## T04-09 | 预览面板实时刷新策略
编辑内容变化后预览面板的更新：

| 选项 | 描述 |
|------|------|
| A | 实时更新 (每次 keystroke) |
| B | 防抖更新 (停止输入 300ms 后刷新) |
| C | 手动刷新 (点击按钮或 Ctrl+Shift+P) |

**你的选择**: __A_
**补充**: ___

## T04-10 | 是否需要数学公式/Mermaid 在导出中正确渲染？
| 选项 | 描述 |
|------|------|
| A | 需要 — 导出 HTML 中包含渲染后的 SVG/MathML |
| B | 导出为图片 (截图替代) |
| C | v2.1 导出不支持公式/图表，仅编辑器内渲染 |

**你的选择**: __A_
**补充**: ___

### Task 04 深挖补充题（建议在本组基础题后继续填写）

## T04-11 | 公式与图表主题跟随策略
KaTeX、Mermaid、代码高亮在亮色 / 暗色 / 导出平台下的主题关系应如何定义？

| 选项 | 描述 |
|------|------|
| A | 编辑器主题与导出主题完全独立，各自单独配置 |
| B | 默认跟随全局主题，导出时按目标平台再做适配 |
| C | 公式、图表、代码高亮三者分别有独立主题策略 |
| D | 建立统一的“渲染主题映射表”，编辑态 / 预览态 / 导出态都从同一映射规则推导 |

**你的选择**: _B__
**补充**: _沿袭此前确定的一平台一渲染流程__

## T04-12 | 渲染失败后的缓存与重试策略
KaTeX / Mermaid / 代码高亮在渲染失败后，系统应如何避免抖动与重复报错？

| 选项 | 描述 |
|------|------|
| A | 每次输入都重新尝试渲染，错误即时刷新 |
| B | 内容不变时缓存失败结果，只在用户修改内容后再重试 |
| C | 失败后转入“手动刷新”模式，用户点击后再重试 |
| D | 失败后保留源码视图，同时后台按节流策略重试，并记录错误状态 |

**你的选择**: _A__
**补充**: _错误提示在修改之后自动消失__

## T04-13 | 代码块的阅读态与导出态契约
代码块在编辑器内和导出后，是否要统一支持这些增强能力？

| 选项 | 描述 |
|------|------|
| A | 只保留语法高亮，不做额外增强 |
| B | 语法高亮 + 复制按钮 + 自动换行 / 横向滚动二选一 |
| C | 语法高亮 + 复制按钮 + 行号 + 折行/横滚策略 |
| D | C + 对导出态也定义明确契约（哪些能力保留，哪些能力只存在于编辑器内） |

**你的选择**: __C_
**补充**: _渲染一律以平台自身的支持规则为准__

## T04-14 | 渲染资产的嵌入策略
导出时，图片、Mermaid 结果、公式渲染结果、代码高亮样式应如何被打包？

| 选项 | 描述 |
|------|------|
| A | 尽量全部内联（base64 / inline SVG / inline style），换取可移植性 |
| B | HTML 导出允许外链或相对资源引用，平台导出再转内联 |
| C | 根据目标平台分别决定：公众号优先内联，HTML/PDF 可保留外部资源 |
| D | 根据资源大小和类型做阈值策略，小资源内联，大资源保留引用 |

**你的选择**: _C__
**补充**: _一切以平台规则决定__

## T04-15 | 预览与导出的安全沙箱策略
考虑到 HTML、代码块、富文本粘贴、渠道适配，预览与导出链路需要多严格的安全限制？

| 选项 | 描述 |
|------|------|
| A | 仅清理明显危险标签/属性，优先保留内容表现 |
| B | 预览链路和导出链路统一走 DOMPurify 白名单清洗 |
| C | 预览、导出、发布三条链路采用不同级别的白名单与协议限制 |
| D | C + 明确定义禁止项（脚本、iframe、内联事件、危险协议、渠道不允许的样式）并对用户可见 |

**你的选择**: _A__
**补充**: _一切以平台规则决定__

---

# 第六组：Task 05 — 浮动工具栏 + 右键菜单 + 斜杠命令 [已有部分实现，需补全] (8 题)

> **当前现状**: 当前已有 `FloatingToolbar` 和基础 `SlashCommands`，但命令总表不完整、图片插入仍偏原始、不同入口之间没有明确统一注册表。  
> **依赖第一层**: `L1-27` `L1-28` `L1-29` `L1-40`  
> **本组目标**: 明确轻工具栏、上下文菜单、命令系统之间的职责边界，而不是无序叠加入口。

## T05-01 | 右键菜单剪贴板操作
| 选项 | 描述 |
|------|------|
| A | Tauri clipboard API (仅桌面端) |
| B | navigator.clipboard + execCommand 降级 |
| C | 不包含剪贴板操作，用户用 Ctrl+C/V |

**你的选择**: _A__
**补充**: ___

## T05-02 | Callout 和 Details 命令
| 选项 | 描述 |
|------|------|
| A | v2.1 实现 (blockquote + CSS class 模拟 callout，原生 details) |
| B | 完整自定义 ProseMirror Node |
| C | 推迟到 v2.2 |

**你的选择**: _A__
**补充**: ___

## T05-03 | 图片插入存储策略
| 选项 | 描述 |
|------|------|
| A | 全部 base64 DataURL |
| B | 存储到 IndexedDB Blob (已有 AssetStore 机制) |
| C | 小图 (<500KB) 用 base64，大图用 Blob |
| D | Tauri 文件系统: 复制到应用数据目录 |

**你的选择**: __D_
**补充**: __注意图片要能导出到平台文章中_

## T05-04 | 链接编辑 UI
Ctrl+K 或工具栏链接按钮触发的链接编辑：

| 选项 | 描述 |
|------|------|
| A | 内联 Popover: 在选中文本附近弹出小面板 (URL输入框 + 确认/取消) |
| B | 底部 Sheet: 从底部滑出链接编辑面板 |
| C | 对话框 Modal: 居中弹出对话框 |

**你的选择**: _A__
**补充**: ___

## T05-05 | 颜色选择器设计
FloatingToolbar 的文字颜色按钮：

| 选项 | 描述 |
|------|------|
| A | 预设 8 色网格 (红/橙/黄/绿/蓝/紫/灰/黑) |
| B | 预设 8 色 + 自定义色 (input type=color) |
| C | 预设 16 色 + 最近使用色 + 自定义色 |

**你的选择**: __C_
**补充**: ___

## T05-06 | 斜杠命令搜索算法
输入 `/` 后的命令过滤：

| 选项 | 描述 |
|------|------|
| A | 前缀匹配 (输入 "co" 匹配 "code block") |
| B | 子串匹配 (输入 "bl" 匹配 "code block") |
| C | 模糊匹配 (输入 "cdblk" 匹配 "code block") |

**你的选择**: _C__
**补充**: ___

## T05-07 | FloatingToolbar 在 Source 模式下
Source (Markdown 源码) 模式下的 FloatingToolbar：

| 选项 | 描述 |
|------|------|
| A | 不显示 (Source 模式是纯文本编辑) |
| B | 显示简化版 (仅插入 Markdown 语法片段) |
| C | 与 Typora 模式一致 |

**你的选择**: __C_
**补充**: ___

## T05-08 | 右键菜单是否需要二级菜单？
例如"插入"展开为"表格/图片/分割线"。

| 选项 | 描述 |
|------|------|
| A | 不需要二级菜单，所有项平铺 (当前 spec 的 15 项设计) |
| B | "插入"和"格式"作为二级子菜单展开 |
| C | 根据上下文动态显示不同项 (无需二级菜单但更智能) |

**你的选择**: _B_
**补充**: __不只是插入和格式，能做二级菜单、三级菜单的应做尽做_

### Task 05 深挖补充题（建议在本组基础题后继续填写）

## T05-09 | 命令系统的注册表形态
为了避免斜杠命令、右键菜单、工具栏、快捷键各自维护一套动作定义，命令系统应如何组织？

| 选项 | 描述 |
|------|------|
| A | 各入口各自实现动作，保持简单 |
| B | 建立统一命令注册表（id / label / icon / enableWhen / handler），各入口只消费元数据 |
| C | B + 命令分层（编辑命令 / 插入命令 / 系统命令 / AI 命令 / 发布命令） |
| D | C + 命令注册表还承担权限、审计、回滚、搜索排序等职责 |

**你的选择**: _D__
**补充**: ___

## T05-10 | 斜杠命令的排序与推荐策略
当命令越来越多时，`/` 菜单的结果应该怎么排序？

| 选项 | 描述 |
|------|------|
| A | 固定分组 + 固定顺序，保证稳定预期 |
| B | 固定分组 + 最近使用优先 |
| C | 上下文相关排序（例如在列表里优先列表命令、在代码块里优先代码命令） |
| D | C + 最近使用 + 用户自定义收藏 / 置顶 |

**你的选择**: __D_
**补充**: ___

## T05-11 | 拖拽、粘贴、插入图片/文件的统一规则
图片和文件不只会通过按钮插入，用户还会拖拽、粘贴截图、粘贴 HTML 片段。默认统一规则应是什么？

| 选项 | 描述 |
|------|------|
| A | 只支持文件选择器插入，其他方式全部禁用 |
| B | 支持拖拽 + 粘贴图片，统一进入同一资源处理管线 |
| C | B + 支持粘贴带图片/链接的 HTML 片段，并尽量提取为结构化内容 |
| D | C + 所有进入方式都先过资源清洗、命名、去重、大小阈值与存储策略判断 |

**你的选择**: _D__
**补充**: ___截图、GIF、SVG、远程图片 URL、带附件的 HTML 粘贴等均按照一般策略判断，优先收归为素材存储于本地相关文件夹中__

## T05-12 | AI / 自动化命令的写权限策略
如果后续命令系统承载 AI 辅助能力，涉及改写正文、批量插入结构、重排内容时，默认应怎么做？

| 选项 | 描述 |
|------|------|
| A | 直接执行，结果写入正文，用户自行撤销 |
| B | 先生成预览 diff，用户确认后写入 |
| C | 小改动可直接执行，大改动必须先 diff + 确认 |
| D | C + 所有 AI/自动化写操作都自动生成版本点并进入审计日志 |

**你的选择**: _D__
**补充**: __参照Notion以及IDE的设计，提供建议后由用户自行抉择是否进行应用。务必可以回版_

## T05-13 | 只读态 / 预览态 / 发布预览态的命令可见性
不同上下文下，命令入口应如何收缩？

| 选项 | 描述 |
|------|------|
| A | 所有入口都隐藏不可用命令，尽量保持界面干净 |
| B | 保留命令，但置灰显示，帮助用户建立心智模型 |
| C | 根据上下文显示不同的命令子集，保持“可做什么”清晰 |
| D | C + 支持通过提示文案解释“为什么此处不可用” |

**你的选择**: _C__
**补充**: ___

---

# 第七组：Task 06 — 本地账户管理 [仅文档存在，代码未落地] (7 题)

> **当前现状**: 0327 已提出完整本地账户方向，但当前代码中尚未看到成熟 `AccountWelcome.vue` / `stores/account.ts` 这一整套主路径。  
> **依赖第一层**: `L1-02` `L1-03` `L1-20` `L1-23` `L1-24` `L1-33` `L1-34` `L1-40`  
> **本组目标**: 先把多账户是否进入本轮、隔离范围、切换语义、入口位置、删除风险彻底说清。

## T06-01 | 多账户数据隔离
| 选项 | 描述 |
|------|------|
| A | 完全隔离 — 文章/分类/设置 按账户分开 (类似 Chrome Profile) |
| B | 共享文章/分类，仅账户资料独立 |
| C | 共享所有数据，多账户仅标识不同作者 |

**你的选择**: __A_
**补充**: ___

## T06-02 | AccountWelcome.vue 定位
| 选项 | 描述 |
|------|------|
| A | 可选页面 — 通过 Hub 头像进入 |
| B | 首次启动引导页 — 首次打开时显示，之后直接进 Hub |
| C | 每次启动都显示 |

**你的选择**: _A__
**补充**: ___

## T06-03 | 头像上传流程
| 选项 | 描述 |
|------|------|
| A | 选择文件 → 自动 Canvas 裁剪 200x200 → 保存 |
| B | 选择文件 → 弹出裁剪对话框 (可拖拽/缩放) → 确认后保存 |
| C | 选择文件 → 直接保存 (不裁剪，CSS 圆角) |

**你的选择**: _B__
**补充**: ___

## T06-04 | 账户删除确认流程
| 选项 | 描述 |
|------|------|
| A | 单次确认对话框 (输入"删除"二字) |
| B | 双重确认: 第一次警告 → 第二次输入账户名称确认 |
| C | 简单确认对话框 (是/否) |

**你的选择**: _B__
**补充**: ___

## T06-05 | 账户切换时的体验
| 选项 | 描述 |
|------|------|
| A | 即时切换，页面保持不变 (仅 Store 数据更新) |
| B | 切换后自动跳转到 Hub 首页 |
| C | 切换后全页面 reload (确保所有状态重置) |

**你的选择**: _C__
**补充**: ___

## T06-06 | "远程同步(即将推出)" 区域
AccountWelcome.vue 中的远程同步占位区：

| 选项 | 描述 |
|------|------|
| A | 显示占位文案 + 禁用的"配置同步"按钮 |
| B | 不显示此区域 (v2.1 不需要) |
| C | 显示 + 点击后跳转到 Settings 的 Sync Tab |

**你的选择**: _A__
**补充**: ___

## T06-07 | Hub Header 头像点击行为
| 选项 | 描述 |
|------|------|
| A | 直接跳转 /account 页面 |
| B | 弹出小气泡菜单: 账户名 + "管理账户" + "设置" + "退出" |
| C | 弹出下拉面板: 头像+名称 + 快捷操作列表 |

**你的选择**: _B__
**补充**: ___

### Task 06 深挖补充题（建议在本组基础题后继续填写）

## T06-08 | 首次没有任何账户时的启动流
如果本地数据库里一个账户都没有，应用首次启动应怎么进入？

| 选项 | 描述 |
|------|------|
| A | 强制先创建账户，创建后才能进入 Hub / Workstation |
| B | 允许匿名临时进入，但核心数据创建前仍提示建立账户 |
| C | 直接进入 Hub，把账户创建当成可选功能 |
| D | 首启给出分流选择：创建正式账户 / 先试用匿名模式 / 导入已有数据 |

**你的选择**: __D_
**补充**: _拒绝匿名模式__

## T06-09 | 本地账户的认证形式
本地账户是否需要密码、PIN 或系统级解锁？

| 选项 | 描述 |
|------|------|
| A | 不需要，本地账户只是 Profile 切换，不做认证 |
| B | 支持本地密码 / PIN，切换高敏账户时需要输入 |
| C | 优先集成系统级认证（Windows Hello / 系统凭据） |
| D | B + C 并存，用户可为不同账户配置不同保护级别 |

**你的选择**: _D__
**补充**: _高危场景，例如删除文章、删除帐号信息、查看敏感设置、导出全量数据等需要高级认证__

## T06-10 | 删除账户后的保留期与恢复策略
账户删除后是否允许恢复？恢复窗口多长？

| 选项 | 描述 |
|------|------|
| A | 立即硬删除，不保留恢复窗口 |
| B | 软删除 7 天，可恢复 |
| C | 软删除直到用户手动彻底清理 |
| D | 删除前强制导出备份，并提供恢复向导 |

**你的选择**: _B__
**补充**: _延续此前一账户一profile设置，一切打包删除同时也可恢复__

## T06-11 | 跨账户共享对象的范围
多账户下，哪些对象允许共享，哪些必须绝对隔离？

| 选项 | 描述 |
|------|------|
| A | 一律不共享，所有对象都按账户隔离 |
| B | 仅共享模板、主题、导出预设等低风险对象 |
| C | B + 允许共享公共资源库（图片/素材） |
| D | 由管理员或设备所有者定义共享对象范围 |

**你的选择**: __A_
**补充**: __目前不进行提供共享操作。远端开发时匹配_

## T06-12 | 切换账户时未保存文档的处置
如果当前账户下有未保存修改，用户又要切换到另一个账户，系统应如何处理？

| 选项 | 描述 |
|------|------|
| A | 阻止切换，必须先保存或放弃 |
| B | 自动创建恢复快照后关闭当前文档，再切换账户 |
| C | 将当前会话挂起，日后切回该账户时恢复现场 |
| D | 支持多窗口 / 多工作区并行，不要求先关闭当前账户会话 |

**你的选择**: __A_
**补充**: __自动保存失败时禁止切换_

---

# 第八组：Task 07 — Settings 全量实装 [已有部分实现，需补全] (7 题)

> **当前现状**: 当前 `SettingsView.vue` 实际只有 7 个 Tab，不是 0327 文档中的完整 10 Tab；`settings.ts` 也缺少部分目标字段。  
> **依赖第一层**: `L1-03` `L1-20` `L1-23` `L1-27` `L1-36` `L1-37` `L1-38`  
> **本组目标**: 先确认哪些设置是 v2.1 真正 scope，哪些只是预留，不让 Settings 再次成为“文档比代码大很多”的漂移源。

## T07-01 | AI Tab Provider 范围
| 选项 | 描述 |
|------|------|
| A | 仅 UI 占位，标注"即将推出" |
| B | 支持 OpenAI-compatible API (自定义 endpoint) |
| C | 支持 OpenAI + Anthropic + Ollama |
| D | B + 用于写作辅助 (润色/续写/翻译) |

**你的选择**: __A_
**补充**: ___

## T07-02 | Sync Tab 实现
| 选项 | 描述 |
|------|------|
| A | 仅 UI 占位 |
| B | 实现 WebDAV 同步 |
| C | 本地文件系统导出/导入 (最简方案) |
| D | Git 同步 (文章目录作为 Git 仓库) |

**你的选择**: _D__
**补充**: ___

## T07-03 | Data Tab 功能
| 选项 | 描述 |
|------|------|
| A | 导出全量 JSON + 导入 + 清除数据 + 存储统计 |
| B | A + 定期自动备份到本地文件夹 |
| C | A + IndexedDB 数据库浏览器 (开发者工具) |

**你的选择**: __B+C_
**补充**: ___

## T07-04 | Advanced Tab 内容
高级设置应包含什么？

| 选项 | 描述 |
|------|------|
| A | 开发者模式 + 日志级别 + 缓存清理 + 数据库重置 |
| B | A + 自定义 CSS 注入 + 自定义 JS 注入 |
| C | A + 性能监控面板 + 网络诊断 |

**你的选择**: _B+C__
**补充**: ___

## T07-05 | Settings 页面是否需要搜索功能？
10 个 Tab 可能有 50+ 设置项。

| 选项 | 描述 |
|------|------|
| A | 不需要，Tab 导航足够 |
| B | 全局搜索栏 (过滤设置项并高亮) |
| C | 仅在 Shortcuts Tab 中有搜索 |

**你的选择**: _B__
**补充**: ___

## T07-06 | 设置变更的持久化时机
| 选项 | 描述 |
|------|------|
| A | 即时持久化 (每次变更立即写入 localStorage) |
| B | 防抖持久化 (变更后 500ms 自动保存) |
| C | 手动保存按钮 |

**你的选择**: _A__
**补充**: ___

## T07-07 | 设置导入/导出
| 选项 | 描述 |
|------|------|
| A | 支持导出/导入设置 JSON (跨设备迁移) |
| B | 不需要，设置随账户数据一起导出 |
| C | A + 支持分享设置链接 (base64 encoded) |

**你的选择**: _B__
**补充**: ___

### Task 07 深挖补充题（建议在本组基础题后继续填写）

## T07-08 | 设置作用域
同一份设置到底属于设备、账户、工作区、还是文档？

| 选项 | 描述 |
|------|------|
| A | 几乎全部是设备级设置 |
| B | 编辑器/界面类设置是设备级，账户相关与同步类设置是账户级 |
| C | 大多数设置按账户隔离，设备只保留少量系统偏好 |
| D | 建立多层作用域：设备级 / 账户级 / 工作区级 / 文档级，并定义覆盖顺序 |

**你的选择**: _C__
**补充**: ___

## T07-09 | 危险设置的保护等级
对于“数据库重置、清缓存、实验功能、自定义 CSS/JS、同步重连”等危险设置，需要多强保护？

| 选项 | 描述 |
|------|------|
| A | 仅弹一次确认框 |
| B | 确认框 + 输入关键字确认 |
| C | 必须先开启开发者模式 / 高级模式才能看到和修改 |
| D | C + 自动备份 + 审计记录 + 风险提示文案分级 |

**你的选择**: __C_
**补充**: ___

## T07-10 | 设置的版本化与迁移预览
设置结构升级、导入外部设置、从旧版本迁移时，是否需要预览和回滚？

| 选项 | 描述 |
|------|------|
| A | 不需要，直接覆盖即可 |
| B | 需要 schema version 与自动迁移 |
| C | B + 导入前显示差异预览（哪些值将变化） |
| D | C + 迁移或导入前自动生成回滚点，可一键撤回 |

**你的选择**: _D__
**补充**: ___如果某些设置项在新版本已废弃，进行提示____

## T07-11 | Settings 搜索结果的定位方式
如果加入 Settings 搜索，用户搜索到结果后应该如何被带到目标项？

| 选项 | 描述 |
|------|------|
| A | 仅做过滤，用户自己滚动查找 |
| B | 自动切到对应 Tab，并高亮匹配项 |
| C | B + 支持结果列表、快捷跳转、上下一个匹配项 |
| D | C + 为每个设置项提供稳定锚点/深链接，便于后续文档引用与跳转 |

**你的选择**: _C__
**补充**: _支持同义词、快捷键名、关键词__

## T07-12 | 设置重置的粒度
用户需要怎样的“恢复默认”能力？

| 选项 | 描述 |
|------|------|
| A | 只提供“全部恢复默认” |
| B | 提供“按 Tab 恢复默认” + “全部恢复默认” |
| C | 提供“单项恢复默认” + “按 Tab 恢复默认” + “全部恢复默认” |
| D | C + 恢复前展示受影响项列表，并允许部分勾选重置 |

**你的选择**: _C__
**补充**: __恢复默认后立即持久化、同时生成回滚点_

---

# 第九组：Task 08 — 数据洞察 [已有部分实现，需补全] (6 题)

> **当前现状**: 当前已存在部分图表与统计基础，但距离 0327 所定义的更丰富洞察面还有明显差距。  
> **依赖第一层**: `L1-01` `L1-03` `L1-35` `L1-36` `L1-40`  
> **本组目标**: 定义数据洞察到底是“美观展示层”还是“真正能帮助创作决策的分析层”，并决定深度。

## T08-01 | 新增图表范围
| 选项 | 描述 |
|------|------|
| A | 全部 6 个新图表 |
| B | 4 个核心 (WritingTimeline, ProductivityInsights, WordDistribution, RecentActivity) |
| C | 3 个 (WritingTimeline, ProductivityInsights, RecentActivity) |

**你的选择**: _A__
**补充**: ___

## T08-02 | ExportFrequency 数据源
| 选项 | 描述 |
|------|------|
| A | activity_logs 已记录导出事件，直接用 |
| B | 需要新增导出日志记录 |
| C | 砍掉 ExportFrequency |

**你的选择**: _B__
**补充**: ___

## T08-03 | 图表实现技术
| 选项 | 描述 |
|------|------|
| A | 纯 SVG/CSS (零依赖，与现有 3 个图表一致) |
| B | 允许用 Chart.js (~60KB gzipped) |
| C | 允许用 lightweight 图表库如 unovis 或 frappe-charts |

**你的选择**: _C__
**补充**: ___

## T08-04 | 图表交互需求
| 选项 | 描述 |
|------|------|
| A | 纯展示，无交互 |
| B | Hover 显示 Tooltip 数值 |
| C | Hover + 点击跳转到相关文章/时间段 |

**你的选择**: _C__
**补充**: ___

## T08-05 | 数据洞察区时间范围
| 选项 | 描述 |
|------|------|
| A | 固定时间范围 (热力图 52 周，趋势 30 天等) |
| B | 可切换时间范围 (最近 7 天 / 30 天 / 90 天 / 全部) |
| C | A 为默认，部分图表可展开查看更多 |

**你的选择**: _C__
**补充**: ___

## T08-06 | 图表数据是否可导出？
| 选项 | 描述 |
|------|------|
| A | 不需要 |
| B | 每个图表有"导出 CSV"按钮 |
| C | 统一"导出洞察报告"按钮 (包含所有图表数据) |

**你的选择**: __B_
**补充**: ___

### Task 08 深挖补充题（建议在本组基础题后继续填写）

## T08-07 | 统计口径的统一标准
字数、创作时长、活跃天数、导出次数等指标是否需要统一口径定义？

| 选项 | 描述 |
|------|------|
| A | 不需要，只要大致可用即可 |
| B | 核心指标需要统一定义，其余可近似统计 |
| C | 所有图表指标都必须有正式定义，并写入 PRD / Spec |
| D | C + 对每个指标定义“来源表 / 计算方式 / 边界样本 / 异常值处理” |

**你的选择**: _D__
**补充**: __字数按照纯文本，不算标题，不算代码块，不算公式__

## T08-08 | 数据新鲜度与刷新频率
洞察面板的数据刷新应该有多实时？

| 选项 | 描述 |
|------|------|
| A | 进入页面时重新计算一次即可 |
| B | 编辑行为后按防抖策略局部刷新 |
| C | 关键指标实时刷新，重计算图表后台异步刷新 |
| D | 建立多层刷新策略：实时指标、会话级指标、日级指标分别更新 |

**你的选择**: __D_
**补充**: _不要让所有计算同时进行，占据大量资源导致卡顿__

## T08-09 | 异常值与脏数据的展示策略
如果活动日志缺失、导出日志不完整、历史数据迁移有空洞，图表应如何表现？

| 选项 | 描述 |
|------|------|
| A | 忽略异常值，尽量画图 |
| B | 缺数据就显示空值/断点，不做解释 |
| C | 图表允许缺口，同时提供“数据不完整”提示 |
| D | C + 提供重建统计 / 重新计算 / 查看异常来源的入口 |

**你的选择**: _D__
**补充**: _我注重统计可信__

## T08-10 | 洞察面板的行动化程度
数据洞察是纯观察面板，还是应该能直接驱动下一步动作？

| 选项 | 描述 |
|------|------|
| A | 纯展示即可，不要变成交互控制台 |
| B | 少量行动入口，例如“查看相关文档”“打开该时间段文章” |
| C | 洞察卡片要能直接跳转、筛选、生成任务或导出报告 |
| D | C + 可以基于洞察触发建议动作（整理草稿、补写目标、回顾低产期） |

**你的选择**: _D__
**补充**: _允许数据洞察与 Hub / Workstation 强联动__

## T08-11 | 大数据量下的图表降级策略
当文章、日志、版本、导出记录数量变大时，图表如何降级？

| 选项 | 描述 |
|------|------|
| A | 不做特别降级，能算就算 |
| B | 按时间窗口聚合，减少点数和柱数 |
| C | 后台预计算缓存，前端只取聚合结果 |
| D | C + 对每类图表定义最大采样量、聚合规则、超限提示 |

**你的选择**: __D_
**补充**: __基于资源占用进行降级_

---

# 第十组：Task 09 — UI 打磨 [已有部分实现，需补全] (8 题)

> **当前现状**: 视觉打磨不是从零开始，而是在现有 Ethereal Constructivism 基础上补足暗色、层级、空状态、动画、预览保真与溢出治理。  
> **依赖第一层**: `L1-12` `L1-31` `L1-35` `L1-36` `L1-39` `L1-40`  
> **本组目标**: 只做“顺着现有风格加深”和“补齐全局一致性”，不做风格改朝换代。

## T09-01 | 暗色模式完成度
| 选项 | 描述 |
|------|------|
| A | 所有 14+ 组件 100% 暗色适配 |
| B | 核心路径 (Hub + Workstation + Editor + Settings) 优先 |
| C | 仅 CSS 变量框架 + 核心组件 |

**你的选择**: _A__
**补充**: ___

## T09-02 | 页面切换动画
| 选项 | 描述 |
|------|------|
| A | fade + slide-left + slide-right 全部实现 |
| B | 仅 fade 过渡 |
| C | 不做页面过渡动画 |

**你的选择**: _A__
**补充**: ___

## T09-03 | 面板折叠/展开动画
左栏、右栏面板的折叠动画：

| 选项 | 描述 |
|------|------|
| A | 宽度 transition (250ms ease) |
| B | 宽度 transition + 内容淡出 |
| C | 瞬间切换，无动画 |

**你的选择**: _A__
**补充**: ___

## T09-04 | Loading/空状态统一设计
| 选项 | 描述 |
|------|------|
| A | 每个组件自定义空状态 (icon + 文案) |
| B | 统一空状态组件 (可配置 icon + title + description + action) |
| C | 骨架屏 Loading + 统一空状态组件 |

**你的选择**: _A__
**补充**: ___

## T09-05 | 全局 Z-index 层级
| 选项 | 描述 |
|------|------|
| A | 不定义标准，各组件自行管理 |
| B | 定义统一层级: Modal=300, FloatingToolbar=200, ContextMenu=250, Toast=400, Overlay=500 |
| C | 使用 CSS 变量: `--z-toolbar`, `--z-menu`, `--z-modal`, `--z-toast` |

**你的选择**: _B__
**补充**: ___

## T09-06 | 滚动条样式
| 选项 | 描述 |
|------|------|
| A | 使用默认浏览器滚动条 |
| B | 自定义细滚动条 (6px 宽，hover 时 8px) |
| C | macOS 风格 overlay 滚动条 |

**你的选择**: _B__
**补充**: ___

## T09-07 | 焦点指示器 (Focus Ring) 样式
键盘导航时的焦点轮廓：

| 选项 | 描述 |
|------|------|
| A | 默认浏览器 outline |
| B | 品牌红 (#D32F2F) 2px outline + 2px offset |
| C | box-shadow: 0 0 0 2px rgba(211,47,47,0.3) |

**你的选择**: _B__
**补充**: ___

## T09-08 | 微信公众号预览的视觉保真度
Stage 面板中的手机框预览：

| 选项 | 描述 |
|------|------|
| A | 仅渲染内容，不模拟手机框 |
| B | 模拟 iPhone 框 + 渲染内容 |
| C | 多设备预览 (iPhone/Android/iPad 切换) |

**你的选择**: __C_
**补充**: _参照https://md.doocs.org/__

### Task 09 深挖补充题（建议在本组基础题后继续填写）

## T09-09 | 动画系统的全局开关与分级
除了 reduced motion，动画系统是否需要更细的全局分级？

| 选项 | 描述 |
|------|------|
| A | 不需要，只有开/关 |
| B | 提供“完整动画 / 精简动画 / 无动画”三级 |
| C | 动画按类别分级（页面切换、悬停反馈、编辑显隐、图表动效） |
| D | C + 允许按设备性能或环境自动降级 |

**你的选择**: _D__
**补充**: _一旦性能与环境不足以满足需求则统一降级__

## T09-10 | 全局密度与留白策略
为了保持纸张式气质但兼顾信息量，整体 UI 的密度应该如何控制？

| 选项 | 描述 |
|------|------|
| A | 始终保持宽松留白，优先氛围感 |
| B | Hub 宽松、Workstation 中等、Settings 更紧凑 |
| C | 支持全局“舒适 / 标准 / 紧凑”密度档位 |
| D | C + 某些页面锁定最低密度，防止破坏纸张式设计 |

**你的选择**: _B__
**补充**: _所有页面都不可以像后台表格系统，一定要富有美感与品味__

## T09-11 | 骨架屏与真实空状态的区分规则
你希望系统如何区分“正在加载”与“确实没有数据”？

| 选项 | 描述 |
|------|------|
| A | 尽量少用骨架屏，直接闪现真实内容或空状态 |
| B | 短加载用骨架屏，长加载再显示说明文案 |
| C | 所有异步卡片都必须先骨架屏，再判断空状态 |
| D | 为不同数据源定义统一规则：本地瞬时加载、远端慢加载、统计重算三者分别处理 |

**你的选择**: _A__
**补充**: __直接说明即可_

## T09-12 | 触控与键盘双通道的统一体验
虽然主要是桌面写作工具，但如果存在触控屏或仅键盘用户，UI 打磨需要做到什么程度？

| 选项 | 描述 |
|------|------|
| A | 只优化鼠标主路径即可 |
| B | 鼠标 + 键盘都要完整可用，触控不重点优化 |
| C | 鼠标 + 键盘 + 触控三者都要可用，但允许体验层次不同 |
| D | 对所有核心控件明确最小点击区、键盘焦点路径、触控手势替代策略 |

**你的选择**: _A__
**补充**: _不做触控优化__

## T09-13 | 全局视觉一致性审查的严格程度
后续开发过程中，视觉一致性应该被当作什么等级的验收项？

| 选项 | 描述 |
|------|------|
| A | 只要主功能可用即可，细微不一致可以接受 |
| B | 核心路径必须一致，边缘页面稍后再补 |
| C | 颜色、圆角、阴影、边框、层级、交互动效都要有统一规范 |
| D | C + 每个新增组件都必须映射到已有设计语汇，禁止“似是而非的新风格” |

**你的选择**: _D__
**补充**: _必须保持和谐统一，任何突兀的东西，例如emoji都会让我极其反感__

---

# 第十一组：跨任务依赖与高级需求 [跨层强依赖] (8 题)

> **当前现状**: 这部分问题往往不是某个组件自己的问题，而是内容模型、命令系统、导出链路、版本恢复、异常恢复之间的跨层契约。  
> **依赖第一层**: 默认依赖第一层多数问题，尤其是 `L1-05` ~ `L1-08`、`L1-17` ~ `L1-22`、`L1-27` ~ `L1-34`。  
> **本组目标**: 把跨任务联动的“真难点”提前锁死，避免每个 Task 各自实现后互相打架。

## X-01 | Typora 模式 ↔ FloatingToolbar 联动
| 选项 | 描述 |
|------|------|
| A | 两种模式下 FloatingToolbar 行为完全一致 |
| B | Typora 模式减少按钮 (去掉标题等块级操作) |
| C | Source 模式不显示 FloatingToolbar |

**你的选择**: __A_
**补充**: ___

## X-02 | KaTeX/Mermaid ↔ Typora 模式
| 选项 | 描述 |
|------|------|
| A | Typora 风格: 光标在公式/图表时显示源码，离开时渲染 |
| B | 始终渲染，不做 Typora 切换 |
| C | 公式做 Typora 切换，图表始终渲染 (复杂度折中) |

**你的选择**: _B__
**补充**: ___

## X-03 | 所有 Task 的错误边界策略
编辑器扩展崩溃时如何处理？

| 选项 | 描述 |
|------|------|
| A | Vue errorHandler 全局捕获 + Toast 通知用户 |
| B | A + 自动禁用出错的扩展并恢复编辑器 |
| C | 不做特殊处理，依赖浏览器默认行为 |

**你的选择**: _A__
**补充**: ___

## X-04 | IndexedDB 版本升级策略
v2.1 可能新增表或字段 (accounts 多账户等)。

| 选项 | 描述 |
|------|------|
| A | Dexie 的 `.version()` 升级机制自动处理 |
| B | 首次启动时跑迁移脚本 + 进度提示 |
| C | v2.1 全新安装，不兼容 v2.0 数据 |

**你的选择**: __B_
**补充**: ___

## X-05 | 性能基准要求
| 选项 | 描述 |
|------|------|
| A | 不设具体性能指标，能用就行 |
| B | 首屏加载 <3s，编辑器输入延迟 <50ms，Hub 渲染 <1s |
| C | B + Lighthouse Performance Score >80 |

**你的选择**: _C__
**补充**: _性能必须极致优化__

## X-06 | 文章存储格式
TipTap 支持 HTML 和 JSON 两种格式。这影响 Typora↔Source 转换和导出。

| 选项 | 描述 |
|------|------|
| A | 内部用 TipTap JSON 存储，导出时转 HTML/Markdown |
| B | 内部用 HTML 存储 (当前实现) |
| C | 双格式: JSON 主存储 + Markdown 文本副本 (方便搜索/版本对比) |

**你的选择**: __B_
**补充**: ___

## X-07 | 自动保存与版本历史
| 选项 | 描述 |
|------|------|
| A | 自动保存覆盖当前版本，无历史 |
| B | 自动保存 + 保留最近 10 个版本 (可回滚) |
| C | 自动保存 + 无限版本历史 (diff 存储节省空间) |

**你的选择**: _C__
**补充**: ___

## X-08 | v2.1 预期完成时间
| 选项 | 描述 |
|------|------|
| A | 2 周内 (需要大幅砍 scope) |
| B | 1 个月 (合理节奏) |
| C | 不设时间限制，做到满意为止 |

**你的选择**: __C_
**补充**: __不设工期，我是AI协助下开发，开发完为止_

### 第十一组深挖补充题（建议在基础题后继续填写）

## X-09 | 内容版本与资源版本的绑定方式
当文章正文、图片资源、Mermaid 渲染结果、导出缓存同时存在时，版本历史应绑定到什么层级？

| 选项 | 描述 |
|------|------|
| A | 只给正文做版本，资源单独管理，不绑定版本 |
| B | 正文版本保存资源引用关系，但不冻结资源内容 |
| C | 正文版本 + 资源引用快照一起保存，确保历史可回放 |
| D | 建立“文档版本包”概念，正文、资源引用、导出参数、评论锚点统一进版本语义 |

**你的选择**: _D__
**补充**: __回滚是一致回到当时状态的_

## X-10 | AI / 命令系统批量操作的留痕与撤回
如果未来支持批量格式化、整篇重排、批量替换、AI 改写，系统至少要做到什么程度的可追溯？

| 选项 | 描述 |
|------|------|
| A | 普通撤销即可，不做额外留痕 |
| B | 记录命令名称和时间 |
| C | 记录命令名称 + 影响范围摘要 + 自动版本点 |
| D | C + 差异摘要、操作者、确认记录、可单独回滚该次批量操作 |

**你的选择**: _C__
**补充**: _任何自动化操作必须强制生成版本点__

## X-11 | 数据库损坏或升级失败时的灾难恢复
如果 IndexedDB 升级失败、数据损坏、版本表异常、迁移中断，产品默认应如何自救？

| 选项 | 描述 |
|------|------|
| A | 只提示错误，让用户自行清理数据 |
| B | 启动前做基本完整性检查，异常时进入安全模式 |
| C | B + 尝试自动恢复最近备份 / 快照 |
| D | C + 提供恢复向导（导出诊断信息、选择备份、局部恢复、只读抢救模式） |

**你的选择**: _C__
**补充**: __决不能接受文章消失，文档内容是底线_

## X-12 | 最终验收矩阵的真值来源
后续进入 PRD / Spec / 开发后，最终验收矩阵应以什么作为“通过”的铁标准？

| 选项 | 描述 |
|------|------|
| A | 只要人工主观觉得好用就算通过 |
| B | 关键主路径手测通过 + `build` / `typecheck` 通过 |
| C | B + 每个 Task 都要有可复现的验收步骤与结果记录 |
| D | C + 数据迁移、恢复、导出保真、异常处理、权限/审计都必须有证据化验收 |

**你的选择**: _D__
**补充**: __人工主观+机器测试。机器测试在先，所有验收必须附截图/日志/导出结果/对比样本_

---

# 第十二组：超出原 spec 的潜在需求（补充题）

> **当前现状**: 这些题是从现有代码、0327 文档漂移、市场最佳实践中反推出来的扩展点；不一定要本轮全部做，但必须先问清是否要保留为正式方向。  
> **填写原则**: 请明确区分“本轮必须实现 / 本轮只留接口 / 明确后移 / 永久不做”。  
> **注意**: 这里的很多题会直接影响后续 PRD 和数据模型，不要因为“现在还没做”就简单跳过。

以下是我在分析 spec 时发现的**原 spec 完全没有提到但可能需要的功能**。请标记是否需要在 v2.1 中实现。

## S-01 | 文章模板系统
当前 TemplateMarketCard 存在但模板功能不明确。

- [x] v2.1 实现: 用户可创建/保存/使用文章模板
- [ ] v2.2 推迟
- [ ] 不需要
- **补充**: ___

## S-02 | 拖拽排序 (文件管理器)
FileManager 中文章和分类的排序方式。

- [x] 支持拖拽排序
- [ ] 仅支持按属性排序 (日期/名称/字数)
- [ ] 不需要
- **补充**: ___

## S-03 | 文章搜索 (全局)
Hub 或 Workstation 中搜索文章内容。

- [x] v2.1 实现全文搜索 (IndexedDB + 分词)
- [ ] v2.1 仅标题搜索
- [ ] 推迟
- **补充**: ___

## S-04 | 键盘快捷键 Ctrl+N 新建文章
Hub 中快速新建。

- [x] 需要 (Hub 页面全局快捷键)
- [ ] 不需要 (用 QuickActionFab 或 Header 按钮)
- **补充**: ___

## S-05 | 文章标签系统
articles 数据模型有 `tags` 字段，但 UI 中标签编辑/过滤功能不明确。

- [x] v2.1 实现标签编辑 + 按标签过滤
- [ ] v2.1 仅在文章元数据中显示标签
- [ ] 推迟
- **补充**: ___

## S-06 | 字数统计的作用域
EditorStatusBar 的字数统计：

- [ ] 仅统计编辑器正文 (body)
- [ ] 正文 + 标题
- [x] 正文 + 标题 + 选中文本单独统计
- **补充**: ___

## S-07 | Tauri 原生菜单栏
是否使用 Tauri 的原生窗口菜单 (File/Edit/View 等)？

- [ ] 需要 — 完整原生菜单
- [ ] 需要 — 最简菜单 (File: 新建/打开/保存, Edit: 撤销/重做, View: 缩放)
- [x] 不需要 — 纯 Web UI
- **补充**: ___

## S-08 | 文章导入
是否支持从外部导入 Markdown 文件？

- [ ] v2.1 支持: 拖拽或文件选择器导入 .md 文件
- [x] v2.1 支持: 导入 .md + .docx
- [ ] 推迟
- **补充**: ___

## S-09 | 多标签编辑 (TabBar)
当前 TabBar 支持多标签。是否需要增强？

- [ ] 保持现有功能即可
- [x] 增加: 拖拽排序 + 中键关闭 + 右键菜单 (关闭其他/关闭所有)
- [ ] 推迟增强
- **补充**: ___

## S-10 | 打印功能
| 选项 | 描述 |
|------|------|
| A | 不需要 |
| B | Ctrl+P 调用浏览器打印 |
| C | 自定义打印样式 (隐藏 UI，仅打印文章内容) |

**你的选择**: __A_
**补充**: ___

### 第十二组深挖补充题（建议在本组基础题后继续填写）

## S-11 | 模板系统的版本化能力
如果做文章模板，模板本身是否需要版本和升级机制？

| 选项 | 描述 |
|------|------|
| A | 不需要，模板只是一次性内容片段 |
| B | 模板可编辑，但不保留历史 |
| C | 模板应保留版本历史，便于长期维护 |
| D | C + 允许“模板升级不反向污染已基于模板创建的文章” |

**你的选择**: __D_
**补充**: _模板更新后，既有文章不应当收到升级建议，坚决制止污染已基于模板创建的文章__（建议写明：模板更新后，既有文章要不要收到升级建议）

## S-12 | 全局搜索的索引范围
如果后续做全局搜索，你希望搜索索引覆盖哪些对象？

| 选项 | 描述 |
|------|------|
| A | 仅文章标题 |
| B | 标题 + 正文纯文本 |
| C | 标题 + 正文 + 标签 + 分类 + 摘要 |
| D | C + 评论、模板、导出记录、版本标题、资源说明等辅助对象 |

**你的选择**: __D_
**补充**: _搜索结果应当区分注明各个部分的差别，提供给用户选择使用__

## S-13 | 外部导入的冲突策略

导入 Markdown / docx / 其他文件时，如果标题重名、标签冲突、资源重名、分类缺失，应如何处理？

| 选项 | 描述 |
|------|------|
| A | 直接导入并自动重命名冲突项 |
| B | 导入前显示冲突摘要，让用户决定 |
| C | 提供导入向导：重命名、合并、跳过、放入新分类 |
| D | C + 支持“导入规则模板”，方便以后批量导入复用 |

**你的选择**: _D__
**补充**: ___

## S-14 | 标签系统的数据模型深度

如果做标签系统，标签应只是字符串，还是需要正式对象模型？

| 选项 | 描述 |
|------|------|
| A | 字符串即可，轻量够用 |
| B | 标签对象化（id、name、color） |
| C | B + 标签分组 / 层级 / 别名 |
| D | C + 标签与模板、搜索、过滤、洞察联动，成为正式信息架构的一部分 |

**你的选择**: __D_
**补充**: __标签和分类的职责应当严格区分_

## S-15 | 多标签编辑的会话恢复深度
如果继续增强多标签编辑，当应用崩溃、重启、切账户、升级后，你希望恢复到什么程度？

| 选项 | 描述 |
|------|------|
| A | 只恢复最后打开的单篇文档 |
| B | 恢复标签列表，但不保证每个标签的滚动 / 光标状态 |
| C | 恢复标签列表 + 当前激活标签 + 基本编辑现场 |
| D | C + 尽量恢复每个标签的光标、滚动、折叠、预览状态，并区分账户/工作区 |

**你的选择**: __D_
**补充**: __恢复延迟必须要优于microsoft words以及office类软件，降级不可以触及正文内容_

---

# 填写完成后

请将填写完成的文件保存，然后告诉我。我将：
1. 先输出 **第一层结论总表**：哪些边界已锁死、哪些仍有冲突、哪些必须补问
2. 再把 **第二层问卷答案** 映射到正式 PRD 结构：目标 / 范围 / 非范围 / 用户场景 / 信息架构 / 交互 / 数据模型 / 风险 / 验收
3. 基于你的答案，重写/更新 `prompts/0327/` 的相关 spec 文档，或在 `prompts/0408/` 生成新版 PRD / Spec
4. 为每个 Task 生成精确实施清单、依赖清单、风险清单、验收矩阵
5. 在你审阅并批准后，再正式进入开发实施阶段
