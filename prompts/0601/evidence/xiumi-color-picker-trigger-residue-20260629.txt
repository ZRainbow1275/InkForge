Xiumi Color Picker Trigger Residue - 2026-06-29

Scope:
- Local static publishability protection for copied or partially cleaned Xiumi v5 paper-editor
  hidden plain/gradient color picker trigger nodes.
- No platform action, no phone preview, no sync, no upload, no publish.

Live DOM source:
- CloakBrowser read the live Xiumi v5 paper editor DOM at the logged-in paper editor surface.
- The sampled DOM exposed source-specific body-level editor trigger ids:
  - tnColorPickerTrigger
  - tnGradientColorPickerTrigger
- The live nodes are hidden editor triggers for opening plain and gradient color picker popups.
  They are not article body DOM, style semantics, or platform-safe color values.

Implemented:
- Added `Xiumi color picker trigger residue` to `MARKET_EDITOR_RESIDUE_RULES`.
- The rule matches class/id attributes containing:
  - tnColorPickerTrigger
  - tnGradientColorPickerTrigger
- The rule is ordered adjacent to color-palette panel handling and before color-selector controls
  so body-level picker triggers keep an exact diagnostic.
- Added three-platform regression coverage for WeChat, Xiaohongshu, and Zhihu.

Negative scope:
- The rule does not block ordinary prose or article markup containing color, picker, trigger,
  gradient, palette, popup, hidden, or theme wording by itself.
- The reduced fixture does not rely on color-palette panels, color-selector controls, Angular
  runtime classes, hosted media, sidebar controls, or meta panels.

Verification:
- Red:
  pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "color picker triggers" --reporter=default
  Result: failed as expected before implementation, 1 failed / 312 skipped / 313 total, because
  no wechat-market-editor-residue issue was emitted.
- Green:
  pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "color picker triggers|color palette panels|color selector controls" --reporter=default
  Result: passed after implementation, 1 file / 3 selected tests passed / 310 skipped / 313 total.
- Full validation:
  pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts --reporter=default --test-timeout=90000
  Result: passed, 1 file / 313 tests.
  pnpm -C inkforge exec vitest run src/services/export --reporter=default --maxWorkers=1 --no-file-parallelism --test-timeout=90000
  Result: passed, 36 files / 1290 tests.
  pnpm -C inkforge exec eslint src/services/export/quality-detector.ts src/services/export/platform-export-rendering.test.ts --quiet
  Result: passed.
  pnpm -C inkforge exec vue-tsc --noEmit --pretty false
  Result: passed.
  NODE_OPTIONS=--max-old-space-size=4096 pnpm -C inkforge build
  Result: passed, 4653 modules transformed in 40.26s.
- Release preflight:
  pnpm -C inkforge style-proof:release-preflight --json
  Result: exited 1 as expected with canClaimComplete=false, status=blocked-by-external,
  blockerKinds=[phone-preview, external-dependency, unsafe-to-automate, mutating-platform],
  blockerCount=4, combinedIssueCount=11, cannotClaimSteps=29, phoneOpenSteps=4,
  externalDependencyOpenSteps=14, unsafeToAutomateOpenSteps=13, mutatingOpenSteps=13,
  externalHandoffRows=18, safeExternalRows=0, actionableLocalRows=0, nextRowRefs=5,
  uniqueNextRows=3.

Boundary:
- This proves local static publishability protection only.
- It does not prove WeChat PC paste, ordinary Ctrl+V, phone preview, mobile SMIL/click,
  mobile Dark Mode, cover thumbnail acceptance, credentialed sync, scheduled send, platform
  preview, public article rendering, public-host acceptance, XHS/Zhihu account upload, or publish
  success.
