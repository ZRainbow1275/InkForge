135 SVG Material Category Helper Asset Residue - 2026-06-27

Scope:
- Static publishability protection for copied or partially cleaned 135 SVG editor material-category
  helper asset pairs.
- Platforms covered by the local quality gate: WeChat, Xiaohongshu, Zhihu.
- No platform action, no phone preview, no sync, no upload, and no publish.

Observed source pattern:
- Live 135 SVG editor material categories can retain paired relative helper resources:
  `img/hot.74ee6ac4.png`
  `img/icon-up2.e0ef1973.png`

Implemented:
- Detector label: `135 SVG material category helper asset residue`
- Trigger scope: nearby paired source-specific relative `img/hot` and `img/icon-up2` PNG resource
  paths.
- Non-goals: single generic PNG image paths, generic hot/fold/up/category wording, and ordinary
  article prose.

Regression coverage:
- Added a reduced fixture containing only the paired category helper asset paths and readable
  article text.
- Verified WeChat, Xiaohongshu, and Zhihu all emit the platform market-editor-residue issue.
- Verified the reduced fixture does not rely on `135 SVG material category wrapper residue`,
  `135 SVG material filter control residue`, `135 SVG material preview asset residue`,
  `135 SVG material action asset residue`, or `135 SVG sidebar icon asset residue`.

Verification:
- Red: `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "category helper asset" --reporter=default`
  Result: failed with 1 selected failing test before the detector rule existed.
- Green: `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "category helper asset" --reporter=default`
  Result: passed with 1 selected test and 257 skipped tests after the detector update.
- `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts --reporter=default`
  Result: passed with 1 file and 258 tests.
- `pnpm -C inkforge exec vitest run src/services/export --reporter=default --maxWorkers=1 --no-file-parallelism`
  Result: passed with 36 files and 1235 tests.
- `pnpm -C inkforge exec eslint src/services/export/quality-detector.ts src/services/export/platform-export-rendering.test.ts --quiet`
  Result: passed.
- `pnpm -C inkforge exec vue-tsc --noEmit --pretty false`
  Result: passed.
- `NODE_OPTIONS=--max-old-space-size=4096 pnpm -C inkforge build`
  Result: passed with 4653 modules transformed and Vite build completed in 55.26s.
- `pnpm -C inkforge style-proof:release-preflight --json`
  Result: exited 1 as expected with `status=blocked-by-external`, `canClaimComplete=false`,
  `externalHandoffRows=18`, `safeExternalRows=0`, `actionableLocalRows=0`, `nextRowRefs=5`,
  and `uniqueNextRows=3`.

Boundary:
- This proves local static publishability protection only.
- It does not prove WeChat paste, phone preview, mobile interaction, mobile Dark Mode, cover
  thumbnail acceptance, credentialed sync, scheduled send, platform preview, public article
  rendering, public-host acceptance, XHS/Zhihu account upload, or publish success.
