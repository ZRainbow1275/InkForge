Xiumi Group Box Wrapper Residue - 2026-06-25

Scope:
- Local static publishability protection for Xiumi applied-editor group box wrapper residue.
- No platform action, no browser profile artifact, no phone preview, no sync, no upload, no
  scheduled send, and no publish.

Implemented:
- Re-labeled the remaining group-wrapper detector as `Xiumi group box wrapper residue`.
- The detector matches class/id attributes containing:
  - `tn-group-box-wrapper`
  - `tn-group-fixed-box`
- The previous `Xiumi SVG gallery state wrapper residue` bucket is no longer required for the
  concrete Xiumi `tn-*` gallery state-wrapper markers currently covered by executable tests.

Reduced Regression:
- Fixture contains only `tn-group-box-wrapper` plus readable text.
- The reduced fixture intentionally omits broader `tn-*`, `ng-*`, `opera-*`,
  `contenteditable`, hosted-media, SVG content-layer, `raw-image`,
  `tn-image-presenter`, `tn-content-overlap`, `tn-image-inst-wrapper`,
  `tn-overflow-hidden`, `tn-page-vessel`, `tn-group-sortable-box`, `tn-sortable-pin`,
  `tn-quick-input*`, `tn-state-*`, `tn-on-*`, `tn-in-cell-state-active`, `ui-slider`,
  `ui-sortable`, `op-loader`, `touch-action`, `user-select`, `pointer-events`,
  `visibility:hidden`, and `foreignObject` markers.

Verification:
- TDD red:
  `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "group box wrappers" --reporter=default`
  failed before implementation because the issue message still contained
  `Xiumi SVG gallery state wrapper residue`.
- Focused green:
  `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "group box wrappers" --reporter=default`
  PASS: 1 selected test / 196 skipped tests.
- Aggregate fixture guard:
  `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "gallery group wrappers" --reporter=default`
  PASS: 1 selected test / 196 skipped tests.
- Platform rule suite:
  `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts --reporter=default`
  PASS: 1 file / 197 tests.
- Export suite:
  `pnpm -C inkforge exec vitest run src/services/export --reporter=default --maxWorkers=1 --no-file-parallelism`
  PASS: 36 files / 1174 tests.
- Lint:
  `pnpm -C inkforge exec eslint src/services/export/quality-detector.ts src/services/export/platform-export-rendering.test.ts --quiet`
  PASS.
- Type check:
  `pnpm -C inkforge exec vue-tsc --noEmit --pretty false`
  PASS.
- Build:
  `NODE_OPTIONS=--max-old-space-size=4096 pnpm -C inkforge build`
  PASS: 4653 modules transformed, Vite build completed in 23.25s.
- Release preflight:
  `pnpm -C inkforge style-proof:release-preflight -- --json`
  EXPECTED BLOCKED: exit 1, `status=blocked-by-external`, `canClaimComplete=false`,
  `externalHandoffRows=18`, `safeExternalRows=0`, `nextRowRefs=5`, `uniqueNextRows=3`.

Boundary:
- This proves local static residue detection only.
- It does not prove WeChat paste, phone preview, mobile interaction, Dark Mode, cover thumbnail
  acceptance, credentialed sync, scheduled send, platform preview, public rendering, upload,
  public-host acceptance, or publish success.
