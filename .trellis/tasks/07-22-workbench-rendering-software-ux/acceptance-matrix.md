# Acceptance Matrix — R-01..R-23

## Status vocabulary

- `verified`: current product code, a regression check, and real Tauri evidence all cover the requirement.
- `implementation-tested`: the production path and focused automation exist, but the full visible/restart interaction required by the PRD is not yet proven.
- `open`: a material behavior or required evidence is still missing.

No neighboring row supplies proof for another row. XHS/Zhihu account upload and publication are user-manual boundaries and are not automated or inferred from local artifacts.

## Requirement audit

| ID | Current production anchor | Automated evidence | Real runtime evidence | Status | Remaining acceptance gate |
| --- | --- | --- | --- | --- | --- |
| R-01 | `views/WorkstationView.vue` manager navigation now keeps 文件/版本/大纲/标签/对话 in one five-column compact grid; each real destination and the independent collapse action use installed Lucide icons, while destinations retain complete labels, `aria-pressed`, deterministic `data-manager-tab`, and keyboard focus | The first source contract failed with only 2/5 deterministic tabs; a follow-up red pass rejected the remaining hand-written collapse SVG; final contract passes 15/15. Adjacent Workstation layout/focus/vignette checks pass 3 files / 25 tests; exact ESLint, `vue-tsc`, and the 4,675-module production build pass | Original `image-1.png` and the pre-fix `tag-browser-flat-runtime-20260722.png` prove the former horizontal crowding. `native-passive-maximized-rendering-20260723.png` now proves the current five equal icon/label destinations are visible without overlap in the maximized 150%-DPI state; it does not substitute for default/narrow interaction proof. The prior command-bar screenshot remains only R-04/R-10 shell evidence | implementation-tested | In the current native app, confirm all five icon/label slots at default manager width, narrow usable width, maximized/restored states, Windows scaling, Light/Dark themes, keyboard focus/activation, and collapse/reopen without clipping or content loss |
| R-02 | `components/tag-system/TagBrowser.vue` keeps one flattened manager surface; `TagInput.vue` now presents a compact search row plus styled native color/create actions, while filtering separates an accessible two-state match switch from Apply/Clear actions | `FileManager.information-architecture.test.ts` red 1/4 then green 4/4 on the compact-control contract; tag service plus information-architecture checks pass 2 files / 17 tests; the current core serial regression passes 15 files / 98 tests; exact ESLint, `vue-tsc`, and production build pass | `tag-folder-crud-runtime-20260722.txt` remains valid proof of create/assign, OR/AND filter, rename, reload, remove, merge, cleanup, and zero residual `IF0722-TAG-*`; the older `tag-browser-flat-runtime-20260722.png` exposed the still-browser-like controls that this follow-up repairs, so it is not reused as post-fix visual proof | implementation-tested | In the running native app, confirm default/narrow widths have no clipping or full-width grey CTA, color selection uses the styled control, match mode and Apply/Clear read as separate groups, focus/disabled/dark states are coherent, and one real create/filter/clear cycle still succeeds |
| R-03 | Unpinned `.panel-inspector` is an absolute edge overlay; docked state alone reserves width. Its expanded desktop overlay begins below the canonical 44px Stage header so the single WeChat/XHS/Zhihu selector stays operable | `WorkstationView.desktop-layout.test.ts` rejects the former flex-column reveal, pins one platform selector, and requires expanded-overlay clearance equal to the 44px Stage header. The red run failed 1/16; focused green is 16/16 and adjacent Workstation/persistence coverage is 38/38 | `edge-writing-focus-runtime-20260722.txt`; DPI-aware 150%-scale Win32/UIA probe completed 10/10 open, hold, and collapse cycles with `0px` maximum editor-rectangle delta. `native-passive-maximized-rendering-20260723.png` then exposed the covered platform row; `r03-r14-platform-tabs-unobscured-20260723.png` proves the current open Inspector leaves 微信/小红书/知乎 and Stage actions unobscured | verified | None for the current unpinned desktop overlay contract |
| R-04 | Permanently mounted `data-testid="workstation-review-mode"` action | `WorkstationView.desktop-layout.test.ts` pins the non-overflow Review entry | `review-mode-runtime-20260722.png`, `review-mode-native-widget-check-20260722.png` | verified | None for the current desktop-width contract |
| R-05 | `settings.appearance.typography` through `useTypography()` and shared export overrides; legacy `settings.export.textIndent` is migration-only and has no active UI/renderer consumer. Explicit indentation now matches preset `p:first-of-type` specificity and resets quote paragraphs. Post-Juice flagship decorators preserve recognized canonical heading/quote overrides instead of swallowing them | Existing Store/Settings/Workstation/Export/Publish coverage remains green. The all-16-preset indent matrix proves ON gives first/second body indentation 16/16 with quotes reset 16/16, and OFF leaves all three unindented. A final-artifact control matrix now proves all 8 canonical controls affect all 16 WeChat presets: 128/128 with `noEffect=[]`; before repair it failed exactly 8 pairs across the four flagship presets. Direct decorator/matrix checks pass 73/73, the expanded chain passes 152/152, the full serial export suite passes 44 files / 1,386 tests, and the earlier renderer chain remains 405/405. Exact ESLint, `vue-tsc`, the application style preflight, and a fresh 4,675-module production build pass | Native PID 21140 and Vite PID 39544 remain alive and port 3005 returns HTTP 200 after the final test reload. `r05-indent-specificity-fix-20260723.txt` and `r05-flagship-control-effect-matrix-20260723.txt` record exact readback. The visible article must be reconfirmed after reload | implementation-tested | Change every visible parameter through UI and compare Workstation preview, Export artifact, Settings preview copy, and WeChat Publish artifact. In a flagship preset, heading style and quote style must both visibly change rather than retaining only the flagship decorator. For indent ON, first and second ordinary paragraphs must be `2em` while a quote stays unindented; for OFF, all three must be unindented. Confirm no visible no-op duplicate |
| R-06 | Existing `TypewriterMode` extension and writing-assist settings; disabled-to-enabled activation now anchors the unchanged selection immediately | `TypewriterMode.decorations.test.ts`, `writingAssist.test.ts`, and `WorkstationView.vignette.test.ts` cover activation, decoration, reduced motion, persistence, and independent vignette state | `edge-writing-focus-runtime-20260722.txt` plus the `writing-focus-*-runtime-20260722.png` set prove button/F9 anchoring, visible dimming, bounded vignette, reversal, and unchanged real selection text | verified | None for the current local writing-focus contract |
| R-07 | Shared inspector widget content supports docked, in-app floating, and whitelist-only Tauri utility windows | `WorkstationView.desktop-layout.test.ts`, desktop/window Rust tests, inspector-widget service tests | `native-platform-widget-detached-20260722.png`, `native-platform-widget-xhs-sync-20260722.png`, `widget-restart-runtime-20260722.png` | verified | None for preview/references/statistics widget set |
| R-08 | `InspectorWidgetContent.vue` bounded references card with wrapped links and independent scroll | `WorkstationView.desktop-layout.test.ts` pins min height, scroll owner, and URL wrapping | `references-many-long-inspector-runtime-20260722.png`, `references-many-long-pinned-inspector-runtime-20260722.png` | verified | None for empty/long/many reference layouts |
| R-09 | Focus exit control is absolute inside the editor surface below global titlebar allocation | `WorkstationView.desktop-layout.test.ts` rejects fixed-window positioning | Current focus mode is available for hand test | implementation-tested | Verify at Windows scaling, maximized/restored, and narrow host sizes with native controls visible |
| R-10 | Workstation duplicate `ForgeNibMark/header-brand` removed; global `TitleBar` and native icon retained | `WorkstationView.desktop-layout.test.ts` pins a single application brand | `desktop-current-20260722.png` plus the post-R13 `workstation-restored-commandbar-runtime-20260722.png` directly show one title-bar brand, no second Workstation brand, and the retained native taskbar identity | verified | None for the current single-brand desktop contract |
| R-11 | `FileManager.vue` accepts only canonical `tree`; articles/categories/assets and context actions remain connected; `editorStore.renameArticleTitle()` keeps article metadata and edited content coherent | `FileManager.information-architecture.test.ts`, layout migration tests | `file-tree-canonical-runtime-20260722.png`, `article-category-title-coherence-runtime-20260722.png`, `tag-folder-crud-runtime-20260722.txt`, `asset-persistence-full-restart-20260722.txt`; native selection of the owned `public/favicon.svg` appeared as `素材 (1)` / `favicon.svg`, then a full Tauri process stop/start preserved `IF0722-CAT-B (1)`, `IF0722-ARTICLE-A`, its 20-paragraph body, and the same asset | verified | None for the current canonical article/category/asset persistence contract |
| R-12 | Split state, divider, persistence, explicit insufficient-space state, canonical platform `previewHtml`, active Typora/CodeMirror scroll-surface selection, mode-change listener rebind, and Source-mode proportional fallback. The entry now distinguishes requested state (`aria-pressed`) from actual pane visibility (`aria-expanded`) and controls the named preview pane; the split toolbar uses installed `Link2` / `Unlink` / `X` icons and names its close action. | The toolbar-semantics contract failed exactly 1/16 before the template repair and is green 16/16 afterward. The current split/layout/sync/persistence/preview regression passes 6 files / 44 tests; exact ESLint, `vue-tsc --noEmit`, and the 4,675-module production build pass. | Native UIA previously proved Split `0→1→0`, visible rendered-right pane, adjustable divider, sync-scroll control, close action, and restored closed state. Vite accepted the current template HMR without an error and the user-owned app session stayed running. | implementation-tested | Native pointer/keyboard divider movement, both-direction Typora/Source synchronized scrolling, platform-style response, reload persistence, requested-versus-expanded narrow behavior, and narrow insufficient-space capture remain |
| R-13 | Workstation keeps one visible preset selector and places width, font, typography, indent, heading, and quote controls in one native default-closed `<details class="inspector-advanced-settings">`; every button now has explicit type/pressed semantics, the range has an accessible name, and color/font marks use installed Lucide icons; ExportModal keeps its singular `preset-grid` primary, default-closed diagnostic drawers, and one pinned `nativeResult` copy/download pair; the native metadata card no longer duplicates those actions | Workstation hierarchy red 1/10 became green 10/10; the new control-semantics contract failed 1/16 and is green 16/16 after the template-only repair. Current adjacent rendering/Workstation regression passes 8 files / 64 tests; integrated theme/Settings/Workstation/preview/Export/Publish checks remain 9 files / 307 tests. The Export primary-action red contract failed 1/3 before consolidation; green is 3/3 and the directly related Export/Workstation/Publish/cross-platform/history run passes 6 files / 417 tests; exact ESLint, `vue-tsc`, and production build pass | `native-passive-maximized-rendering-20260723.png` proves the current maximized native frame contains one primary 16-choice WeChat selector and one closed `高级排版参数` disclosure, with no second permanently expanded typography grid. Vite accepted the template/store repairs and the native app remains responsive, but no foreground interaction was used to open/close the panes or activate every control | implementation-tested | Visually confirm both panes start closed, keyboard open/close, focus and selected state for every retained control, one primary selector, no quality/proof text permanently occupying the normal export flow, and exactly one single-line native copy/download pair in Export |
| R-14 | Canonical registries expose 16 WeChat, 5 XHS, and 3 Zhihu presets through real converters; Workstation retains a valid preset per platform and sends that exact selected ID to preview; the unpinned Inspector leaves the single platform switch unobscured instead of duplicating it; `ARTICLE_PRESETS` is now a compatibility projection of all 16 canonical WeChat presets and `generatedCSS` delegates to the canonical generator | Persona/count/affinity contracts remain green; the stricter rich-fixture audit removes non-visual metadata, normalizes colors, sorts attributes/CSS declarations, and still proves 16/16 WeChat, 5/5 XHS, and 3/3 Zhihu final-output signatures unique with zero collisions. Its red/green contract and expanded serial renderer run pass 8 files / 725 tests; legacy migration/canonical precedence plus native-copy path remain covered by `theme.test.ts`. The current application preflight exits 0 with `application-ready`: 27 SVG modules across 7 families, 108/108 persona renders, 5/5 slots, 2/2 surfaces, 3/3 pipeline contracts, 13/13 selectable rendered style samples, 45 sample SVG modules, and zero local/safety/choice issues | Read-only UIA captured the running `IF0722-ARTICLE-A` body; `r14-r15-real-article-platform-gallery-20260722.json/.html` rendered all 16 WeChat presets through the production converter with 16 unique hashes; `native-passive-maximized-rendering-20260723.png` visibly shows all 16 canonical WeChat choices together in the current native selector; `r03-r14-platform-tabs-unobscured-20260723.png` proves 微信/小红书/知乎 remain fully visible above the open Inspector; current Tauri accepted the repairs and remains HTTP 200 | implementation-tested | Visually review all 16 WeChat recipes; switch WeChat → XHS → Zhihu → back with exactly one selected preset, then open Export and Publish and confirm the same pair drives each matching preview/artifact |
| R-15 | Independent `xiaohongshu.ts` and `zhihu.ts` preset/converter/native-format paths; Publish and both Workstation copy entries consume `convertToNativeFormat()` for manual handoff; preview HTML remains preview-only; no WeChat SVG wrapper reuse | Publish passes 12/12; Workstation passes 14/14; integrated Workstation/Publish/cross-platform rendering passes 6 files / 441 tests with XHS similarity 0.9979; XHS slicer remains 8/8 | The real article gallery emits 5/5 unique XHS and 3/3 unique Zhihu outputs. Native artifacts and the 10-page XHS PNG pack retain prior readback/visual evidence; Tauri PID 21140 and port 3005 remained alive after both copy repairs | implementation-tested | In Workstation and Publish, visibly select all 5 XHS and 3 Zhihu presets, confirm labels identify XHS text / Zhihu Markdown, then paste and inspect the real clipboard content. Account upload/publication remains user-manual and external |
| R-16 | Workstation Export opens `ExportModal`; Publish routes to `/publish`; Workstation, Export, and Publish copy WeChat rich HTML / XHS text / Zhihu Markdown; empty Workstation sample previews cannot be copied as real output; WeChat credentialed draft operations remain in Publish; Publish has no local preview-HTML download; native folder/blog history is `local` + `write-local` | `WorkstationView.desktop-layout.test.ts`, `PublishView.wechat-presets.test.ts`, `ExportModal.local-delivery.test.ts`, `settings.export-history.test.ts`; Workstation/Publish/cross-platform integration passes 6 files / 441 tests. The Export primary-action source contract first failed 1/3 on the duplicate `previewHtml` handler, then passed 3/3 after the pinned action was moved to `nativeResult`; the related 6-file regression passes 417/417. The Publish local-download contract moved from 1 failed / 11 passed to 12/12, and its adjacent 5-file regression passes 411/411 with exact ESLint, `vue-tsc`, and production build green. Current no-foreground chain revalidation also passes the directly relevant 6 files / 53 tests with XHS preview similarity 1.0000 and cross-route similarity 0.9979 | Both route actions remain present; Tauri stayed alive after the repaired handoffs and builds, port 3005 remains HTTP 200, and no clipboard or account action was automated | implementation-tested | Visible route/copy/history check: Export never opens Publish; Publish exposes source inspection/manual/native/channel actions but no local preview-HTML download; Workstation, Export, and Publish clipboard/file output plus exact byte history match each platform-native format; empty real body fails closed; Settings keeps local readback separate from social artifact copy; no external success is inferred |
| R-17 | `local-delivery.ts` bundle builder, typed desktop bridge, and Tauri staged/readback/rollback writer; ExportModal folder/blog controls | Three Vitest files / 14 tests; Rust desktop tests 3/3 | Visible Windows folder-picker runs produced personal-folder Markdown and personal-blog HTML; both showed per-file readback, matched independent disk hashes, and left no stage/backup residue | verified | None for the local folder/static-site delivery contract |
| R-18 | Stable `/settings` route identity, mounted shell, inner-tab-only transition, bounded content scroller | `ViewTransition.test.ts`, `SettingsView.software-shell.test.ts` | `settings-about-independent-scroll-runtime-20260722.png` | verified | None for shell persistence and no-black-frame category switching |
| R-19 | Shared settings control classes; updater child owns styles; settings, CustomCSS, and extension native inputs remain accessible behind styled triggers; shared action hover excludes `:disabled` and disabled buttons expose muted/not-allowed affordance | `SettingsView.software-shell.test.ts` passes 9/9, rejects a raw `sv-input` file control, and pins the shared disabled/hover selectors; focused Hub/Settings run passes 23/23; core serial regression passes 15 files / 96 tests | The repaired Settings surface is running at HTTP 200 in native PID 21140; `r19-r21-source-closure-20260722.txt` records the red/green closure | implementation-tested | Visible extension refresh, styled manifest chooser cancel/select, focus ring, narrow no-wrap and disabled-state review |
| R-20 | About remains inside Settings shell with compact identity and independently scrollable navigation/content | `SettingsView.software-shell.test.ts` | `settings-about-polished-runtime-20260722.png`, `settings-about-independent-scroll-runtime-20260722.png` | verified | None for current About/navigation overflow contract |
| R-21 | Hub retains Start and red FAB; header/Recent/Hero/article-empty/template-market duplicate creation actions are removed; “最近编辑” is a semantic 18px/700 card heading rather than a small eyebrow; real creation functions and template selection remain intact | `HubView.cards.test.ts` passes 15/15, explicitly rejects every removed entry path, retains Start/FAB, and pins the recent-heading semantic/size contract; focused Hub/Settings run passes 23/23; latest core serial regression passes 15 files / 97 tests | Running Tauri accepted the consolidated Hub HMR; HTTP 200 and native PID 21140 remained alive; no article/store/clipboard mutation; `r19-r21-source-closure-20260722.txt`, `r21-recent-heading-hierarchy-20260722.txt` | implementation-tested | Confirm visually that “最近编辑” has the intended prominence and only Start/red FAB remain, then exercise blank/template/import, shortcut, close/focus return, and a template market containing only real templates with no extra new-card/dead space |
| R-22 | Persisted local/AI source; editable bounded local text/author; deep-watch `localStorage` save with visible failure; existing `aiStore.generate()` path with explicit unavailable setup, loading status, parsed success, alert failure, and retry controls. AI empty state never substitutes the local quote. | Current Hub plus AI Settings/Store regression passes 2 files / 19 tests. It covers real storage round trip, malformed recovery, disabled/missing-key truth, and real failed provider requests; the expected `127.0.0.1:1 ECONNREFUSED` calls prove the failure path without fabricating a provider success. | Source/runtime audit found no deterministic implementation gap, but no complete visible provider-state/restart capture exists yet | implementation-tested | Visible local edit/restart, AI unconfigured, loading, failure, retry, and real configured-provider result when credentials exist |
| R-23 | Anchored Hub result surface uses real filtered articles with Arrow/Enter/Escape/clear paths; global Ctrl/Cmd+F overrides editable-control defaults and result Escape restores search focus | `HubView.cards.test.ts` red/green contract passes 14/14 after first failing 2/14 on shortcut ordering and result-focus close | Running Tauri app accepted Hub HMR; HTTP 200; native PID stayed alive | implementation-tested | Search a real article, invoke Ctrl/Cmd+F from a local-inspiration field, navigate/open/return/clear, Tab into a result then Escape back to input, and prove honest no-result state after restart |

## Latest delivery checkpoint — 2026-07-23

- `pnpm style-proof:current-round` exits 0 with `application-acceptance-ready`; the declared `application-svg-style-wechat-local` target is `current-round-ready` with `canClaimCurrentRoundTarget:true`, `actionableLocalRows:0`, and the strict external-release boundary preserved.
- The current serial cross-row matrix passes 17/17 files and 118/118 tests, including Publish and export-history coverage added after the previous 15-file baseline. The focused style-surface suite passes 8/8 files and 346/346 tests.
- Whole-worktree and staged diff checks pass. No InkForge product feature/module/component deletion exists; the only tracked deletion set is the pre-existing misspelled Trellis skill-path migration documented in `worktree-integrity-audit-20260723.txt`.
- These checkpoints prove local application readiness and regression safety, not the visible native interactions listed below. The goal must stay active until those interactions are observed or repaired.

## Current blocking rows

The goal is not complete while any `open` row remains or any `implementation-tested` row lacks its listed real Tauri gate. The next non-destructive runtime sequence is:

1. Finish the user's current hand test without stealing the foreground window.
2. Visually retest the now-implemented R-13 Workstation advanced pane; the source regression is green but no neighboring screenshot may supply the open/close proof.
3. Run the remaining Workstation, rendering, Settings, Hub, and post-fix R-02 tag-control interaction matrix in `handtest-remaining-20260722.md`; R-10 and R-11 are verified.
4. Integrated serial checks, build, Rust checks, and GitNexus change detection are complete; repeat only the checks affected by any hand-test repair, then run the final exact-scope diff review.

## 2026-07-23 user re-acceptance override

This table is the current status authority when it conflicts with the
historical R-01..R-23 rows above. The user's current screenshots are direct
runtime counter-evidence; prior source-contract, build, or screenshot evidence
does not close the reopened behavior.

| ID | Current counter-evidence / target | Current status | Required current proof |
| --- | --- | --- | --- |
| U-01 | Current screenshot 1: manager collapse leaves the project/file surface expanded | open | Expand/collapse/reopen/restart geometry and state at standard/narrow/maximized/restored sizes |
| U-02 | Current screenshot 2: inspector content is clipped and lacks a complete floating-window frame | open | Dock/in-app/native frame, min size, scroll, focus, move/resize, close/reopen/re-dock |
| U-03 | User reports typography works but has insufficient range and choice | open | Capability inventory, no-op rejection, expanded controls, preview/artifact effect matrix |
| U-04 | User rejects current dim-focus/typewriter quality | open | Long-document typing/selection/undo/navigation/combination/reload visual and state proof |
| U-05 | User reports floating content folding and lifecycle/animation gaps | open | Single-placement invariant plus close/reopen/side-load/restart animation and focus proof |
| U-06 | User reports multi-platform preview is usable but not faithful/elegant | open | Same real article in current WeChat/XHS/Zhihu editor-calibrated frames and artifacts |
| U-07 | Current screenshot 3: non-flagship presets do not all communicate a unique signature | open | Every selectable preset passes structural distinction and current side-by-side visual review |
| U-08 | CC, reading-time notice, and platform components are absent from delivery | open | Typed snapshot, UI, renderer/fallback, persistence, export and publish checks |
| U-09 | Export/Publish separation is correct and must remain intact | implementation-tested | Regression proving no responsibility crossover after U-08 |
| U-10 | High-density view debt and duplicate local presentation/state remain | open | Symbol-impact-backed extraction/deduplication with no feature/data deletion |
| U-11 | Current screenshot 4: Publish source viewer is squeezed at the bottom | open | Responsive source viewer height, independent scroll, disclosure, copy and action-bar clearance |
| U-12 | Current screenshots 5/6: Hub screen composition leaves large dead areas and incomplete panels | open | Supported viewport matrix with complete content, bounded cards and correct scroll owners |
| U-13 | Current screenshot 7: local quote/author editing is incorrectly exposed on Hub | open | Settings-owned edit/persistence plus read-only Hub source/refresh/settings route |
| U-14 | Current screenshot 8: search input shows two red focus borders | open | One focus-within ring, result keyboard lifecycle and focus return |
| U-15 | Current screenshot 9: immutable visual baseline is presented as an ordinary setting | open | Relocation to Advanced/Developer diagnostics; normal Appearance contains actionable controls only |
| U-16 | Pi is not yet the real AI provider core | open | Real configured Pi-compatible health/model/stream/cancel/error path with redacted evidence |
| U-17 | Current screenshot 10: Sync is unconfigured and offers no provider configuration workflow | open | Real provider create/edit/test/save/select, engine result, queue/conflict and failure proof |
| U-18 | Current screenshots 11/12/13: About/Advanced/Data/Sync ownership is mixed | open | Stable navigation and complete relocated capability inventory with no deletion |
| U-19 | Current screenshot 13: Developer Mode control/lifecycle and visual brand quality are incomplete | open | Toggle/shortcut/persistence/lazy-panel/disabled/focus plus current visual proof |

The previous `application-acceptance-ready` record is now scoped only to the
local SVG/style pipeline checks it executed. The overall task is not ready for
delivery until U-01..U-19 are closed with current evidence.

## 2026-07-24 native software acceptance authority

This section supersedes the 2026-07-23 `open` table. InkForge acceptance was
performed against the real Tauri desktop process with Windows UI Automation,
Win32 geometry/capture, packaged-asset builds, typed native boundaries, and
durable application state. Browser/Vite screenshots are not product evidence.
External editor research remains input to renderer rules only.

| ID | Current implementation and native evidence | Status | Boundary after this round |
| --- | --- | --- | --- |
| U-01 | The manager uses an explicit collapsed state, no hidden hover reopen strip, one constrained editor scroll owner, and selection-viewport capture/restore across the width transition. `native-manager-collapse-final-20260724.*`, `native-manager-hover-latch-final-20260724.txt`, and `native-manager-expand-final-20260724.*` keep paragraph 20 visible through collapse, edge dwell, and reopen. | verified | None for the local desktop manager contract. |
| U-02 | Docked, in-app floating, closed, and detached-native inspector hosts share `InspectorWidgetContent` and the complete widget frame. `native-inspector-menu-current-20260723.*`, `native-platform-float-current-20260723.*`, `native-platform-closed-current-20260723.*`, `native-platform-widget-detached-20260722.png`, and restart evidence cover frame, scroll, close/reopen, re-dock, and native-window persistence. | verified | None for preview/references/statistics widgets. |
| U-03 | `settings.appearance.typography` remains the only writable source. The expanded capability model exposes font stacks, paragraph/list/title/quote/divider/media/code/table/platform-safe controls only where consumed. The all-preset final-artifact matrix proves all eight canonical controls affect all 16 WeChat recipes (128/128, no no-op pairs); focused UI/schema/renderer tests are included in the 185-test final matrix. | verified | XHS plain-text and Zhihu Markdown intentionally do not pretend to carry unsupported CSS. |
| U-04 | Typewriter and dim-focus use the real editor scroll owner, bounded vignette, live option updates, selection-safe anchoring, bidirectional end spacer, hydration cleanup, reduced-motion behavior, and immediate activation anchoring. The `native-typewriter-*-20260723.*` series plus the final paragraph-anchor manager proof covers long-document navigation, enable/disable, reload, start/end, and cleanup. | verified | None for the local editor behavior. |
| U-05 | Widget placement is a single-state lifecycle (`docked`, `floating-in-app`, `detached-native`, `closed`) with one content owner, persisted bounded geometry, focus return, close/reopen/re-dock, and reduced-motion-aware transition. Current float/close evidence and the prior detached/restart sequence exercise the production lifecycle without duplicated card content. | verified | Native OS animation cadence may vary with Windows reduced-motion settings by design. |
| U-06 | WeChat/XHS/Zhihu use independent preview and artifact families. Native switching evidence (`native-rendering-*-switch-20260724.txt`) changes the real handoff labels and restores the WeChat choice. The application style proof passes 27 SVG modules, 108 module/persona pairs, 5/5 slots, 2/2 surfaces, and 3/3 export-pipeline contracts with zero local application issues. | verified | Real WeChat editor paste/phone rendering is the user's external hand test; XHS/Zhihu publication is explicitly user-manual. |
| U-07 | All 16 WeChat, 5 XHS, and 3 Zhihu selectable presets have unique normalized final-output signatures. Native Publish exposes every WeChat persona/flagship; structural tests require multiple real recipe differences rather than IDs, metadata, or accent-only changes. | verified | Final aesthetic preference remains available for the user's visual tuning pass. |
| U-08 | `DeliveryAdornmentConfig` supplies reading-time notice, optional CC license, and ordered typed platform components. Native Publish shows CC, reading time, and WeChat song/image/link/related-article/contact-card controls; renderer tests cover output and explicit unsupported-platform fallback. | verified | Credential-bound platform insertion remains an explicit manual/channel action, never fake success. |
| U-09 | Export remains local copy/file/folder/static-site delivery; Publish remains channel/account/draft/manual handoff. Current source contracts, native route inspection, and cross-route artifact tests reject responsibility crossover. | verified | None. |
| U-10 | Shared roots were repaired instead of adding parallel state: canonical typography/preset/artifact paths, shared widget content, shared scroll-owner resolution, extracted delivery/inspiration/Pi/Sync services, and dead duplicate entry removal. No existing business module, data, compatibility migration, or product feature was deleted. | implementation-tested | The repository still contains large historical views; this round does not claim a whole-repository rewrite or zero future refactoring. |
| U-11 | `publish-source-panel` is a large independent source surface with its own scroll, copy, and collapse controls. `native-publish-source-open-20260724.*` records a 765×1049 panel containing the real generated inline WeChat HTML and `data-ink-svg` output without action-bar compression. | verified | None for current supported desktop sizes. |
| U-12 | Four native Hub captures (`native-hub-current`, `native-hub-second-screen`, `native-hub-third-screen`, `native-hub-fourth-screen`) show complete screen compositions for creation flow, categories/recent/inspiration, templates, and real articles without the former dead lower areas. | verified | Sparse datasets continue to use truthful empty states rather than fabricated cards. |
| U-13 | Hub inspiration is read-only output with source selection and Settings entry; editing/default author/source policy lives in `InspirationSettingsPanel` under Settings and reuses the existing persisted record. Native Hub and Settings evidence confirm the ownership split. | verified | A live external AI result requires configured credentials; local edit/persistence is complete. |
| U-14 | Search owns one shared `:focus-within` ring. Native search with real query `IF0722` returned the real article and `native-hub-search-result-20260724.*` shows one brand focus shell; the query was cleared after proof. | verified | None. |
| U-15 | Immutable ThemeEngine/FontSystem baseline diagnostics moved out of ordinary Appearance into Advanced/Developer diagnostics. Native Settings captures show Appearance as actionable settings and Advanced as the explicit diagnostics owner. | verified | None. |
| U-16 | Pi is implemented through the existing typed AI provider contract with keychain-only credentials, health/model discovery, manual model, streaming, cancellation, normalized errors, and fail-closed fallback. Native Settings identifies `Pi AI 运行内核 v0.81.1`; unit tests exercise the real request/failure boundary without fabricated success. | implementation-tested | Live provider success requires the user's configured Pi-compatible endpoint/key and is not a local release claim. |
| U-17 | Sync Settings can choose Disabled/WebDAV/Git/SelfHosted, edit typed provider fields, test/save/select configuration, and display real engine queue/conflict/last-result state. Native WebDAV draft evidence confirms the real configuration workflow and the UI was restored to Disabled afterward. | implementation-tested | A successful remote sync requires an operator-owned endpoint/repository and credentials. |
| U-18 | Storage/backup is owned by Data, provider/queue by Sync, runtime/log/user-agent/diagnostics/Developer Mode by Advanced, account/security by Profiles, and product identity/release/license/credits by About. The `native-settings-*-20260724.*` suite covers every relocated section. | verified | None. |
| U-19 | Developer Mode has one persisted store lifecycle, three-chord activation, lazy DevPanel mount/unmount, disabled action state, and current InkForge settings chrome. Native before/open/close/off evidence verifies the full toggle lifecycle and leaves the setting disabled. | verified | None. |

### Current handoff disposition

- No U-row remains `open`.
- `implementation-tested` on U-10, U-16, and U-17 is an explicit scope
  boundary, not a hidden product failure: whole-repository zero-debt, live Pi
  credentials, and live remote Sync infrastructure are not available local
  release assertions.
- The local application target `application-svg-style-wechat-local` is ready
  for user testing. It does not claim WeChat phone preview, credentialed
  draft/sync/publish, or XHS/Zhihu account publication.
- Final product evidence comes from the copied packaged Release executable,
  not a browser/Vite surface. The NSIS installer, MSI, direct executable,
  hashes, native UIA/Win32 route evidence, and runtime-engine boundary are
  recorded in `evidence/native-software-handoff-20260724.md`.
