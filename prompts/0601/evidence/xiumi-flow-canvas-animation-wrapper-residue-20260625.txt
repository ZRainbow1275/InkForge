Xiumi Flow-Canvas Animation Wrapper Residue - 2026-06-25

Scope:
- Convert the 2026-06-25 Xiumi applied SVG/style DOM readback into one additional static
  publishability blocker.
- No browser profile, screenshot, platform URL, account label, auth secret, QR payload, HAR payload,
  raw template source, media URL, save action, preview action, upload action, sync action, or
  publish action is recorded here.

Observed source:
- The sanitized Xiumi applied editor readback listed `tn-group-flow-canvas-for-svg-animation`
  among central editor wrapper families after a visible SVG style card mutated the center editor.
- Existing residue rules already covered broad `tn-*`, `ng-*`, `opera-*`, `contenteditable`,
  hosted-media, `foreignObject`, content-layer, and flow-canvas binding patterns.
- A cleaned fragment that retained only the flow-canvas animation wrapper class remained worth
  blocking independently because it still represents Xiumi authoring/runtime state, not an
  InkForge-owned publishable module.

Implemented:
- Added a `MARKET_EDITOR_RESIDUE_RULES` entry labeled
  `Xiumi flow-canvas animation wrapper residue`.
- Added a reduced regression fixture with only the wrapper class and a plain inline SVG shell,
  intentionally without `tn-cell`, `tn-animate`, `ng-*`, `opera-*`, `contenteditable`, or hosted
  media markers.

TDD evidence:
- Red run before implementation:
  `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "flow-canvas animation wrapper" --reporter=default`
  failed because `wechat-market-editor-residue` was absent for the reduced fixture.
- Green run after implementation:
  `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "flow-canvas animation wrapper" --reporter=default`
  passed with 1 selected test and 177 skipped tests.
- Full platform rendering regression:
  `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts --reporter=default`
  passed with 1 file and 178 tests.
- Full export service serial regression:
  `pnpm -C inkforge exec vitest run src/services/export --reporter=default --maxWorkers=1 --no-file-parallelism`
  passed with 36 files and 1155 tests.
- Targeted lint, `vue-tsc --noEmit`, and production build passed.
- Release preflight remained correctly blocked with `canClaimComplete=false`,
  `status=blocked-by-external`, `externalHandoffRows=18`, `safeExternalRows=0`,
  `nextRowRefs=5`, and `uniqueNextRows=3`.

Boundary:
- This is static market-editor residue protection only.
- It does not prove WeChat editor paste, phone preview, mobile SVG/SMIL interaction, Dark Mode,
  cover thumbnail acceptance, credentialed sync, scheduled send, platform preview, public
  rendering, public-host acceptance, XHS/Zhihu upload, or publish success.
