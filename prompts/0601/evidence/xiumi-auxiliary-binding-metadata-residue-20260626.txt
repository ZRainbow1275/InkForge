Xiumi Auxiliary Binding Metadata Residue - 2026-06-26

Scope:
- Static publishability protection for copied or partially cleaned Xiumi auxiliary binding
  metadata.
- Covers tn-bind-aux-prop when it appears as an attribute name.
- No platform save, sync, copy-to-WeChat, preview, upload, scheduled send, or publish action.

Live source:
- CloakBrowser opened the live Xiumi v5 paper editor and entered a visible paper template/style
  into the center paper.
- The center-paper DOM contained a shape/line cell with:
  tn-bind-aux-prop="{ backgroundColor: compAux.bgc1 }"
- That binding points at editor-side auxiliary state. Publishable output must contain resolved
  inline style or an InkForge-owned fallback, not the Xiumi binding expression.

Implemented:
- Added a reduced regression fixture that keeps only tn-bind-aux-prop plus readable text.
- Added the detector label:
  Xiumi auxiliary binding metadata residue
- Kept the generic Xiumi tn-* attribute guard as the final catch-all for unexpected Xiumi tn-*
  leakage.

Why this matters:
- Existing Xiumi guards already catch component template bindings, identity metadata, animation
  bindings, link bindings, image bindings, component/cell/child/page bindings, atom context,
  runtime path bindings, authoring trees, overlay controls, operator docks, and Angular runtime
  controls.
- This slice covers the narrower auxiliary-property path where copied DOM keeps editor-side color
  or auxiliary state bindings after broader component wrappers have been removed.

Regression coverage:
- WeChat: detectQuality(..., 'wechat') reports wechat-market-editor-residue with
  Xiumi auxiliary binding metadata residue.
- Xiaohongshu: detectQuality(..., 'xiaohongshu') reports xhs-market-editor-residue with
  Xiumi auxiliary binding metadata residue.
- Zhihu: detectQuality(..., 'zhihu') reports zhihu-market-editor-residue with
  Xiumi auxiliary binding metadata residue.

TDD:
- Red:
  pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "auxiliary binding" --reporter=default
  Result: failed as expected because the detector emitted only Xiumi tn-* attribute.
- Green:
  pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "auxiliary binding" --reporter=default
  Result: passed, 1 selected test, 227 skipped tests.

Verification:
- Slice diff:
  git diff --check -- <slice paths>
  Result: passed with only normal Windows LF-to-CRLF warnings.
- Focused file:
  pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts --reporter=default
  Result: passed, 1 file, 228 tests.
- Export suite:
  pnpm -C inkforge exec vitest run src/services/export --reporter=default --maxWorkers=1 --no-file-parallelism
  Result: passed, 36 files, 1205 tests.
- Targeted ESLint:
  pnpm -C inkforge exec eslint src/services/export/quality-detector.ts src/services/export/platform-export-rendering.test.ts --quiet
  Result: passed.
- Type check:
  pnpm -C inkforge exec vue-tsc --noEmit --pretty false
  Result: passed.
- Build:
  NODE_OPTIONS=--max-old-space-size=4096 pnpm -C inkforge build
  Result: passed, 4653 modules transformed, Vite built in 40.09s.
- Release preflight:
  pnpm -C inkforge style-proof:release-preflight --json
  Result: expected exit 1, status=blocked-by-external, canClaimComplete=false,
  externalHandoffRows=18, safeExternalRows=0, actionableLocalRows=0, nextRowRefs=5,
  uniqueNextRows=3.

Boundary:
- This proves local static residue detection only.
- It does not prove WeChat PC rich paste, WeChat phone preview, mobile SMIL/click interaction,
  mobile Dark Mode, cover thumbnail acceptance, credentialed sync, scheduled send, platform
  preview, public article rendering, XHS/Zhihu account upload, public host acceptance, or publish
  success.
- No local browser runtime directory, session secret, QR code, account capture, capture-file
  reference, traffic archive, or account artifact is included in this committed evidence.
