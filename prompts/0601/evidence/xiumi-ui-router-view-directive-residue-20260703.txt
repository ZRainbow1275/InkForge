Xiumi UI Router View Directive Residue Gate - 2026-07-03

Scope:
- CloakBrowser-only sampling on the live Xiumi v5 paper editor.
- Local static publishability protection for WeChat, Xiaohongshu, and Zhihu export output.
- No save, copy, sync, upload, preview, publish, account-state artifact, local browser runtime
  material, template source, material source, or account text is committed.

Live observation:
- The Xiumi v5 paper editor exposed Angular UI Router route outlet chrome.
- Observed source-specific marker: `ui-view`.
- This marker is route placeholder plumbing for editor UI. It is not article content and is
  separate from `ng-*`, Angular runtime classes, `uib-*`, modal runtime, `tn-*`,
  operation-panel, hosted-media, and template markers.

Implemented:
- Expanded the existing `Angular/Vue authoring attribute` detector to cover `ui-view`.
- Added a reduced fixture with only `ui-view`.
- The detector remains anchored to a real `ui-view` attribute and does not block ordinary route or
  view prose.

Verification:
- Red:
  `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "UI Router view directives" --reporter=default`
  failed with 1 selected failing test before the detector update because no
  `wechat-market-editor-residue` issue was emitted.
- Green:
  the same command passed with 1 selected test and 367 skipped tests.
- Adjacent:
  `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "Angular (runtime controls|input source|link and dropzone)|UI Router view directives" --reporter=default`
  passed with 4 selected tests and 364 skipped tests.
- Platform regression:
  `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts --reporter=default --test-timeout=90000`
  passed with 1 file and 368 tests.
- Export service regression:
  `pnpm -C inkforge exec vitest run src/services/export --reporter=default --maxWorkers=1 --no-file-parallelism --test-timeout=90000`
  passed with 36 files and 1345 tests.
- Static checks:
  `pnpm -C inkforge exec eslint src/services/export/quality-detector.ts src/services/export/platform-export-rendering.test.ts --quiet`
  passed.
  `pnpm -C inkforge exec vue-tsc --noEmit --pretty false`
  passed.
  `NODE_OPTIONS=--max-old-space-size=4096 pnpm -C inkforge build`
  passed; the build transformed 4653 modules and completed in 35.45s after the interrupted run
  was restarted cleanly.
- Release preflight:
  `pnpm -C inkforge style-proof:release-preflight --json`
  exited 1 as expected with `canClaimComplete=false`, `status=blocked-by-local-conflict`, blocker
  kinds `local-conflict`, `phone-preview`, `external-dependency`, `unsafe-to-automate`, and
  `mutating-platform`, plus summary `blockerCount=5`, `combinedIssueCount=15`,
  `cannotClaimSteps=30`, `phoneOpenSteps=4`, `externalDependencyOpenSteps=15`,
  `unsafeToAutomateOpenSteps=10`, `mutatingOpenSteps=14`, `externalHandoffRows=19`,
  `safeExternalRows=0`, `actionableLocalRows=0`, `nextRowRefs=5`, and `uniqueNextRows=4`.

Boundary:
- This proves local static detection only.
- It does not prove WeChat paste, phone preview, route rendering fidelity, mobile interaction,
  Dark Mode, upload, credentialed sync, scheduled send, public rendering, public-host acceptance,
  XHS/Zhihu account upload, or publish success.
