Xiumi Layout Form Child Residue - 2026-06-29

Scope:
- Static publishability gate for copied or partially cleaned Xiumi v5 paper-editor layout/form
  child controls.
- Applies to WeChat, Xiaohongshu, and Zhihu quality reports.
- No platform action, phone preview, sync, upload, schedule, or publish action was performed.

Live source:
- CloakBrowser reviewed the logged-in Xiumi v5 paper editor DOM.
- The editor exposed layout/form child controls with source-specific class markers:
  `cell-layout-box`, `menuitem-level`, `padding-input`, `attr-thin-label`, and `attr-btn`.
- These nodes appeared around layout dropdowns, table/column insertion menus, padding controls,
  attribute labels, and confirm/reset buttons.

Implementation:
- Added the `Xiumi layout form child residue` detector label.
- Added a reduced regression fixture containing only the child markers, without the older
  `layout-box-panel`, `form-input-panel`, `op-ce-form-input`, `trigger-props-panel`, or
  `trigger-radio-input` parent/panel markers.
- Kept the rule anchored to class/id attributes, not prose.
- Kept adjacent detector labels independent from `Xiumi layout form panel residue`,
  `Xiumi operation bar dropdown residue`, `Xiumi generated link control residue`, and
  `Xiumi operator dock control residue`.

TDD:
- Red:
  `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "layout form child controls" --reporter=default`
  failed before the detector update because no `*-market-editor-residue` issue was emitted.
- Green:
  `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "layout form child controls|layout form panels|generated link controls|operator dock child controls|operation bar dropdown" --reporter=default`
  passed with 5 selected tests and 292 skipped tests after the detector update.

Full verification:
- `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts --reporter=default --testTimeout=60000`
  passed with 1 file and 297 tests.
- `pnpm -C inkforge exec vitest run src/services/export --reporter=default --maxWorkers=1 --no-file-parallelism --testTimeout=60000`
  passed with 36 files and 1274 tests.
- `pnpm -C inkforge exec eslint src/services/export/quality-detector.ts src/services/export/platform-export-rendering.test.ts --quiet`
  passed.
- `pnpm -C inkforge exec vue-tsc --noEmit --pretty false` passed.
- `NODE_OPTIONS=--max-old-space-size=4096 pnpm -C inkforge build` passed with 4653 modules
  transformed and Vite build completed in 39.11s.
- `pnpm -C inkforge style-proof:release-preflight --json` exited 1 as expected with
  `status=blocked-by-external`, `canClaimComplete=false`,
  `blockerKinds=["phone-preview","external-dependency","unsafe-to-automate","mutating-platform"]`,
  `externalHandoffRows=18`, `safeExternalRows=0`, `actionableLocalRows=0`, `nextRowRefs=5`,
  and `uniqueNextRows=3`.

Boundary:
- This is static publishability protection only.
- It does not prove WeChat PC paste, phone preview, mobile SMIL/click interaction, mobile Dark
  Mode, cover thumbnail acceptance, credentialed sync, scheduled send, platform preview, public
  article rendering, public-host acceptance, XHS/Zhihu account upload, or publish success.
