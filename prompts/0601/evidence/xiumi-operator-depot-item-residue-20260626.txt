Xiumi Operator Depot Item Residue - 2026-06-26

Scope:
- Static publishability protection for copied or partially cleaned Xiumi operator-depot and
  operation-item controls.
- Covers Xiumi operation item classes such as op-dc-depot, op-dc-hidden, op-dc-slot, ce-dc,
  cp-dc, dc-ce-*, dc-cp-*, dc-multi-cp-*, op-gl-dc-attr-bars, cp-role-*, ce-type-*, and the
  tn-op-dc-item attribute.
- No platform save, sync, copy-to-WeChat, preview, upload, scheduled send, or publish action.

Live source:
- CloakBrowser opened the live Xiumi v5 paper editor and read back the applied center paper plus
  editor control DOM.
- The operation-control DOM exposed hidden depot/menu item families including op-dc-depot,
  op-dc-slot, ce-dc, dc-ce-svg, dc-ce-animation, dc-cp-aux-props, and
  dc-cp-copy-to-clipboard.
- These nodes describe editor operation menu actions. They are not article content, style, SVG
  payload, or platform-safe export markup.

Implemented:
- Added a reduced regression fixture that keeps only operator-depot and operation-item child
  controls after the parent operator dock has been removed.
- Added the detector label:
  Xiumi operator depot item residue

Why this matters:
- Existing Xiumi guards already catch operator-dock parents, external edit panels, operation-panel
  loaders, selection overlays, Angular runtime attributes/classes, broad tn-* trees, hosted media,
  interaction styles, and applied SVG content layers.
- This slice covers the child-only path where copied DOM keeps operation item depots after broader
  dock wrappers have been cleaned.
- The rule stays anchored to Xiumi operation-item names and does not match generic slot, menu,
  bar, dc, or cp prose.

Regression coverage:
- WeChat: detectQuality(..., 'wechat') reports wechat-market-editor-residue with
  Xiumi operator depot item residue.
- Xiaohongshu: detectQuality(..., 'xiaohongshu') reports xhs-market-editor-residue with
  Xiumi operator depot item residue.
- Zhihu: detectQuality(..., 'zhihu') reports zhihu-market-editor-residue with
  Xiumi operator depot item residue.

TDD:
- Red:
  pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "operator depot item" --reporter=default
  Result: failed as expected because no market-editor-residue issue was emitted.
- Green:
  pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "operator depot item" --reporter=default
  Result: passed, 1 selected test, 228 skipped tests.

Verification:
- Slice diff:
  git diff --check -- <slice paths>
  Result: passed with only normal Windows LF-to-CRLF warnings.
- Focused file:
  pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts --reporter=default
  Result: passed, 1 file, 229 tests.
- Export suite:
  pnpm -C inkforge exec vitest run src/services/export --reporter=default --maxWorkers=1 --no-file-parallelism
  Result: passed, 36 files, 1206 tests.
- Targeted ESLint:
  pnpm -C inkforge exec eslint src/services/export/quality-detector.ts src/services/export/platform-export-rendering.test.ts --quiet
  Result: passed.
- Type check:
  pnpm -C inkforge exec vue-tsc --noEmit --pretty false
  Result: passed.
- Build:
  NODE_OPTIONS=--max-old-space-size=4096 pnpm -C inkforge build
  Result: passed, 4653 modules transformed, Vite built in 42.11s.
- Release preflight:
  pnpm -C inkforge style-proof:release-preflight --json
  Result: expected exit 1, status=blocked-by-external, canClaimComplete=false,
  externalHandoffRows=18, safeExternalRows=0, actionableLocalRows=0, nextRowRefs=5,
  uniqueNextRows=3.

Boundary:
- This proves local static residue detection only.
- It does not prove WeChat PC rich paste, WeChat phone preview, mobile SMIL/click interaction,
  mobile Dark Mode, cover thumbnail acceptance, credentialed sync, scheduled send, platform
  preview, public article rendering, XHS/Zhihu account upload, public host acceptance, or publish
  success.
- No local browser runtime directory, session secret, QR code, account capture, capture-file
  reference, traffic archive, or account artifact is included in this committed evidence.
