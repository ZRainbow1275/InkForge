Xiumi Template Scene Marker Residue - 2026-06-28

Scope:
- Local static publishability protection for copied or partially cleaned Xiumi v5 paper-editor
  template scene/list markers.
- No platform action, no phone preview, no sync, no upload, no publish.

Live DOM source:
- CloakBrowser reviewed the live Xiumi v5 paper editor DOM.
- The page exposed `tn-scene-paper` and `tn-lighting-box` around template-card lists,
  format-extraction menu entries, and editor projection surfaces.
- Minimal local probing showed single-token class values such as `class="tn-scene-paper"` and
  `class="tn-lighting-box"` only produced generic class/id warnings before this slice and could
  avoid the broad `Xiumi tn-* attribute` hard-block.

Implemented:
- Added the `Xiumi template scene marker residue` quality detector for class/id tokens
  `tn-scene-paper` and `tn-lighting-box`.
- Added a reduced regression fixture that keeps only those source-specific single class tokens,
  without `tn-tpl*`, `tn-from-house*`, renderer-pipeline bindings, broad Angular runtime
  attributes, hosted media, sidebar controls, or meta panels.

Regression coverage:
- WeChat, Xiaohongshu, and Zhihu all report the precise `Xiumi template scene marker residue`
  market-editor issue.
- The WeChat assertion verifies the reduced fixture does not fall through to
  `Xiumi template authoring tree residue`, `Xiumi source-house authoring residue`, or
  `Xiumi tn-* attribute`.

Verification:
- pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "template scene markers" --reporter=default
  RED: 1 selected test failed before the detector because no market-editor-residue issue was emitted.
- pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "template scene markers|template authoring tree|source-house authoring|basic style fragment" --reporter=default
  PASS: 4 selected tests / 286 skipped tests.
- pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts --reporter=default --testTimeout=60000
  PASS: 1 file / 290 tests.
- pnpm -C inkforge exec vitest run src/services/export --reporter=default --maxWorkers=1 --no-file-parallelism --testTimeout=60000
  PASS: 36 files / 1267 tests.
- pnpm -C inkforge exec eslint src/services/export/quality-detector.ts src/services/export/platform-export-rendering.test.ts --quiet
  PASS.
- pnpm -C inkforge exec vue-tsc --noEmit --pretty false
  PASS.
- NODE_OPTIONS=--max-old-space-size=4096 pnpm -C inkforge build
  PASS: 4653 modules transformed; Vite build completed in 34.13s.
- pnpm -C inkforge style-proof:release-preflight --json
  EXPECTED BLOCK: exited 1 with status=blocked-by-external, canClaimComplete=false,
  externalHandoffRows=18, safeExternalRows=0, actionableLocalRows=0, nextRowRefs=5,
  uniqueNextRows=3.

Boundary:
- This proves local static publishability protection only.
- It does not prove WeChat paste, phone preview, mobile SMIL/click interaction, mobile Dark Mode,
  cover thumbnail acceptance, credentialed sync, scheduled send, platform preview, public article
  rendering, XHS/Zhihu account upload, public host acceptance, or publish success.
