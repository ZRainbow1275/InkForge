Xiumi Text Operation Section Residue - 2026-06-29

Scope:
- Local static publishability protection for copied or partially cleaned Xiumi v5 editor HTML.
- No platform action, no phone preview, no sync, no upload, no scheduled send, no publish.

Observed source family:
- Live Xiumi v5 paper-editor DOM readback exposed editor-side text operation section markers:
  - op-text-sec

Implemented:
- Extended the existing detector label:
  Xiumi text toolbar control residue
- Added a reduced regression fixture containing only op-text-sec.
- The rule is anchored to the Xiumi-specific op-text-sec class/id marker. It does not block
  ordinary text wording, toolbar wording, CSS font properties, article typography prose, generic
  text sections, or non-Xiumi class names by itself.

Regression coverage:
- WeChat, Xiaohongshu, and Zhihu quality reports now emit their platform-specific
  market-editor-residue issue ids for the reduced text operation section fixture.
- Adjacent regressions keep color selector, color selector class, font/format, text toolbar, text
  editing flyout, and brush panel residue labels independent.

Verification:
- Red:
  pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "text operation sections" --reporter=default
  FAIL: 1 selected test failed because no wechat-market-editor-residue issue was emitted.
- Green:
  pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "text operation sections|text toolbar controls|text editing flyout|font and format controls|color selector controls|color selector class controls" --reporter=default
  PASS: 1 file / 7 selected tests passed / 329 skipped tests.
- Full platform-rendering regression:
  pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts --reporter=default --testTimeout=90000
  PASS: 1 file / 336 tests.
- Full export service regression:
  pnpm -C inkforge exec vitest run src/services/export --reporter=default --maxWorkers=1 --no-file-parallelism --testTimeout=90000
  PASS: 36 files / 1313 tests.
- Lint:
  pnpm -C inkforge exec eslint src/services/export/quality-detector.ts src/services/export/platform-export-rendering.test.ts --quiet
  PASS.
- Type check:
  pnpm -C inkforge exec vue-tsc --noEmit --pretty false
  PASS.
- Production build:
  NODE_OPTIONS=--max-old-space-size=4096 pnpm -C inkforge build
  PASS: 4653 modules transformed; Vite built in 34.15s.
- Release preflight:
  pnpm -C inkforge style-proof:release-preflight --json
  EXPECTED BLOCK: exit 1 with canClaimComplete=false, status=blocked-by-external, blockerKinds
  phone-preview, external-dependency, unsafe-to-automate, and mutating-platform.

Boundary:
- This evidence proves local static publishability protection and diagnostic precision only.
- It does not prove WeChat PC paste, WeChat phone preview, typography fidelity, Dark Mode,
  cover thumbnail acceptance, credentialed sync, scheduled send, platform preview,
  public article rendering, public-host acceptance, XHS/Zhihu account upload, or publish success.
