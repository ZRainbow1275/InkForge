Xiumi Text Cell Class Residue - 2026-06-25

Scope:
- Convert the 2026-06-25 Xiumi applied SVG/style DOM readback into one additional static
  publishability blocker.
- No browser profile, screenshot, platform URL, account label, auth secret, QR payload, HAR
  payload, raw template source, media URL, save action, preview action, upload action, sync
  action, or publish action is recorded here.

Observed source:
- The sanitized Xiumi applied editor readback listed text-cell authoring state including
  `tn-text`, `tn-link`, `tn-animate`, editable cells, Angular bindings, runtime bindings, and
  component wrappers.
- Existing rules already blocked `contenteditable`, `tn-link` attributes, `tn-animate` attributes,
  Angular/runtime bindings, and broad component wrappers.
- A cleaned fragment that retained only the `tn-text` class remained worth blocking independently
  because it is still Xiumi editor text-cell state, not an InkForge-owned text span.

Implemented:
- Added a `MARKET_EDITOR_RESIDUE_RULES` entry labeled `Xiumi text cell class residue`.
- Added a reduced regression fixture with only the `tn-text` class and readable text, intentionally
  without `tn-cell`, `tn-link`, `tn-animate`, `contenteditable`, `ng-*`, `opera-*`, hosted-media,
  SVG content-layer, or `foreignObject` markers.

TDD evidence:
- Red run before implementation:
  `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "text cell classes" --reporter=default`
  failed because `wechat-market-editor-residue` was absent for the reduced fixture.
- Green run after implementation:
  `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "text cell classes" --reporter=default`
  passed with 1 selected test and 179 skipped tests.
- Full platform rendering regression:
  `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts --reporter=default`
  passed with 1 file and 180 tests.
- Full export service serial regression:
  `pnpm -C inkforge exec vitest run src/services/export --reporter=default --maxWorkers=1 --no-file-parallelism`
  passed with 36 files and 1157 tests.
- Targeted lint, `vue-tsc --noEmit`, and production build passed.
- Release preflight remained correctly blocked with `canClaimComplete=false`,
  `status=blocked-by-external`, `externalHandoffRows=18`, `safeExternalRows=0`,
  `nextRowRefs=5`, and `uniqueNextRows=3`.

Boundary:
- This is static market-editor residue protection only.
- It does not prove WeChat editor paste, phone preview, mobile SVG/SMIL interaction, Dark Mode,
  cover thumbnail acceptance, credentialed sync, scheduled send, platform preview, public
  rendering, public-host acceptance, XHS/Zhihu upload, or publish success.
