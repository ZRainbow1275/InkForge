135 SVG Sidebar Icon Asset Residue - 2026-06-27

Scope:
- Local static publishability protection for WeChat, Xiaohongshu, and Zhihu output.
- No platform action, phone preview, sync, upload, scheduled send, or publish action.

Source:
- CloakBrowser review of the logged-in 135 SVG editor left sidebar found relative editor chrome
  icon resources in the form:
  img/sidebar-*.png
- The reduced live marker used for local regression is:
  img/sidebar-work-active.1e2c6eb1.png
- These resources describe 135 SVG editor sidebar icons. They are not article content and must not
  appear in publishable InkForge output.
- This evidence file records only reduced asset-marker facts and local validation results. It
  contains no account/session/runtime artifacts, request archives, QR material, screenshots,
  local paths, platform artifact, sync artifact, or publish artifact.

Implementation:
- Added MARKET_EDITOR_135_SVG_SIDEBAR_ICON_ASSET_RESIDUE_HTML as a reduced regression fixture.
- Added the quality detector label:
  135 SVG sidebar icon asset residue
- The detector is anchored to relative img/sidebar-*.png paths. It does not treat generic data
  images, generic image paths, or prose about sidebar/icon/image wording as residue.

Regression contract:
- WeChat must emit wechat-market-editor-residue with 135 SVG sidebar icon asset residue.
- Xiaohongshu must emit xhs-market-editor-residue with 135 SVG sidebar icon asset residue.
- Zhihu must emit zhihu-market-editor-residue with 135 SVG sidebar icon asset residue.
- The reduced fixture must not emit 135 SVG sidebar icon/help residue or
  135 SVG sidebar navigation residue.

Verification:
- Red:
  pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "sidebar icon asset" --reporter=default
  Result: failed with 1 selected failing test because no market-editor-residue issue was emitted.
- Green:
  pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "sidebar icon asset" --reporter=default
  Result: PASS, 1 selected test / 254 skipped tests.
- Focused Mermaid rerun after an unrelated full-file timeout:
  pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "routes WeChat markdown Mermaid" --reporter=default
  Result: PASS, 1 selected test / 254 skipped tests.
- Full platform export rendering:
  pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts --reporter=default
  Result: PASS, 1 file / 255 tests.
- Serial export suite:
  pnpm -C inkforge exec vitest run src/services/export --reporter=default --maxWorkers=1 --no-file-parallelism
  Result: PASS, 36 files / 1232 tests.
- Targeted lint:
  pnpm -C inkforge exec eslint src/services/export/quality-detector.ts src/services/export/platform-export-rendering.test.ts --quiet
  Result: PASS.
- Type check:
  pnpm -C inkforge exec vue-tsc --noEmit --pretty false
  Result: PASS.
- Production build:
  NODE_OPTIONS=--max-old-space-size=4096 pnpm -C inkforge build
  Result: PASS, 4653 modules transformed, Vite built in 57.42s.
- Release preflight:
  pnpm -C inkforge style-proof:release-preflight --json
  Result: expected exit 1 with status=blocked-by-external, canClaimComplete=false,
  externalHandoffRows=18, safeExternalRows=0, actionableLocalRows=0, nextRowRefs=5,
  uniqueNextRows=3.

Boundary:
- This proves only local static publishability protection and cannot-claim enforcement for this
  residue class.
- It does not prove WeChat PC paste, phone preview, mobile SMIL/click interaction, mobile Dark
  Mode, cover thumbnail acceptance, credentialed sync, scheduled send, platform preview, public
  article rendering, public-host acceptance, Xiaohongshu/Zhihu account upload, or publish success.
