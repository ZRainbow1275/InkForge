Xiumi UI Bootstrap Modal Directive Residue Gate - 2026-06-29

Scope:
- CloakBrowser-only sampling on the live Xiumi v5 paper editor.
- Local static publishability protection for WeChat, Xiaohongshu, and Zhihu export output.
- No save, copy, sync, upload, preview, publish, account-state artifact, local browser runtime
  material, template source, material source, or account text is committed.

Live observation:
- The Xiumi v5 paper editor exposed UI Bootstrap modal directive chrome.
- Observed source-specific marker: `uib-modal-animation-class`.
- This marker is modal animation plumbing for editor UI. It is not article content and is separate
  from dropdown, tooltip, tab, button-state, Angular/Vue, `tn-*`, operation-panel, hosted-media,
  and template markers.

Implemented:
- Expanded the existing `Xiumi UI Bootstrap control directive residue` detector to cover
  `uib-modal-*`.
- Added a reduced fixture with only `uib-modal-animation-class`.
- The detector remains anchored to a real `uib-*` attribute and does not block ordinary modal
  prose.

Verification:
- Red:
  `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "UI Bootstrap modal directives" --reporter=default`
  failed with 1 selected failing test before the detector update because no
  `*-market-editor-residue` issue was emitted.
- Green:
  the same command passed with 1 selected test and 365 skipped tests.
- Adjacent:
  `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "UI Bootstrap (directives|tab content directives|button directives|modal directives)" --reporter=default`
  passed with 4 selected tests and 362 skipped tests.
- Platform regression:
  `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts --reporter=default --test-timeout=90000`
  passed with 1 file and 366 tests.
- Export service regression:
  `pnpm -C inkforge exec vitest run src/services/export --reporter=default --maxWorkers=1 --no-file-parallelism --test-timeout=90000`
  passed with 36 files and 1343 tests.
- Static checks:
  `pnpm -C inkforge exec eslint src/services/export/quality-detector.ts src/services/export/platform-export-rendering.test.ts --quiet`
  passed.
  `pnpm -C inkforge exec vue-tsc --noEmit --pretty false`
  passed.
  `NODE_OPTIONS=--max-old-space-size=4096 pnpm -C inkforge build`
  passed; the build transformed 4653 modules and completed in 26.47s.
- Release preflight:
  `pnpm -C inkforge style-proof:release-preflight --json`
  exited 1 as expected with `canClaimComplete=false`, `status=blocked-by-external`, blocker kinds
  `phone-preview`, `external-dependency`, `unsafe-to-automate`, and `mutating-platform`,
  plus summary `blockerCount=4`, `combinedIssueCount=11`, `cannotClaimSteps=29`,
  `phoneOpenSteps=4`, `externalDependencyOpenSteps=14`, `unsafeToAutomateOpenSteps=13`,
  `mutatingOpenSteps=13`, `externalHandoffRows=18`, `safeExternalRows=0`,
  `actionableLocalRows=0`, `nextRowRefs=5`, and `uniqueNextRows=3`.

Boundary:
- This proves local static detection only.
- It does not prove WeChat paste, phone preview, modal fidelity, mobile interaction, Dark Mode,
  upload, credentialed sync, scheduled send, public rendering, public-host acceptance,
  XHS/Zhihu account upload, or publish success.
