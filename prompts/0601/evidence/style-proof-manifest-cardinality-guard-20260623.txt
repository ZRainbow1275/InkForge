Style Proof Manifest Cardinality Guard Report - 2026-06-23

Scope:
- Local manifest intake cardinality guard for redacted StyleProofManifest packs.
- No renderer output, no browser action, no phone preview, no sync, no upload, no scheduled send,
  no platform preview, no public rendering, and no publish action.

Implemented:
- STYLE_PROOF_MANIFEST_INTAKE_MAX_MANIFESTS = 128
- STYLE_PROOF_MANIFEST_INTAKE_MAX_ARTIFACTS = 512

Behavior:
- Rejects root manifest packs above 128 manifests before parsing individual entries.
- Rejects per-manifest artifact arrays above 512 artifacts before parsing individual artifacts.
- Returns normal StyleProofManifestIntakeReport objects for oversized inputs instead of throwing.
- Oversized root packs report style-proof-manifest-intake-manifest-count-too-large.
- Oversized artifact arrays report style-proof-manifest-intake-artifact-count-too-large.
- Does not truncate oversized inputs into partial accepted proof.
- Does not change style availability, style selection, selectable, usable, blocked, or unavailable
  decisions.

Regression coverage:
- Oversized root manifest packs keep inputManifestCount=0, acceptedManifestCount=0, and
  canClaimComplete=false.
- Oversized artifact arrays reject the manifest, keep acceptedManifestCount=0, and keep
  canClaimComplete=false.

Verification:
- pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "manifest intake|JSON intake|style proof manifest" --reporter=default
  PASS: 1 file / 10 selected tests
- pnpm -C inkforge exec eslint src/services/export/style-catalog.ts src/services/export/platform-export-rendering.test.ts --quiet
  PASS
- pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts --reporter=default
  PASS: 1 file / 168 tests
- pnpm -C inkforge exec vitest run src/services/export --reporter=default --maxWorkers=1 --no-file-parallelism
  PASS: 36 files / 1145 tests
- pnpm -C inkforge exec vue-tsc --noEmit --pretty false
  PASS
- NODE_OPTIONS='--max-old-space-size=4096' pnpm -C inkforge build
  PASS: 4653 modules transformed, Vite built in 36.09s
- GitNexus detect_changes(scope=staged)
  PASS: low risk, 7 staged files, 9 changed symbols, 0 affected processes

Boundary:
- This proves local proof-handoff resource protection only.
- It does not prove WeChat official editor paste, phone preview, mobile SMIL/click interaction,
  mobile Dark Mode, cover thumbnail acceptance, credentialed sync, public-host acceptance,
  Xiaohongshu account upload, Zhihu account upload, scheduled send, platform preview, public
  rendering, or publish success.
