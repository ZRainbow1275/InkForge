Xiumi Account Sync Panel Residue - 2026-06-29

Scope:
- Static publishability gate for copied or partially cleaned Xiumi v5 paper-editor
  account/authorization/sync dropdown panel controls.
- Applies to WeChat, Xiaohongshu, and Zhihu quality reports.
- No platform action, phone preview, sync, upload, schedule, or publish action was performed.

Live source:
- CloakBrowser reviewed the logged-in Xiumi v5 paper editor DOM.
- The editor exposed an account/authorization/sync dropdown panel with the source-specific class
  marker `wx-user-panel`.
- The node appeared around account authorization, material sync, comment-permission, and preview
  option controls. No account name, credential value, QR, screenshot, or local browser runtime
  material is part of this committed evidence.

Implementation:
- Added the `Xiumi account sync panel residue` detector label.
- Added a reduced regression fixture containing only `wx-user-panel` plus generic dropdown
  context, without `usr-info`, `user-info-toggle`, dropdown directives, generated-link controls,
  operation-bar menus, account names, platform credentials, Angular runtime attributes, hosted
  media, sidebar controls, or meta panels.
- Kept the rule anchored to class/id attributes, not prose.
- Kept adjacent detector labels independent from `Xiumi dropdown directive residue`,
  `Xiumi generated link control residue`, and `Xiumi menu pin control residue`.

TDD:
- Red:
  `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "account sync panel controls" --reporter=default`
  failed before the detector update because no `*-market-editor-residue` issue was emitted.
- Green:
  `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "account sync panel controls|dropdown directives|generated link controls|menu pin controls" --reporter=default`
  passed with 4 selected tests and 299 skipped tests after the detector update.

Full verification:
- `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts --reporter=default --testTimeout=60000`
  passed with 1 file and 303 tests.
- `pnpm -C inkforge exec vitest run src/services/export --reporter=default --maxWorkers=1 --no-file-parallelism --testTimeout=60000`
  passed with 36 files and 1280 tests.
- `pnpm -C inkforge exec eslint src/services/export/quality-detector.ts src/services/export/platform-export-rendering.test.ts --quiet`
  passed.
- `pnpm -C inkforge exec vue-tsc --noEmit --pretty false` passed.
- `NODE_OPTIONS=--max-old-space-size=4096 pnpm -C inkforge build` passed with 4653 modules
  transformed and Vite build completed in 48.54s.
- `pnpm -C inkforge style-proof:release-preflight --json` exited 1 as expected with
  `status=blocked-by-external`, `canClaimComplete=false`,
  `blockerKinds=["phone-preview","external-dependency","unsafe-to-automate","mutating-platform"]`,
  `externalHandoffRows=18`, `safeExternalRows=0`, `actionableLocalRows=0`, `nextRowRefs=5`,
  and `uniqueNextRows=3`.

Boundary:
- This is static publishability protection only.
- It does not prove WeChat PC paste, phone preview, mobile SMIL/click interaction, mobile Dark
  Mode, cover thumbnail acceptance, credentialed sync, scheduled send, platform preview, public
  article rendering, public-host acceptance, XHS/Zhihu account upload, or publish success.
