Style Proof Required Artifact Fingerprint Contract - 2026-06-19

Scope:
- Local style-proof manifest validator and acceptance-audit contract hardening.
- No platform action, no account upload, no phone preview, no sync, no send, no publish.

Implemented:
- Added a generic artifactFingerprint contract layer for every StyleProofExecutionArtifactContract that lists artifactFingerprint in requiredFields.
- The validator now checks matching action/channel proof rows and rejects them unless at least one matching row carries a non-empty artifactFingerprint.
- Existing specialized validators keep their precise exact-artifact checks; the generic layer only fills the common fingerprint traceability gap and skips duplicate exact-artifact issues.

Regression coverage:
- phone-screenshot with exactArtifact:true and phone content proof but no artifactFingerprint remains invalid.
- credentialed-channel-response with exactArtifact:true and authenticated external account proof but no artifactFingerprint remains invalid.
- scheduled-send-readback with scheduledSendVerified:true but no artifactFingerprint remains invalid.
- published-url-or-platform-preview with exactArtifact:true but no artifactFingerprint remains invalid.
- Positive phone-state fixtures now carry redacted artifact fingerprints so tests distinguish mobile-state failures from traceability failures.

Verification:
- pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts --reporter=default
  PASS: 1 file / 119 tests
- pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts src/services/export/__tests__/pipeline-cross-platform.test.ts src/services/export/xhs.test.ts src/services/export/zhihu.test.ts --reporter=default
  PASS: 4 files / 158 tests
- pnpm -C inkforge exec vitest run src/services/export --reporter=default --maxWorkers=1 --no-file-parallelism
  PASS: 35 files / 1092 tests
- pnpm -C inkforge exec eslint src/services/export/style-catalog.ts src/services/export/platform-export-rendering.test.ts --quiet
  PASS
- pnpm -C inkforge exec vue-tsc --noEmit --pretty false
  PASS
- NODE_OPTIONS=--max-old-space-size=4096 pnpm -C inkforge build
  PASS: Vite built in 35.43s; generated inkforge/tsconfig.tsbuildinfo was restored after validation.

Boundary:
- This proves local required-field accounting and cannot-claim enforcement only.
- It does not prove WeChat phone preview, mobile SMIL/click, mobile Dark Mode, cover thumbnail acceptance, credentialed sync, scheduled send, platform preview, public rendering, XHS/Zhihu account upload, or publish success.
