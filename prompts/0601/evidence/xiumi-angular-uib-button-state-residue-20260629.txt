Xiumi Angular/UI Bootstrap Button State Residue - 2026-06-29

Scope:
- Local static publishability protection for Angular/UI Bootstrap button-state directives observed
  on the live Xiumi v5 paper editor surface.
- Covered attributes: ng-checked, uib-btn-radio, uib-btn-checkbox.
- No platform action, no phone preview, no sync, no upload, no publish, no account artifact, no
  browser runtime artifact, no capture-location reference, no QR artifact, and no credential
  material.

Implemented:
- Extended the existing Angular/Vue authoring attribute detector with checked.
- Extended the existing Xiumi UI Bootstrap control directive detector with
  uib-btn-radio and uib-btn-checkbox.
- Added reduced regression fixtures that contain only the button-state directive attributes.

Behavior:
- WeChat, Xiaohongshu, and Zhihu quality checks report Angular/Vue authoring attribute for the
  reduced ng-checked fixture.
- WeChat, Xiaohongshu, and Zhihu quality checks report Xiumi UI Bootstrap control directive
  residue for the reduced uib-btn-radio / uib-btn-checkbox fixture.
- Angular authoring class stays absent for the reduced ng-checked fixture.
- Xiumi dropdown directive residue stays absent for the reduced UI Bootstrap button fixture.
- The rule does not treat ordinary checkbox/radio prose, checked wording, button wording,
  option prose, or Xiumi prose as residue by itself.

Regression coverage:
- Class-cleaned Angular checked residue cannot pass publishability checks.
- Class-cleaned UI Bootstrap button-state residue cannot pass publishability checks.
- Adjacent UI Bootstrap dropdown and Angular link/dropzone diagnostics remain independent.
- The change reuses existing Angular/Vue and UI Bootstrap labels instead of adding broad any-ng-*
  or any-uib-* detectors.

Verification:
- pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "button directives|checked attributes" --reporter=default
  RED: 2 selected failing tests before the detector update; no *-market-editor-residue issue was emitted.
- pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "button directives|checked attributes|UI Bootstrap directives|dropdown directives|Angular link and dropzone" --reporter=default
  PASS: 1 file / 5 selected tests / 350 skipped tests after the detector update.
- pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts --reporter=default --testTimeout=90000
  PASS: 1 file / 355 tests.
- pnpm -C inkforge exec vitest run src/services/export --reporter=default --maxWorkers=1 --no-file-parallelism --testTimeout=90000
  PASS: 36 files / 1332 tests.
- pnpm -C inkforge exec eslint src/services/export/quality-detector.ts src/services/export/platform-export-rendering.test.ts --quiet
  PASS.
- pnpm -C inkforge exec vue-tsc --noEmit --pretty false
  PASS.
- NODE_OPTIONS=--max-old-space-size=4096 pnpm -C inkforge build
  PASS: 4653 modules transformed; built in 27.93s.
- pnpm -C inkforge style-proof:release-preflight --json
  EXPECTED BLOCKED: exit 1; canClaimComplete=false; status=blocked-by-external; blocker kinds
  phone-preview, external-dependency, unsafe-to-automate, mutating-platform; externalHandoffRows=18;
  safeExternalRows=0; actionableLocalRows=0; nextRowRefs=5; uniqueNextRows=3.

Boundary:
- This proves local static publishability protection and diagnostic coverage only.
- It does not prove WeChat PC paste, phone preview, form-control fidelity, upload, cover thumbnail
  acceptance, mobile Dark Mode, mobile SMIL/click interaction, credentialed sync, scheduled send,
  public rendering, public-host acceptance, XHS/Zhihu account upload, or publish success.
