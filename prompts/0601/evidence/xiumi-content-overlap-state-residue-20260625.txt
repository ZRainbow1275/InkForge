Xiumi Content-Overlap State Residue - 2026-06-25

Scope:
- Static publishability protection for copied or partially cleaned Xiumi applied-editor
  content-overlap state residue.
- No browser action, platform paste, phone preview, sync, upload, scheduled send, or publish
  action was performed by this slice.

Implemented:
- Added a `MARKET_EDITOR_RESIDUE_RULES` entry labeled
  `Xiumi content-overlap state residue`.
- The rule matches class/id attributes containing `tn-content-overlap`.
- Removed `tn-content-overlap` from the generic `Xiumi SVG gallery state wrapper residue`
  alternation so content-overlap state is diagnosed with a source-specific label while other
  gallery state wrappers remain blocked.

Reduced regression fixture:
- Contains only a normal section with `<div class="tn-content-overlap">Xiumi content overlap
  state residue</div>`.
- Intentionally omits broader `tn-*`, `ng-*`, `opera-*`, `contenteditable`, hosted-media,
  SVG content-layer, `tn-page-vessel`, `tn-group-sortable-box`, `tn-image-inst-wrapper`,
  `tn-overflow-hidden`, `ui-slider`, `ui-sortable`, `op-loader`, `touch-action`,
  `user-select`, `pointer-events`, `visibility:hidden`, and `foreignObject` markers.

Verification:
- TDD red:
  `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t
  "content-overlap state" --reporter=default`
  Failed before implementation because the market residue message reported
  `Xiumi SVG gallery state wrapper residue`, not `Xiumi content-overlap state residue`.
- Focused green:
  `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t
  "content-overlap state" --reporter=default`
  PASS: 1 selected test / 184 skipped tests.
- Platform rendering regression:
  `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts
  --reporter=default`
  PASS: 1 file / 185 tests.
- Export service regression:
  `pnpm -C inkforge exec vitest run src/services/export --reporter=default --maxWorkers=1
  --no-file-parallelism`
  PASS: 36 files / 1162 tests.
- Lint:
  `pnpm -C inkforge exec eslint src/services/export/quality-detector.ts
  src/services/export/platform-export-rendering.test.ts --quiet`
  PASS.
- Type check:
  `pnpm -C inkforge exec vue-tsc --noEmit --pretty false`
  PASS.
- Build:
  `NODE_OPTIONS=--max-old-space-size=4096 pnpm -C inkforge build`
  PASS: 4653 modules transformed, Vite built in 24.70s.
- Build cleanup:
  `git restore -- inkforge/tsconfig.tsbuildinfo`
  PASS: no tracked build-info diff remained.
- Release preflight:
  `pnpm -C inkforge style-proof:release-preflight -- --json`
  Expected exit 1 with `status=blocked-by-external`, `canClaimComplete=false`,
  `externalHandoffRows=18`, `safeExternalRows=0`, `nextRowRefs=5`, and `uniqueNextRows=3`.

Boundary:
- This proves static residue detection and label precision only.
- It does not prove WeChat paste, phone preview, mobile SMIL/click interaction, Dark Mode,
  cover thumbnail acceptance, credentialed sync, scheduled send, platform preview, public
  rendering, Xiaohongshu upload, Zhihu public-host acceptance, or publish success.
