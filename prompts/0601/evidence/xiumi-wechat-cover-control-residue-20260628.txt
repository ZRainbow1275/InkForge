Xiumi WeChat Cover Control Residue - 2026-06-28

Source:
- CloakBrowser reviewed the live Xiumi v5 paper editor after restart using the existing logged-in
  browser context.
- The editor control DOM exposed `op-ce-wx-cover` with cover-selection text equivalent to choosing
  a cover from the left gallery.
- Co-observed controls included generated-link, formatting, scale, icon, mask, and toolbar classes,
  but the reduced regression isolates `op-ce-wx-cover` so the rule does not depend on those
  broader markers.
- No account-state material, local browser runtime material, capture-file reference, platform
  publish artifact, export artifact, copy artifact, sync artifact, preview artifact, QR artifact,
  credential secret, or local browser directory is part of this evidence.

Implemented:
- Added `Xiumi WeChat cover control residue` to `MARKET_EDITOR_RESIDUE_RULES`.
- Added a reduced three-platform regression fixture containing only `op-ce-wx-cover` and an inert
  `op-dark-mask` child.

Verification:
- Red:
  `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "WeChat cover controls" --reporter=default`
  failed before the detector update because no market-editor-residue issue was emitted.
- Green:
  `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "WeChat cover controls" --reporter=default`
  passed after the detector update with 1 selected test and 271 skipped tests.
- Full platform file:
  `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts --reporter=default --testTimeout=60000`
  passed with 1 file and 272 tests.
- Export serial suite:
  `pnpm -C inkforge exec vitest run src/services/export --reporter=default --maxWorkers=1 --no-file-parallelism --testTimeout=60000`
  passed with 36 files and 1249 tests.
- Targeted ESLint:
  `pnpm -C inkforge exec eslint src/services/export/quality-detector.ts src/services/export/platform-export-rendering.test.ts --quiet`
  passed.
- Type check:
  `pnpm -C inkforge exec vue-tsc --noEmit --pretty false`
  passed.
- Production build:
  `NODE_OPTIONS=--max-old-space-size=4096 pnpm -C inkforge build`
  passed with 4653 modules transformed and Vite built in 38.37s.
- Release preflight:
  `pnpm -C inkforge style-proof:release-preflight --json`
  exited 1 as expected with `status=blocked-by-external`, `canClaimComplete=false`,
  `externalHandoffRows=18`, `safeExternalRows=0`, `actionableLocalRows=0`, `nextRowRefs=5`, and
  `uniqueNextRows=3`.

Boundary:
- This is static publishability protection only.
- It does not prove WeChat PC paste, phone preview, mobile interaction, mobile Dark Mode, cover
  thumbnail acceptance, credentialed sync, scheduled send, platform preview, public article
  rendering, public-host acceptance, XHS/Zhihu account upload, or publish success.
