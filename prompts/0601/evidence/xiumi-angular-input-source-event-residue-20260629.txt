Xiumi Angular Input Source Event Residue - 2026-06-29

Scope:
- Local static publishability protection for copied or partially cleaned Xiumi editor fragments.
- Covers Angular authoring attributes observed in the live Xiumi v5 editor surface:
  ng-keydown, ng-keyup, ng-src, ng-mousedown, ng-mouseup, ng-mouseenter, ng-copy, ng-readonly, and
  ng-transclude.
- No platform action, phone preview, sync, upload, scheduled send, or publish action is included.

Implementation:
- Added a reduced three-platform regression fixture containing only input/source/mouse/copy/
  transclusion Angular attributes.
- Extended the existing Angular/Vue authoring attribute detector to include those live attributes.
- The fixture avoids Angular runtime classes, tn-* markers, opera-tn-* bindings, contenteditable,
  hosted media, sidebar controls, and meta panels, so the diagnostic is proven independently.

TDD:
- Red:
  pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "Angular input source and mouse event" --reporter=default
  Result: failed with 1 selected failing test because no market-editor-residue issue was emitted.
- Green:
  pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "Angular input source and mouse event|Angular runtime controls" --reporter=default
  Result: passed with 2 selected tests and 347 skipped tests.

Verification:
- pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts --reporter=default --test-timeout=90000
  PASS: 1 file / 349 tests.
- pnpm -C inkforge exec vitest run src/services/export --reporter=default --maxWorkers=1 --no-file-parallelism --test-timeout=90000
  PASS: 36 files / 1326 tests.
- pnpm -C inkforge exec eslint src/services/export/quality-detector.ts src/services/export/platform-export-rendering.test.ts --quiet
  PASS.
- pnpm -C inkforge exec vue-tsc --noEmit --pretty false
  PASS.
- NODE_OPTIONS=--max-old-space-size=4096 pnpm -C inkforge build
  PASS: 4653 modules transformed, built in 31.84s.
- pnpm -C inkforge style-proof:release-preflight --json
  EXPECTED BLOCK: exited 1 with canClaimComplete=false, status=blocked-by-external, blocker kinds
  phone-preview, external-dependency, unsafe-to-automate, and mutating-platform.

Boundary:
- This proves local static residue detection only.
- It does not prove WeChat PC paste, phone preview, input behavior fidelity, mobile SMIL/click
  interaction, Dark Mode, cover thumbnail acceptance, credentialed sync, scheduled send, public
  rendering, public-host acceptance, XHS/Zhihu account upload, or publish success.
