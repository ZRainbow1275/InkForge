Market Editor CloakBrowser SVG Pipeline Residue Refresh - 2026-06-21

Scope:
- CloakBrowser-only live browser learning pass for 135 SVG editor and Xiumi v5 paper editor.
- No save, export, sync, upload, scheduled send, publish, phone preview, screenshot capture, HAR,
  credential entry, account artifact, or browser profile artifact.

135 live observations:
- Opened the live 135 SVG editor and used the existing persistent InkForge browser profile.
- The left SVG effect library exposed behavior families such as click, carousel, slide, auto,
  expand, long-press, layout, public-account, mini-program, and other effects.
- A visible `免费试用` button was clicked.
- The center editor was readable through `.editor-content`.
- The center editor contained 12 SVG nodes, 2 section wrappers, 39 inline-style nodes, and 264
  clickable/authoring candidates.
- Material-included background SVG layers used a tall `viewBox="0 0 1080 1920"` plus inline style
  fields including `background-attachment`, `background-position`, `background-repeat`,
  `background-size:100.1% 100.1%`, `margin-top:-1px`, `pointer-events:none`, `user-select:none`,
  `vertical-align:top`, `width:100%`, and the 135-specific `svg:135` marker.
- The observed section wrapper used zero font/line-height and zero margin/padding to remove gaps.

Xiumi live observations:
- Opened the live Xiumi v5 paper editor and cancelled the unsaved-draft recovery prompt to avoid
  mutating account/editor state.
- The editor exposed left taxonomy entries for title, card, image, layout, SVG, component, hot
  templates, recommended templates, and recent templates.
- The export menu exposed distinct credentialed/non-credentialed paths: preview, save, export,
  sync to official account, plugin copy, continue copy/paste, and copy to WeChat.
- Hidden SVG/animation controls exposed behavior families such as lottery scroll, grid switch,
  rotate, flip, frame sequence, click, single-step click, auto, long-press, speed category,
  carousel item interaction, and SVG animation assistant.
- The visible template list exposed Xiumi renderer pipeline markers including
  `tplLib.onTemplateClicked`, `tpl2BoxClasses`, `tpl2PresentType`, `tn-tpl-pose-fit-box`,
  `renderer_accelerate`, and `validateImageTypeInHtml`.

Implementation:
- Added `135 SVG background style marker` to market-editor residue detection.
- Added `Xiumi template renderer pipeline residue` to market-editor residue detection.
- Both detector labels are emitted through the existing platform-specific issues:
  `wechat-market-editor-residue`, `xhs-market-editor-residue`, and `zhihu-market-editor-residue`.

Verification:
- TDD first run failed:
  `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "135 SVG background style markers|Xiumi template renderer pipeline" --reporter=default`
  failed because the 135 style marker was unclassified and the Xiumi renderer pipeline was only
  reported through generic tn/Angular residue labels.
- After implementation, the same focused command passed: 1 file, 2 selected tests.
- Adjacent market-editor regression passed:
  `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "market editor|135 SVG|Xiumi" --reporter=default`
  passed 1 file, 18 selected tests.

Boundary:
- This is market-editor DOM learning and local detector enforcement only.
- It does not prove WeChat ordinary Ctrl+V rich HTML/SVG paste, phone preview, mobile interaction,
  mobile Dark Mode, cover thumbnail acceptance, credentialed sync, scheduled send, platform
  preview, public article rendering, XHS/Zhihu account upload, public-host acceptance, or publish
  success.
