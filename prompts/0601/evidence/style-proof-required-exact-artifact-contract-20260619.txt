Style Proof Required Exact Artifact Contract - 2026-06-19

Scope:
- Local style-proof manifest validator and acceptance-audit contract hardening.
- No platform action, no account upload, no phone preview, no sync, no send, no publish.

Implemented:
- Added a generic exactArtifact contract layer for every StyleProofExecutionArtifactContract that lists exactArtifact in requiredFields.
- The validator now checks matching action/channel proof rows and rejects them unless at least one matching row carries exactArtifact:true.
- Existing specialized validators keep their precise exact-artifact checks; the generic layer only fills the common exactArtifact binding gap and skips duplicate exact-artifact issues.

Regression coverage:
- phone-screenshot with artifactFingerprint and phone content proof but no exactArtifact:true remains invalid.
- credentialed-channel-response with artifactFingerprint and authenticated external account proof but no exactArtifact:true remains invalid.
- scheduled-send-readback with artifactFingerprint and scheduledSendVerified:true but no exactArtifact:true remains invalid.
- published-url-or-platform-preview with artifactFingerprint but no exactArtifact:true remains invalid.

Verification:
- pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts --reporter=default
  PASS: 1 file / 120 tests
- pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts src/services/export/__tests__/pipeline-cross-platform.test.ts src/services/export/xhs.test.ts src/services/export/zhihu.test.ts --reporter=default
  PASS: 4 files / 159 tests
- pnpm -C inkforge exec vitest run src/services/export --reporter=default --maxWorkers=1 --no-file-parallelism
  PASS: 35 files / 1093 tests
- pnpm -C inkforge exec eslint src/services/export/style-catalog.ts src/services/export/platform-export-rendering.test.ts --quiet
  PASS
- pnpm -C inkforge exec vue-tsc --noEmit --pretty false
  PASS
- NODE_OPTIONS=--max-old-space-size=4096 pnpm -C inkforge build
  PASS: Vite built in 29.30s; generated inkforge/tsconfig.tsbuildinfo was restored after validation.

Boundary:
- This proves local required-field accounting and cannot-claim enforcement only.
- It does not prove WeChat phone preview, mobile SMIL/click, mobile Dark Mode, cover thumbnail acceptance, credentialed sync, scheduled send, platform preview, public rendering, XHS/Zhihu account upload, or publish success.
