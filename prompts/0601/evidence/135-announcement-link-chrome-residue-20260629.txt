135 Announcement Link Chrome Residue Gate - 2026-06-29

Scope:
- CloakBrowser-only sampling on the live 135 ordinary editor.
- Local static publishability protection for WeChat, Xiaohongshu, and Zhihu export output.
- No save, copy, sync, upload, preview, publish, scan-code artifact, account-state artifact,
  local browser runtime material, template source, material source, or account text is committed.

Live observation:
- The 135 ordinary editor exposed announcement-strip link chrome around the editing surface.
- Observed source-specific markers included `class="announcement unread"` and
  `https://www.135editor.com/announcements/view/<id>`.
- These markers represent site notice links. They are not article content, article references, or
  reusable InkForge element source and are separate from full-page navigation chrome, helper iframe
  chrome, style-panel controls, style-card operation buttons, UEditor toolbar chrome, action rail
  chrome, hosted image sources, and SVG-builder markers.

Implemented:
- Added `135 announcement link chrome residue` to `MARKET_EDITOR_RESIDUE_RULES`.
- The detector matches only a real anchor tag with the observed `announcement unread` class pair
  or the 135 `/announcements/view/<id>` URL shape. It does not block ordinary announcement prose.
- Added a reduced regression fixture that contains readable article text plus 135 announcement
  link chrome. It intentionally omits `_135editor`, `data-tools`, `style_id`, `style_name`,
  `style_price`, full-page navigation chrome, helper iframe chrome, style-panel controls,
  style-card operation buttons, UEditor toolbar chrome, action rail chrome, hosted image sources,
  and SVG-builder markers.

Verification:
- Red:
  `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "announcement link chrome" --reporter=default`
  failed with 1 selected failing test before the detector update because no
  `*-market-editor-residue` issue was emitted.
- Green:
  `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "announcement link chrome" --reporter=default`
  passed with 1 selected test and 364 skipped tests.
- Adjacent market regression:
  `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "135 (style panel navigation chrome|full-page navigation chrome|helper iframe chrome|announcement link chrome|UEditor toolbar|editor action rail chrome)" --reporter=default`
  passed with 6 selected tests and 359 skipped tests.
- Platform regression:
  `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts --reporter=default --test-timeout=90000`
  passed with 1 file and 365 tests.
- Export service regression:
  `pnpm -C inkforge exec vitest run src/services/export --reporter=default --maxWorkers=1 --no-file-parallelism --test-timeout=90000`
  passed with 36 files and 1342 tests.
- Static checks and build:
  `pnpm -C inkforge exec eslint src/services/export/quality-detector.ts src/services/export/platform-export-rendering.test.ts --quiet`
  passed.
  `pnpm -C inkforge exec vue-tsc --noEmit --pretty false`
  passed.
  `NODE_OPTIONS=--max-old-space-size=4096 pnpm -C inkforge build`
  passed; Vite transformed 4653 modules and completed in 30.10 seconds.
- Release preflight:
  `pnpm -C inkforge style-proof:release-preflight --json`
  correctly exited 1 with `canClaimComplete=false`, `status=blocked-by-external`,
  blocker kinds `phone-preview`, `external-dependency`, `unsafe-to-automate`, and
  `mutating-platform`, plus `blockerCount=4`, `combinedIssueCount=11`,
  `cannotClaimSteps=29`, `phoneOpenSteps=4`, `externalDependencyOpenSteps=14`,
  `unsafeToAutomateOpenSteps=13`, `mutatingOpenSteps=13`, `externalHandoffRows=18`,
  `safeExternalRows=0`, `actionableLocalRows=0`, `nextRowRefs=5`, and `uniqueNextRows=3`.

Boundary:
- This proves local static detection of copied 135 announcement link chrome only.
- It does not prove WeChat PC paste, phone preview, link fidelity, mobile interaction fidelity,
  mobile Dark Mode, cover thumbnail acceptance, upload, credentialed sync, scheduled send,
  platform preview, public article rendering, public-host acceptance, Xiaohongshu account upload,
  Zhihu account upload, or publish success.
