# Evidence Log

## Environment

- Workspace: `D:\Desktop\Inkforge`
- App package: `D:\Desktop\Inkforge\inkforge`
- Task: `.trellis/tasks/05-12-full-app-functionality-audit-repair`
- Branch at task start: `dev/visual-fixes`
- Runtime commands from `inkforge/package.json`:
  - Web dev: `pnpm -C inkforge dev --host 127.0.0.1 --port 3005`
  - Typecheck: `pnpm -C inkforge exec vue-tsc --noEmit`
  - Non-mutating lint: `pnpm -C inkforge exec eslint src --ext .ts,.tsx,.vue --quiet`
  - Test: `pnpm -C inkforge exec vitest run --reporter=default`
  - Web build: `NODE_OPTIONS=--max-old-space-size=4096 pnpm -C inkforge build`
  - Tauri dev: `pnpm -C inkforge tauri:dev`
  - Tauri build: `pnpm -C inkforge tauri:build`

## Setup Evidence

| Time | Action | Result |
| --- | --- | --- |
| 2026-05-12 | Created task `.trellis/tasks/05-12-full-app-functionality-audit-repair` | Task status started as `planning`. |
| 2026-05-12 | Wrote `prd.md`, `research/verification-strategy.md`, `implement.jsonl`, `check.jsonl` | Trellis `task.py validate` passed with 8 implement entries and 6 check entries. |
| 2026-05-12 | User selected scope decisions | Dev/hidden/experimental entries are blocking; Tauri native runtime is blocking; external unconfigured services may pass only through honest unavailable behavior; execution remains one end-to-end task. |
| 2026-05-12 | Ran `python .\.trellis\scripts\task.py start .\.trellis\tasks\05-12-full-app-functionality-audit-repair` | Task status changed `planning -> in_progress`. |
| 2026-05-12 | Loaded `trellis-before-dev` | Applicable specs: frontend layer plus shared thinking guides. |
| 2026-05-12 | Initial static scope inspection | Confirmed routes, 37 Settings registry rows, DevPanel activation paths, built-in command palette commands, and desktop capability ids. |
| 2026-05-12 | Static Vue control extraction with `rg --count-matches '<button\|<input\|<select\|<textarea\|@click\|@submit\|@change\|@input\|@keydown\|role="button"' .\inkforge\src\views .\inkforge\src\components -g '*.vue'` | Found 53 Vue files with 1117 control/interaction markers. Detailed counts saved to `static-control-inventory.md`. |
| 2026-05-12 | Web route smoke: `/` at 1440x900 | Hub visible; 38 buttons, 2 inputs, 1 select; no horizontal overflow, no mojibake markers, no sample leak markers, console had Vite debug only. |
| 2026-05-12 | Web route smoke: `/settings?tab=appearance` at 1440x900 | Settings visible with only `appearance` tab displayed; all 12 nav tab buttons present; DOM retains 180 buttons, 73 inputs, 9 selects, 3 textareas due hidden `v-show` tabs; no horizontal overflow, mojibake, or sample markers. |
| 2026-05-12 | Web route smoke: `/workstation` empty state at 1440x900 | Workstation visible; no article selected; Copy and Export disabled; 31 buttons, 4 inputs, 2 selects; 0 ProseMirror and 0 tabs; no horizontal overflow, mojibake, or sample markers; console had no errors. |
| 2026-05-12 | Web route smoke: `/publish` empty state at 1440x900 | Publish visible; no selected article; download/code copy disabled; no sample content markers; found unlabeled `.back-btn` icon button. Finding `F-PUB-001`. |
| 2026-05-12 | GitNexus impact before `PublishView.vue` edit | `npx gitnexus impact PublishView -r Inkforge -d upstream --depth 2` returned LOW risk, impactedCount 0. |
| 2026-05-12 | Targeted lint after `F-PUB-001` fix | `pnpm -C inkforge exec eslint src/views/PublishView.vue --ext .vue --quiet` passed. |
| 2026-05-12 | Browser re-check after `F-PUB-001` fix | `/publish?audit=pub-back-a11y` at 1440x900 returned `unnamed: []`; `.back-btn` had `aria=返回工作台`, `title=返回工作台`, `type=button`; no horizontal overflow. |
| 2026-05-12 | Web route smoke: `/drafts` at 1440x900 | Drafts visible; 7 buttons, 1 input, 4 selects; `unnamed: []`; no horizontal overflow, mojibake, or sample leak markers; console had no errors. |
| 2026-05-12 | Web route smoke: `/themes` at 1440x900 | Themes visible; 4 buttons; found unlabeled `.back-btn` icon button. Finding `F-THEMES-001`. |
| 2026-05-12 | GitNexus impact before `ThemesView.vue` edit | `npx gitnexus impact ThemesView -r Inkforge -d upstream --depth 2` returned LOW risk, impactedCount 0. |
| 2026-05-12 | Targeted lint after `F-THEMES-001` fix | `pnpm -C inkforge exec eslint src/views/ThemesView.vue --ext .vue --quiet` passed. |
| 2026-05-12 | Browser re-check after `F-THEMES-001` fix | `/themes?audit=themes-back-a11y` at 1440x900 returned `unnamed: []`; `.back-btn` had `aria=返回上一页`, `title=返回上一页`, `type=button`; all four route buttons declared `type=button`; no horizontal overflow; console had no errors. |
| 2026-05-12 | Web route smoke: `/account` at 1440x900 | Account visible; 9 buttons, 2 inputs; `unnamed: []`; no horizontal overflow, mojibake, or sample leak markers; console had no errors; copy explicitly states local-only and no fake remote authentication. |
| 2026-05-12 | Web route smoke: unknown route `/no-such-page` at 1440x900 | NotFound visible; 1 recovery button; `unnamed: []`; no horizontal overflow, mojibake, or sample leak markers; console had no errors. |
| 2026-05-12 | NotFound recovery click | Clicking the only recovery button on `/no-such-page?audit=notfound-click` routed to `/` and rendered Hub without crash. |
| 2026-05-12 | Redirect smoke: `/editor`, `/client`, `/workspace` | Each compatibility route redirected to `/workstation?audit=redirect-smoke`; Workstation rendered with 31 buttons, `unnamed: []`, no horizontal overflow, and no console errors. |
| 2026-05-12 | Redirect smoke: `/cms`, `/nexus` | Each compatibility route redirected to `/?audit=redirect-smoke`; Hub rendered with 38 buttons, `unnamed: []`, no horizontal overflow, and no console errors. |
| 2026-05-12 | Settings all-tab visible-control scan before fix | 12 visible tabs were scanned with active-tab-only DOM filtering. Found Settings control issues: unnamed range inputs in Appearance, missing button types in Editor/AI/Data, unnamed number inputs in Data, unnamed file inputs in Extensions/Advanced. Finding `F-SETTINGS-001`. |
| 2026-05-12 | GitNexus impact before `SettingsView.vue` edit | `npx gitnexus impact SettingsView -r Inkforge -d upstream --depth 2` returned LOW risk, impactedCount 0. |
| 2026-05-12 | Targeted lint after `F-SETTINGS-001` fix | `pnpm -C inkforge exec eslint src/views/SettingsView.vue --ext .vue --quiet` passed. |
| 2026-05-12 | Browser re-check after `F-SETTINGS-001` fix | `/settings?tab=appearance&audit=settings-all-tabs-a11y-recheck` scanned all 12 Settings tabs: no unnamed visible controls, no visible buttons without `type="button"`, no range inputs without names, no console errors, no horizontal overflow, no mojibake/sample leak markers. |
| 2026-05-12 | Settings shell navigation control re-check after `F-SETTINGS-002` | `pnpm -C inkforge exec eslint src/views/SettingsView.vue --ext .vue --quiet` passed. Playwright desktop `/settings?tab=appearance&audit=settings-nav-controls-recheck` returned 28 visible buttons, 33 visible controls, `visibleButtonsWithoutType: []`, `unnamedVisibleControls: []`, `horizontalOverflowPx: 0`, and no console errors. A fresh 390px mobile context for `/settings?tab=appearance&audit=settings-nav-controls-recheck-mobile` returned the same missing-type/name result, page-level `horizontalOverflowPx: 0`, and no console errors; screenshot saved to `screenshots/settings-appearance-mobile-controls-2026-05-12-2026-05-12T06-19-13-322Z.png`. |
| 2026-05-12 | Settings Appearance field persistence proof | Through real Appearance controls, changed `theme light -> dark`, `fontFamily serif -> mono`, typography preset `标准 -> 舒展`, and accent `#D32F2F -> #1565C0`. After the 5s debounced save, read-only `localStorage["inkforge-settings"]` showed `appearance.theme=dark`, `fontFamily=mono`, `accentColor=#1565C0`, `fontSize=18`, `lineHeight=1.9`, and typography fields `paragraphSpacing=24`, `paragraphIndent=true`, `headingStyle=border-left`, `blockquoteStyle=modern`. The live DOM switched to `html.theme-dark`, CSS token `--accent-primary=#1565C0`, mono `--font-body`, visual cards `dark / 等宽 / 55 / 启用`, and no console errors. |
| 2026-05-12 | Settings Appearance reload proof | Reopened `/settings?tab=appearance&audit=appearance-field-proof-reload`. The UI and storage still showed `theme=dark`, `fontFamily=mono`, `accentColor=#1565C0`, typography preset `舒展`, `fontSize=18`, `lineHeight=1.9`; token preview kept `--accent-primary=#1565C0`, `--font-body` mono stack, and `--font-cjk` mono stack; diagnostics remained visible and page overflow was 0. |
| 2026-05-12 | Settings confirm dialog repair and Appearance reset proof | GitNexus impact for `SettingsView` returned LOW; `pnpm -C inkforge exec eslint src/views/SettingsView.vue --ext .vue --quiet` passed after adding `type="button"` to confirm dialog buttons and `aria-label="确认操作校验文本"` to the required text input. Browser reset proof changed Appearance theme to `dark`, clicked `重置外观 Tab`, observed modal title/message, verified `visibleButtonsWithoutType: []` and `unnamedVisibleControls: []`, confirmed that the OK button was disabled before entering `RESET` and enabled afterward, then confirmed reset. Storage returned default Appearance values (`light`, `serif`, `#D32F2F`, font size 16, line height 1.8, default typography snapshot), the dialog closed, and console errors stayed empty. Audit-created temporary settings were restored after the proof; direct storage write was used only for cleanup, not as pass evidence. |
| 2026-05-12 | Settings Editor mode and width field proof | Through real Settings Editor controls, switched `editorMode` to `Source`, selected width `宽`, waited for the 5s settings debounce, and read `localStorage["inkforge-settings"]` as observation only: `editorMode=source`, `editorWidth=wide`. Reloaded `/settings?tab=editor` and the UI still selected `Source` and `宽`; then restored `Typora` while keeping `wide` for Workstation reflection. `/workstation?id=09cc3999-810e-4884-bb3b-843561a17577` showed `.workstation mode-typora app-route-shell`, `.editor-mode-shell mode-typora`, status text containing `Typora`, `.editor-paper` width 860, CSS `--paper-max-width=860px`, no horizontal overflow, and no console errors. |
| 2026-05-12 | Settings Editor autosave gap before repair | Serena/code search found `settings.editor.autoSave` and `autoSaveInterval` only in `stores/settings.ts` and `SettingsView.vue`; `EditorPanel.vue` had `schedulePersist(markdown)` hardcoded to `setTimeout(..., 900)` and never read Settings autosave fields. Matrix row `SET-EDITOR-003` was therefore not passable from Settings persistence alone. Finding `F-SETTINGS-004`. |
| 2026-05-12 | GitNexus and static gate around autosave repair | `npx gitnexus impact EditorPanel -r Inkforge -d upstream --depth 2`, `schedulePersist`, and `TyporaSyncState` returned `Target not found`. `npx gitnexus impact updateContent -r Inkforge -d upstream --depth 2` returned CRITICAL because AI store flows call `updateContent`; the repair intentionally avoided changing `editorStore.updateContent` or its signature. After the `EditorPanel`/`TyporaMode`/`WorkstationView` repair, `pnpm -C inkforge exec eslint src/components/editor/EditorPanel.vue src/views/WorkstationView.vue src/extensions/TyporaMode.ts --ext .ts,.vue --quiet` passed and `pnpm -C inkforge exec vue-tsc --noEmit --pretty false` passed. |
| 2026-05-12 | Settings Editor autosave disabled behavior proof | In real Settings UI, changed autosave interval from the observed 60s to 10s and clicked the `自动保存` switch off; after the 5s settings debounce, read-only `localStorage["inkforge-settings"]` showed `editor.autoSave=false`, `autoSaveInterval=10`, and the interval row was hidden. Then opened `/workstation?id=09cc3999-810e-4884-bb3b-843561a17577`, inserted marker `自动保存关闭审计-1778571774464` through the real ProseMirror editor, and waited 2.5s. Read-only IndexedDB `contents` still had body length 0, did not contain the marker, and the status pill showed `未保存`; after dispatching the real `Ctrl+S` save shortcut, IndexedDB body length became 22, contained the marker, and status returned `已同步 · 已保存`. Console errors stayed empty and page overflow was 0. |
| 2026-05-12 | Settings Editor autosave interval behavior proof | Re-enabled `自动保存` through Settings UI, kept the interval at 10s, and waited for the settings debounce; storage showed `editor.autoSave=true`, `autoSaveInterval=10`. In Workstation, inserted marker `自动保存十秒审计-1778571843421` through ProseMirror. After 2.5s, IndexedDB still had the previous 22-byte body and did not contain the marker; the status pill showed `同步中…`. After an additional 9.4s, IndexedDB body length became 44, contained the marker, and status showed `已同步 · 已保存`. Console errors stayed empty and page overflow was 0. Cleanup restored the observed Settings state to `autoSave=true`, `autoSaveInterval=60` through the Settings UI; cleanup is not used as pass evidence. |
| 2026-05-12 | Settings low-risk persistence smoke | Changed real UI controls for Appearance font size/accent, Editor mode/autosave interval, Export platform/image width, and Data backup interval/max backups. After the 5s debounced save, `localStorage["inkforge-settings"]` reflected the changed values; original settings JSON was restored afterward. |
| 2026-05-12 | Export platform persistence re-check | Corrected the test to click the actual platform radio card instead of a theme `<select>`. Clicking `小红书` wrote `export.defaultPlatform=xiaohongshu` to `inkforge-settings`; original settings JSON was restored afterward. |
| 2026-05-12 | Settings Sync manual boundary | `/settings?tab=sync`, clicking `立即同步` with no provider changed status to paused/failure text and displayed `同步提供者未配置`; pending queue remained visible and no fake remote success was shown; console had no errors. |
| 2026-05-12 | Settings AI unavailable boundary | `/settings?tab=ai`, clicking `测试连接` without an API key displayed `请先配置 API Key`; no fake model success was shown; original settings JSON was restored; console had no errors. |
| 2026-05-12 | Settings Audit integrity boundary | `/settings?tab=audit`, clicking `校验完整性` read the real audit ledger and changed integrity from `unknown` to `valid` for the current 1-record ledger; console had no errors. |
| 2026-05-12 | Settings Extensions invalid manifest boundary | `/settings?tab=extensions`, entering `{"id":"bad"}` and clicking `安装本地 manifest` displayed schema validation text `Extension id must use author.name lowercase format`; installed count stayed 0; console had no errors. |

| 2026-05-12 | Workstation article persistence correction | Reopened real UI-created article `ef83bc07-b8b8-46ba-a4a8-14309ed40019`; full read-only IndexedDB inspection showed article body is stored in `rawContent`, not `content` or `body`. Existing record keys include `rawContent`, `markdownSource`, `htmlCache`, `sourceHash`, and `updatedAt`; `rawContent` held the previously typed smoke text and `htmlCache` contained matching paragraphs. |
| 2026-05-12 | Workstation keyboard fill persistence | Used Playwright `fill('.ProseMirror', ...)` on the real Workstation editor, waited for autosave, then read IndexedDB read-only. `rawContentLength=155`, `markdownSourceContainsMarker=true`, `htmlCacheContainsMarker=true`, `sourceHash=49d362d3538f555264ff3754ed4f423a6d71fa46ea181d1e79821b097dcb59b0`, and `updatedAt=2026-05-11T19:33:38.590Z`; after reload, ProseMirror still rendered the same marker. |
| 2026-05-12 | Workstation button type static repair gate | Static scan over `WorkstationView.vue`, `FileManager.vue`, and `VersionPanel.vue` returned `missingType: 0` for every `<button>` opening tag after repair. GitNexus impact returned LOW for `WorkstationView`; GitNexus could not map `FileManager` or `VersionPanel` Vue SFC targets directly, so the repair was kept attribute-only and compensated with static scan, targeted lint, and browser evidence. |
| 2026-05-12 | Workstation targeted lint after `F-WS-001` | `pnpm -C inkforge exec eslint src/views/WorkstationView.vue src/components/file/FileManager.vue src/components/version/VersionPanel.vue --ext .vue --quiet` passed. |
| 2026-05-12 | Workstation browser re-check after `F-WS-001` | `/workstation?id=ef83bc07-b8b8-46ba-a4a8-14309ed40019&audit=ws-version-button-type-recheck`: real article marker rendered; `visibleButtonsWithoutType: []`; `unnamedButtons: []`; file panel tabs and FileManager modes all had `type=button`; VersionPanel `保存版本` and diff toggle had `type=button`; no console errors; no horizontal overflow. |
| 2026-05-12 | Export modal real article boundary before `F-EXP-001` | From `/workstation?id=ef83bc07-b8b8-46ba-a4a8-14309ed40019`, clicked `发布` and opened `ExportModal`. The modal read the real article marker into WeChat preview, generated native/styled platform artifacts, and displayed `尚未配置真实 API 授权；当前只提供可复制、可下载的真实导出产物`, so no fake direct-publish success was present. The same scan found modal buttons missing explicit `type="button"`. |
| 2026-05-12 | Export modal static and lint after `F-EXP-001` | `node` static scan over `ExportModal.vue` returned `missingType: 0`. `pnpm -C inkforge exec eslint src/components/export/ExportModal.vue --ext .vue --quiet` passed. GitNexus target `ExportModal` was not found, so this attribute-only/modal-semantics repair was compensated with static scan, targeted lint, and browser evidence. |
| 2026-05-12 | Export modal three-platform browser re-check | Reopened the real article export modal and clicked WeChat, Xiaohongshu, and Zhihu. Each platform retained `role=dialog`, `aria-modal=true`, and `aria-labelledby=export-modal-title`; `visibleButtonsWithoutType: []`; `unnamedButtons: []`; the real marker text was visible in each preview/native output; direct publish showed honest unconfigured API authorization copy; no console errors; no horizontal overflow. |
| 2026-05-12 | Publish route static and lint after `F-PUB-002` | Static scan over `PublishView.vue` returned `missingType: 0`. `pnpm -C inkforge exec eslint src/views/PublishView.vue --ext .vue --quiet` passed. |
| 2026-05-12 | Publish route direct empty-state re-check | Direct `/publish?audit=publish-button-type-recheck` showed honest empty state: `未选择文章`, `等待正文`, `暂无可发布正文`, and copy/download buttons disabled. It returned `visibleButtonsWithoutType: []`, `unnamedButtons: []`, no console errors, and no horizontal overflow. |
| 2026-05-12 | Publish route selected-article SPA re-check | From `/workstation?id=ef83bc07-b8b8-46ba-a4a8-14309ed40019`, route changed in the same SPA session to `/publish?audit=publish-spa-store-source`. Publish read `editorStore.currentContent`, displayed title `未命名文章`, rendered the real marker text in preview, enabled `复制到剪贴板` and `下载HTML`, and returned `visibleButtonsWithoutType: []`, `unnamedButtons: []`, no console errors, and no horizontal overflow. |

| 2026-05-12 | Vite runtime restart for continued browser audit | Port 3005 was no longer listening and the old Node PID was gone. `Start-Process pnpm` failed because PowerShell resolved the shim instead of a Win32 executable; retrying with `pnpm.cmd` started hidden process PID `37744`. The readiness loop returned HTTP 200 for `http://127.0.0.1:3005/`; stdout/stderr logs are under `.trellis/tasks/05-12-full-app-functionality-audit-repair/vite-3005.*.log`. |
| 2026-05-12 | App shell error-boundary button repair | Static scan found two `App.vue` error-boundary recovery buttons missing explicit button type. GitNexus target `App` was not mapped, so this attribute-only fix was compensated with a static scan and targeted ESLint. After repair, the scan returned `missingType: 0`; `pnpm -C inkforge exec eslint src/App.vue --ext .vue --quiet` passed. |
| 2026-05-12 | Command Palette baseline surface | On Hub desktop, `Ctrl+K` opened a modal dialog with `aria-modal=true`, search input focused, 3 quick commands visible, `visibleButtonsWithoutType: []`, `unnamedButtons: []`, and no horizontal overflow. A 390px viewport re-check showed panel width 358px inside a 390px viewport, no unnamed buttons, no missing button types, and no console errors. |
| 2026-05-12 | Command Palette navigation commands | Searching and executing `Appearance settings` routed to `/settings?tab=appearance`. Searching and executing `Open drafts` routed to `/drafts` and rendered the real draft list. Both flows closed the palette without console errors. |
| 2026-05-12 | Command Palette publish command failure and repair | From `/drafts`, executing `Open publish workspace` initially kept the palette open with `permission_denied`. GitNexus impact for `createBuiltinCommands` returned LOW risk. After removing `publish.execute` from the navigation-only command, targeted ESLint passed and browser re-check routed to `/publish`, rendering `未选择文章`, `等待正文`, and `暂无可发布正文` with no console errors. |
| 2026-05-12 | Command Palette DevPanel command boundaries | With Developer Mode disabled, executing `Developer: Toggle Panel` routed to `/settings?tab=about&section=dev-panel`, kept DevPanel hidden, and showed `Developer Mode is disabled. Enable it in Settings > About > Developer tools first.` With Developer Mode enabled through Settings, the same command opened DevPanel with source `command-palette`; Developer Mode was restored to `false` afterward. |
| 2026-05-12 | Command Palette real document creation | New browser context had no article rows, so Workstation honestly showed empty state. Executing `New document` through Command Palette created real draft `c6329558-d6cd-4e5b-bb32-6872a70ee9f3`, routed to `/workstation?id=c6329558-d6cd-4e5b-bb32-6872a70ee9f3`, loaded a real ProseMirror editor, and later Hub showed the created draft with 40 words after real editor input. |
| 2026-05-12 | Command Palette settings command group | In browser, the command group routed as follows: `Open settings` -> `/settings?tab=appearance`; `Editor settings` -> `/settings?tab=editor`; `Writing goal settings` -> `/settings?tab=editor&section=writing-goal`; `Keyboard shortcuts` -> `/settings?tab=shortcuts`; `Export settings` -> `/settings?tab=export`. Each command matched the expected URL and console errors stayed empty. |
| 2026-05-12 | Workstation command bridge | With real draft `c6329558-d6cd-4e5b-bb32-6872a70ee9f3`, the editor was filled through the real ProseMirror surface and reported saved/synced. Command Palette `Switch to Source mode` changed the Workstation mode to source and preserved the real text; `Toggle Split View` opened a split preview showing the same real text; `Toggle manager sidebar` hid the manager panel; no console errors or horizontal overflow occurred. |
| 2026-05-12 | Command Palette export modal bridge | With the same real draft active, Command Palette `Open export modal` opened `ExportModal`, rendered the real text `命令面板工作台桥接审计正文 2026-05-12`, and preserved the honest direct-publish boundary text `尚未配置真实 API 授权`; console errors stayed empty. |
| 2026-05-12 | Command Palette Hub navigation from Workstation | Command Palette `Go to Hub` executed from Workstation and routed to `/`. Hub rendered the real created draft and its text snippet; no console errors or horizontal overflow occurred. |
| 2026-05-12 | DevPanel startup flags | `?dev-panel=1` and `?devPanel=1` both rendered DevPanel with `role=dialog`, `aria-label=InkForge DevPanel`, source `startup-flag`, 7 desktop tabs, no unnamed buttons, no missing button types, no unnamed inputs, no horizontal overflow, and no console errors. A 390px viewport rendered compact tabs `Editor`, `Events`, and `IndexedDB` with no overflow. |
| 2026-05-12 | DevPanel Settings About controls | `/settings?tab=about&section=dev-panel` showed disabled state: checkbox false, status `已隐藏`, launcher disabled. Enabling the checkbox persisted `advanced.developerMode=true`, changed status to `已启用`, enabled the launcher, and the launcher opened DevPanel with source `settings`. Closing the panel and unchecking restored `advanced.developerMode=false` and disabled the launcher. |
| 2026-05-12 | DevPanel triple-shortcut activation | Separate Playwright key-tool calls for `Ctrl+Shift+D` produced valid keydown events but did not meet the 500 ms triple-press activation window. A rapid in-page bubbling keyboard-event sequence opened DevPanel with source `triple-shortcut`; `advanced.developerMode` remained false, proving this path enables only session developer mode. |
| 2026-05-12 | DevPanel tabs and safe actions | With `?dev-panel=1`, each visible tab was clicked: Editor, ProseMirror, Stores, Performance, Events, IndexedDB, Network. Visible cards changed to real runtime diagnostics such as TipTap JSON, PM Document, Stores, Performance SLO, Event Stream, IndexedDB table rows, and Network Ring Buffer. Refresh ran, the height slider changed panel height from 288px to 396px, close removed the panel, and console errors stayed empty. |
| 2026-05-12 | FileManager desktop behavior audit: search/view/sort/filter | On `/workstation?id=c6329558-d6cd-4e5b-bb32-6872a70ee9f3`, FileManager search for `桥接审计正文` found the real article row; search for `不存在的审计搜索词 2026-05-12` rendered `未找到匹配的文章`. View buttons persisted `tree -> flat -> recent -> tree` into `localStorage["inkforge:file-manager:prefs:v1"]`; recent view disabled sort controls; selecting sort field `title` and toggling direction persisted `sortField=title`, `sortDirection=asc`. Smart folders showed `审阅0` as empty and `草稿/写作` with real draft rows. |
| 2026-05-12 | FileManager failure before repair | Toolbar `新建空白文章` created real article `beb73597-9203-479c-9087-18d4589d9335`, but IndexedDB showed `status=read`, `sourceName=手动添加`, and the visible row was `无标题文章已读`. Root cause: FileManager omitted `status`, so `addArticle()` defaulted to `new` and immediate `selectArticle()` converted it to `read`. Finding `F-FILE-001`. |
| 2026-05-12 | GitNexus and lint around FileManager draft fix | `npx gitnexus impact FileManager -r Inkforge -d upstream --depth 2`, `createBlankArticle`, and `ctxNewArticleInCategory` returned `Target not found`; `npx gitnexus impact addArticle -r Inkforge -d upstream --depth 2` returned LOW risk. `pnpm -C inkforge exec eslint src/components/file/FileManager.vue --ext .vue --quiet` passed after the fix. |
| 2026-05-12 | FileManager toolbar draft re-check | After repair, toolbar `新建空白文章` created real article `c1c237ad-784d-4713-bc8f-7208db8944ab`; read-only IndexedDB showed `title=无标题文章`, `status=draft`, `sourceName=InkForge 本地新建`, `rawContent=""`, and the row rendered as `无标题文章草稿刚刚`. `草稿/写作` smart folder showed the new row instead of losing it as read. |
| 2026-05-12 | FileManager category/context-menu re-check | Created real category `审计分类 2026-05-12`, then used its context menu `在此分类新建文章`; IndexedDB showed new article `486f0b01-a353-4640-93be-67ee19287ef5` with `status=draft` and `categoryId=09f4cfca-5c43-4702-b301-6dae7899d83f`, while the category count became 1. Article context menu then renamed `beb73597-9203-479c-9087-18d4589d9335` to `FileManager 审计重命名 2026-05-12`, moved it to the category, and category count became 2. A deletion confirmation for an audit-created article showed typed `取消`/`删除` buttons and removed the row without leaving the modal open. |
| 2026-05-12 | FileManager category delete boundary | Renamed the audit category to `审计分类重命名 2026-05-12`, then used its delete confirmation. The confirmation text warned that category articles move to `未分类`; after confirming, IndexedDB `categories` count was 0 and the affected audit articles had `categoryId=null`, proving category deletion used the real category/article boundary rather than deleting articles or faking the UI. |
| 2026-05-12 | FileManager mobile 390px re-check | Reopened Playwright at 390x844. Empty FileManager rendered with visible tabs, search, view/sort controls, smart folders, and no horizontal overflow. Creating a blank article through the mobile FileManager UI created real draft `abaaa633-0833-4c26-b2b7-d375cb239c9f` with `sourceName=InkForge 本地新建`; `scrollWidth=390`, `overflow=false`, and console errors were empty. |

| 2026-05-12 | Current task import of three-platform export service evidence | Imported the focused export audit from `.trellis/tasks/05-12-export-platform-rendering-real-capability-audit/real-capability-audit.md`: WeChat pasteable HTML, Xiaohongshu native plain text, and Zhihu Markdown-compatible native output are classified as real local service capability; preview simulators remain preview-only; platform uploaders remain explicit `NotImplementedError` stubs; export service suite previously passed 12 files / 171 tests, export-scoped lint passed, and repo lint/typecheck passed in that focused task. This closes only the service-layer matrix row, not the still-pending Settings/Tauri rows. |
| 2026-05-12 | Export modal current-browser three-platform re-check | On the live Vite app, opened a real Workstation article in `ExportModal`, then switched `微信公众号`, `小红书`, and `知乎`. All three platforms kept the real article marker `版本面板审计正文 2026-05-12`, showed native/preview platform artifacts, had no fake `发布成功` / `上传成功` / `同步成功` state, kept the honest unconfigured API authorization boundary, and returned no visible buttons without `type="button"`, no unnamed buttons, no horizontal overflow, and no console errors. |
| 2026-05-12 | Hub route real-data/control re-check after `F-HUB-001` | Hub `/` rendered real local state: `1 篇文章`, `本周产出 1 篇`, `字数 36`, `草稿 1`, recent article text `版本面板审计正文 2026-05-12`, template market, and insight sections. Robust static tag scan returned `missingButtonType=0`, `selectsWithoutName=0`, and `inputsWithoutName=0`; `pnpm -C inkforge exec eslint src/views/HubView.vue src/views/WorkstationView.vue --ext .vue --quiet` passed. Playwright desktop 1440x900 and mobile 390x844 both found no visible buttons without `type="button"`, no unnamed controls, no horizontal overflow, and no console errors. |
| 2026-05-12 | Hub continue action re-check | On Hub, clicking the real `继续创作` button routed to `/workstation?id=abaaa633-0833-4c26-b2b7-d375cb239c9f`, loaded a real ProseMirror editor, and kept the article marker visible with no horizontal overflow. This proves the continue action follows the real selected local article rather than a static link. |
| 2026-05-12 | Hub template route failure before `F-HUB-002` | Clicking the `教程类` template card created real IndexedDB article `8536c26c-747b-467d-b689-799f7cfc14fd` with `status=draft`, `sourceName=模板：教程类`, and rendered template Markdown content, but Workstation navigated to the previously active persisted article `abaaa633-0833-4c26-b2b7-d375cb239c9f`. This proved template creation was real but the route/selection behavior was wrong. |
| 2026-05-12 | Hub template route re-check after `F-HUB-002` | After guarding Workstation layout restore, clicking the `评测类` template card created real IndexedDB article `7039324f-00fe-46c2-b8eb-18969b7c3c3c` with `status=draft`, `sourceName=模板：评测类`, and template Markdown content. Workstation opened `/workstation?id=7039324f-00fe-46c2-b8eb-18969b7c3c3c`, the editor showed the new template title/content, the old article was not selected, no visible buttons lacked type, and console errors stayed empty. |
| 2026-05-12 | Themes route control and persistence audit | `/themes` initially had clean header/action buttons but ten preset cards were clickable `div.theme-card` elements with no native button role or selected state. After repair, each card is a `<button type="button">` with `aria-pressed` and an accessible label. `pnpm -C inkforge exec eslint src/views/ThemesView.vue --ext .vue --quiet` passed; static scan returned `buttons=5`, `missingButtonType=0`, `themeCardButtonCount=1` for the `v-for` card template. Browser evidence: selecting `法学研讨` set `aria-pressed=true`, applying it wrote `inkforge_theme_preset=legal` and routed to Workstation, re-opening Themes showed `法学研讨` as current, then selecting and applying `论文翻译` restored `inkforge_theme_preset=thesis`. Console errors stayed empty and no overflow or unnamed buttons were found. |

## Tool Limitations

| Tool | Status | Evidence / Compensation |
| --- | --- | --- |
| Serena MCP | Initial calls returned `Session terminated`. | Record limitation; compensate with local file inspection, targeted tests, browser evidence, and retry before risky edits if available. |
| GitNexus | Available for some Vue route/service symbols; incomplete for some SFC component targets. | `PublishView`, `ThemesView`, `SettingsView`, `WorkstationView`, and `createBuiltinCommands` impact checks returned LOW risk. `App`, `FileManager`, `VersionPanel`, and `ExportModal` targets were not found, so those narrow repairs were compensated with static scans, targeted lint, and browser evidence. Must still run detect-changes before wrap-up. |

## Browser Evidence

Route smoke evidence is being recorded in the setup/evidence table above. Deeper Settings, Workstation real-article, and Tauri desktop-shell audits remain pending.

## Static Gates

Targeted non-mutating ESLint has passed for `PublishView.vue`, `ThemesView.vue`, `SettingsView.vue`, `WorkstationView.vue`, `FileManager.vue`, `VersionPanel.vue`, `ExportModal.vue`, `App.vue`, and `services/command/builtin.ts`. `PublishView.vue` was re-run after both publish-route repair batches. Repo-wide gates remain pending until route/Settings/Tauri audit coverage is broader.

## Tauri Evidence

Partial. A real Tauri/WebDriver run now starts the packaged WebView2 shell and covers the chrome
window controls, brand mark, visual tokens, theme cascade, SVG export surface, desktop runtime
snapshot, capability matrix, Rust window-list readback, and safe fail-closed native boundary paths.
Native file-dialog selection/cancel, valid Explorer reveal, valid external shell-open, clipboard
text read/write/restore, and other safe shell-only boundaries are now covered. Native file-dialog
selection/cancel, valid Explorer reveal, valid external shell-open, clipboard permission-prompt
variants, and other OS-window side effects still require dedicated/manual rows before the whole
`05-12` task can close.

## 2026-07-05 Continuation Gate Replay

| Time | Action | Result |
| --- | --- | --- |
| 2026-07-05 | Full lint gate: `pnpm -C inkforge exec eslint src --ext .ts,.tsx,.vue --quiet` | Failed on `inkforge/src/components/ai/AIChatPanel.vue:33:24` with `HTMLTextAreaElement is not defined` (`no-undef`). |
| 2026-07-05 | GitNexus impact before repair: `npx gitnexus impact AIChatPanel.vue -r InkForge -d upstream --depth 2` | LOW risk; 1 direct upstream file (`WorkstationView.vue`); 0 affected processes. |
| 2026-07-05 | Minimal repair | `AIChatPanel.vue` textarea ref type narrowed from `HTMLTextAreaElement | null` to `HTMLElement | null`; runtime behavior unchanged because code only uses `style` and `scrollHeight`. |
| 2026-07-05 | Targeted lint: `pnpm -C inkforge exec eslint src/components/ai/AIChatPanel.vue --ext .vue --quiet` | PASS. |
| 2026-07-05 | Full lint replay: `pnpm -C inkforge exec eslint src --ext .ts,.tsx,.vue --quiet` | PASS. |
| 2026-07-05 | Typecheck replay: `pnpm -C inkforge exec vue-tsc --noEmit --pretty false` | PASS. |
| 2026-07-05 | Full Vitest replay: `pnpm -C inkforge exec vitest run --reporter=default --maxWorkers=1 --no-file-parallelism` | PASS: 102 files / 1769 tests. Expected warning/error logs from existing hardening tests were non-failing test output. |
| 2026-07-05 | Production web build: `NODE_OPTIONS=--max-old-space-size=4096 pnpm -C inkforge build` | PASS; Vite built successfully in 26.75s. `inkforge/tsconfig.tsbuildinfo` was restored and is not part of the repair commit. |
| 2026-07-05 | Tauri package build: `pnpm -C inkforge tauri:build` | PASS; produced `src-tauri/target/release/bundle/msi/InkForge_0.1.0_x64_en-US.msi` and `src-tauri/target/release/bundle/nsis/InkForge_0.1.0_x64-setup.exe`. |

### Current native-boundary status

The package build gate is now green. Interactive Tauri shell evidence for native dialogs, updater, file-system/runtime boundaries, and shell-only controls is still not complete in this continuation slice. Those rows remain active and must not be counted as passed until a real desktop-shell run is recorded.

### 2026-07-05 Tauri/WebDriver SVG + chrome replay

| Time | Action | Result |
| --- | --- | --- |
| 2026-07-05 | Initial Tauri/WebDriver e2e replay: `pnpm -C inkforge test:e2e` | Failed because `tests/e2e/specs/svg-render.spec.cjs` still expected pre-amendment three-platform external proof counts (`外部证明清单 19 行`) and disabled rows for styles that are now preset-backed current-round applications. This was a stale e2e contract, not a runtime renderer failure. |
| 2026-07-05 | GitNexus impact before e2e contract edit: `npx gitnexus impact svg-render.spec.cjs -r InkForge -d upstream --depth 2` | LOW risk; file target only; 0 affected processes. |
| 2026-07-05 | E2E contract calibration | Updated the real-binary assertions to the user-confirmed current round: WeChat external handoff has 8 rows, XHS/Zhihu publish-side automation is outside this round, market capability summary is `21/3/16/2`, and preset-backed WeChat style rows such as Amber and Toolbar are selectable locally while release proof remains unclaimed. |
| 2026-07-05 | Final Tauri/WebDriver replay: `pnpm -C inkforge test:e2e` | PASS: 2 spec files / 17 tests. `svg-render.spec.cjs` passed 6/6 with real draft seeding, real ExportModal, WeChat/XHS/Zhihu local style capability readback, flagship Kiln/Tempera/Amber SVG injection, and mobile-comfort CJK line-width check. `visual.spec.cjs` passed 11/11 with TitleBar controls, IPC click round-trips, brand mark, tokens, and theme cascade in the Tauri WebView2 shell. |

Boundary: this Tauri/WebDriver proof strengthens the desktop-shell evidence for chrome and SVG/export UI. It does not complete native dialogs, updater, file-system/runtime boundaries, file reveal, shell-open, external credentials, platform upload, platform sync, public rendering, or publish rows.

### 2026-07-05 Tauri/Desktop Runtime Boundary Replay

| Time | Action | Result |
| --- | --- | --- |
| 2026-07-05 | Code audit against `utils/platform.ts` and `services/desktop/environment.ts` | Found a real native-boundary drift: the central platform detector already recognizes Tauri 1 IPC-only globals such as `__TAURI_INVOKE__` and `__TAURI_IPC__`, but the Desktop runtime detector recognized only `__TAURI__` and `__TAURI_INTERNALS__`. A Tauri 1 build with `withGlobalTauri=false` could therefore be misclassified as web by Settings/About desktop diagnostics. |
| 2026-07-05 | GitNexus impact: `npx gitnexus impact detectTauriRuntimeSignal -r InkForge -d upstream --depth 3` | LOW risk; direct upstream `detectDesktopRuntime`; Desktop module only; 0 affected processes. |
| 2026-07-05 | GitNexus impact: `npx gitnexus impact detectDesktopRuntime -r InkForge -d upstream --depth 3` | MEDIUM risk; direct upstream Desktop service methods and SettingsView; Desktop module only; 0 affected processes. Proceeded because this is the exact native-boundary row under audit and the fix is additive detection. |
| 2026-07-05 | Repair | Extended `DesktopRuntimeSignal` and `detectTauriRuntimeSignal()` to recognize `__TAURI_INVOKE__`, `__TAURI_IPC__`, `__TAURI_METADATA__`, and `__TAURI_POST_MESSAGE__`, matching the existing Tauri 1 platform detector semantics without weakening web fallback. |
| 2026-07-05 | Targeted unit test: `pnpm -C inkforge exec vitest run src/services/desktop/environment.test.ts --reporter=default` | PASS: 1 file / 3 tests. Covers web/no-signal, classic `__TAURI__`, v2 internals, and IPC-only Tauri 1 globals. |
| 2026-07-05 | Targeted lint: `pnpm -C inkforge exec eslint src/services/desktop/environment.ts src/services/desktop/types.ts src/services/desktop/environment.test.ts tests/e2e/specs/native-runtime.spec.cjs --quiet` | PASS. |
| 2026-07-05 | Production web build: `NODE_OPTIONS=--max-old-space-size=4096 pnpm -C inkforge build` | PASS; refreshes the frontend bundle embedded by the Tauri debug binary. |
| 2026-07-05 | First full native e2e replay: `pnpm -C inkforge test:e2e` | Failed once because the new test probe selected the first `.sv-section-note` instead of the runtime status note. The actual UI already showed Tauri runtime and the native command note; only the test selector was wrong. |
| 2026-07-05 | Targeted native replay: `pnpm -C inkforge exec wdio run tests/e2e/wdio.conf.cjs --spec tests/e2e/specs/native-runtime.spec.cjs` | PASS: `native-runtime.spec.cjs` 2/2. Verified Settings About reports `Tauri Desktop`, explicit Tauri signal, current window `main`, a native app-data value without recording the local path, available native capability rows, planned unsupported rows, and no missing button types in the Desktop Runtime section. |
| 2026-07-05 | Native command/fail-closed probe inside the same real Tauri shell | PASS. Through the real Pinia `desktop` store, `desktopStore.refresh()` loaded the Tauri Rust runtime snapshot and window list. `desktopStore.revealPath('   ')` returned typed `invalid-input`; a definitely missing path called the real `reveal_in_explorer` command and returned typed `failed` with `path does not exist`; `desktopStore.openUrl('javascript:...')` and malformed URL both returned typed `invalid-input` before `shell.open`. |
| 2026-07-05 | Final full native e2e replay: `pnpm -C inkforge test:e2e` | PASS: 3 real Tauri/WebDriver specs / 19 tests. `native-runtime.spec.cjs` passed 2/2, `svg-render.spec.cjs` passed 6/6, and `visual.spec.cjs` passed 11/11. |

Boundary: this closes the Desktop runtime detection drift and proves safe native command boundaries in the real Tauri shell. It does not automate OS file-picker selection/cancel, valid Explorer reveal, valid external URL/mail client opening, clipboard permission flows, signed updater endpoint behavior, global shortcuts, tray behavior, or package-signing verification.

### 2026-07-05 Tauri Clipboard Text Boundary Replay

| Time | Action | Result |
| --- | --- | --- |
| 2026-07-05 | Code audit of `DSK-006` | The real Tauri config already allowed clipboard text access, but the Desktop service/store did not expose a typed clipboard boundary. Other UI surfaces used raw browser clipboard APIs, which did not prove the Tauri native allowlist path behind the Desktop capability matrix. |
| 2026-07-05 | External/current API check | Context7 and the local `@tauri-apps/api/clipboard.d.ts` package confirmed the Tauri 1 API shape: `writeText(text): Promise<void>` and `readText(): Promise<string | null>`. |
| 2026-07-05 | GitNexus impact: `npx gitnexus impact buildDesktopCapabilityMatrix -r InkForge -d upstream --depth 3` | LOW risk; one direct upstream caller (`getDesktopRuntimeSnapshot`), Desktop module only, 0 affected processes. |
| 2026-07-05 | GitNexus impact: `npx gitnexus impact useDesktopStore -r InkForge -d upstream --depth 3` | LOW risk; 0 impacted processes. `DesktopCommandResult` was not found as a GitNexus target, so type safety was compensated with `vue-tsc` and targeted tests. |
| 2026-07-05 | Repair | Added typed `writeClipboardText()` and `readClipboardText()` functions to `services/desktop/index.ts`; Tauri uses `@tauri-apps/api/clipboard`, web uses real `navigator.clipboard` only when available/secure, and unavailable/failure states return typed `DesktopCommandResult` values. Exposed both methods through `useDesktopStore`. |
| 2026-07-05 | Targeted unit test: `pnpm -C inkforge exec vitest run src/services/desktop/environment.test.ts src/services/desktop/index.test.ts --reporter=default` | PASS: 2 files / 8 tests. Coverage includes Tauri read/write calls, web unavailable state, degraded browser Clipboard API path, and failed Tauri clipboard API mapping. |
| 2026-07-05 | Targeted lint: `pnpm -C inkforge exec eslint src/services/desktop/environment.ts src/services/desktop/index.ts src/services/desktop/types.ts src/services/desktop/environment.test.ts src/services/desktop/index.test.ts src/stores/desktop.ts tests/e2e/specs/native-runtime.spec.cjs --quiet` | PASS. |
| 2026-07-05 | Typecheck: `pnpm -C inkforge exec vue-tsc --noEmit --pretty false` | PASS. |
| 2026-07-05 | Production web build: `NODE_OPTIONS=--max-old-space-size=4096 pnpm -C inkforge build` | PASS; required before the packaged Tauri debug binary could load the new frontend store/service methods. |
| 2026-07-05 | First targeted native replay before build refresh | Failed because the packaged debug shell still loaded the previous frontend bundle, so the Pinia store did not yet have `readClipboardText()`. This proved the test was exercising the real packaged shell rather than a hot, mocked module surface. |
| 2026-07-05 | Targeted native replay after build refresh: `pnpm -C inkforge exec wdio run tests/e2e/wdio.conf.cjs --spec tests/e2e/specs/native-runtime.spec.cjs` | PASS: `native-runtime.spec.cjs` 3/3. The new clipboard probe read the current text clipboard, wrote a unique probe string through `desktopStore.writeClipboardText()`, read it back exactly through `desktopStore.readClipboardText()`, restored the original text through the same Tauri source, and read the restored text back exactly when the original clipboard content was text. It returned `writeSource/readSource/restoreSource/restoreReadSource = tauri` without exposing the original clipboard content. |
| 2026-07-05 | Final full Tauri/WebDriver replay: `pnpm -C inkforge test:e2e` | PASS: 3 specs / 20 tests. `native-runtime.spec.cjs` passed 3/3 including exact clipboard write/read/restore-readback, `svg-render.spec.cjs` passed 6/6, and `visual.spec.cjs` passed 11/11. |

Boundary: this closes `DSK-006` for the text clipboard boundary only. It does not claim rich HTML clipboard, image clipboard preservation, OS permission-prompt UX, or WeChat rich paste proof.

### 2026-07-05 Export Matrix Current-Round Reconciliation

| Time | Action | Result |
| --- | --- | --- |
| 2026-07-05 | Audit matrix reconciliation for `CMP-EXPORT-001` | The row was still `pending` even though `F-EXP-001`, `EXP-MODAL-001`, `EXP-SVC-001`, and the current real Tauri/WebDriver export suite already provided the required local evidence for ExportModal platform controls and renderer output. |
| 2026-07-05 | Current-round user boundary | XHS/Zhihu publish-side automation is not part of this automatic acceptance pass; those platform publish checks are manual/operator-owned. The automated claim is limited to local application of SVG/style rules, honest unsupported upload states, and WeChat-focused render/style gates. |
| 2026-07-05 | Real binary evidence reused | `pnpm -C inkforge test:e2e` passed 3 specs / 20 tests. `svg-render.spec.cjs` passed 6/6 with real draft seeding, real ExportModal platform controls, WeChat/XHS/Zhihu local style capability readback, WeChat flagship Kiln/Tempera/Amber SVG injection, and mobile-comfort CJK line-width proof. |
| 2026-07-05 | Matrix update | `CMP-EXPORT-001` is marked `fixed` against `F-EXP-001`. This does not claim account upload, scheduled send, XHS publish, Zhihu publish, or WeChat phone preview success. |

### 2026-07-05 Settings Default Export Platform Boundary

| Time | Action | Result |
| --- | --- | --- |
| 2026-07-05 | Reproduction for `SET-EXPORT-001` | Code audit found `SettingsView` persisted `settings.export.defaultPlatform`, but `WorkstationView` and `ExportModal` both initialized local platform state from hardcoded `wechat`. A changed default platform therefore did not drive the actual export surface. |
| 2026-07-05 | GitNexus impact | `selectedPlatform` in `WorkstationView.vue` and `ExportModal.vue` each returned LOW risk / 0 affected processes. `openExportModal` returned LOW risk / 0 affected processes. |
| 2026-07-05 | Repair | `WorkstationView` now initializes its Stage platform from `settingsStore.settings.export.defaultPlatform`; `ExportModal` receives `initialPlatform` and re-seeds its selected platform on open, without overriding manual in-modal selection while the modal remains open. |
| 2026-07-05 | Targeted lint/typecheck | `pnpm -C inkforge exec eslint src/components/export/ExportModal.vue src/views/WorkstationView.vue tests/e2e/specs/svg-render.spec.cjs --quiet` PASS. `pnpm -C inkforge exec vue-tsc --noEmit --pretty false` PASS. |
| 2026-07-05 | Production web build | `NODE_OPTIONS=--max-old-space-size=4096 pnpm -C inkforge build` PASS. |
| 2026-07-05 | Targeted real Tauri/WebDriver replay | `pnpm -C inkforge exec wdio run tests/e2e/wdio.conf.cjs --spec tests/e2e/specs/svg-render.spec.cjs` PASS: 7/7. The new test writes `export.defaultPlatform=zhihu` through the real Settings Pinia store, remounts Workstation with a real seeded draft, observes Stage=`知乎`, opens ExportModal without platform clicking, observes active platform pill=`知乎`, restores the original setting, and then continues the SVG style assertions. |
| 2026-07-05 | Full real Tauri/WebDriver replay | `pnpm -C inkforge test:e2e` PASS: 3 specs / 21 tests. `native-runtime.spec.cjs` 3/3, `svg-render.spec.cjs` 7/7, `visual.spec.cjs` 11/11. |

Boundary: this closes the default export platform local-application path only. It does not claim XHS/Zhihu publish, WeChat phone preview, scheduled send, account upload, or external platform success.

### 2026-07-05 Settings Export Custom CSS Boundary

| Time | Action | Result |
| --- | --- | --- |
| 2026-07-05 | Current-round user boundary | XHS/Zhihu publish automation is cancelled for this round and remains manual/operator-owned. Automated acceptance is narrowed to local application of SVG/style rules and WeChat-compatible export/rendering paths. |
| 2026-07-05 | Reproduction for `SET-EXPORT-002` | Code audit found `SettingsView` persisted `settings.export.customCss`, but the real Workstation/ExportModal export renderer consumed only the modal-local `exportOptions`; the Settings CSS path existed as a Settings preview append helper, not as a first-class renderer input. |
| 2026-07-05 | GitNexus impact | `convertToPlatform` LOW / 2 impacted / 0 processes; `ExportOptions` MEDIUM / 12 direct importing files / 0 processes; `convertToWechat` LOW / 4 impacted / 0 processes; `convertToWechatWithStats` LOW / 8 impacted / 0 processes; `usePreviewRenderer` LOW / 1 direct caller / 0 processes. |
| 2026-07-05 | Repair | Added `ExportOptions.customCss`; `convertToWechatWithStats` sanitizes Settings export CSS and merges it before `juice` so WeChat HTML carries inline CSS. `ExportModal` receives `exportCustomCss` from `WorkstationView`, computes effective export options, and sends the same options to styled and native export paths. Stage WeChat preview also sanitizes and scopes the export CSS through the mock preview stylesheet. |
| 2026-07-05 | Runtime separation | `settings.export.customCss` remains separate from `settings.advanced.customCss`; the repair does not create or mutate `#inkforge-custom-css`, and text/Markdown native platforms are not claimed to support CSS publishing. |
| 2026-07-05 | Targeted lint | `pnpm -C inkforge exec eslint src/services/export/types.ts src/services/export/wechat.ts src/components/export/ExportModal.vue src/views/WorkstationView.vue src/views/SettingsView.vue src/composables/usePreviewRenderer.ts src/services/export/platform-export-rendering.test.ts tests/e2e/specs/svg-render.spec.cjs --quiet` PASS. |
| 2026-07-05 | Service regression | `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts --reporter=default` PASS: 1 file / 377 tests. New assertions prove export-only custom CSS is inlined into WeChat HTML and dangerous CSS/tag payloads are sanitized before the inline pass. |
| 2026-07-05 | Full export service regression | `pnpm -C inkforge exec vitest run src/services/export --reporter=default --maxWorkers=1 --no-file-parallelism` PASS: 41 files / 1366 tests. |
| 2026-07-05 | Type/build | `pnpm -C inkforge exec vue-tsc --noEmit --pretty false` PASS. `NODE_OPTIONS=--max-old-space-size=4096 pnpm -C inkforge build` PASS. |
| 2026-07-05 | Targeted real Tauri/WebDriver replay | `pnpm -C inkforge exec wdio run tests/e2e/wdio.conf.cjs --spec tests/e2e/specs/svg-render.spec.cjs` PASS: 8/8. The new test writes `export.customCss` through the real Settings Pinia store, opens a real seeded Workstation article in WeChat ExportModal, reads back inline paragraph `color:#123456` and `letter-spacing:1px`, verifies the export CSS did not enter runtime `#inkforge-custom-css`, and restores the original setting. |
| 2026-07-05 | Full real Tauri/WebDriver replay | `pnpm -C inkforge test:e2e` PASS: 3 specs / 22 tests. `native-runtime.spec.cjs` 3/3, `svg-render.spec.cjs` 8/8, `visual.spec.cjs` 11/11. |

Boundary: this closes the Settings export custom CSS local renderer path for WeChat-compatible HTML only. It does not claim XHS/Zhihu publish, WeChat phone preview, scheduled send, account upload, external sync, or public rendering success.
