Xiumi Meta Panel Control Residue - 2026-06-26

Scope:
- Static publishability protection for copied or partially cleaned Xiumi editor metadata panel DOM.
- No platform action, phone preview, sync, upload, scheduled send, or publish operation.

Live DOM source:
- CloakBrowser reviewed the live Xiumi v5 paper editor.
- Observed editor-side cover/article metadata panel controls:
  - tn-meta-container
  - tn-meta-panel
  - top-group meta-group
  - toggle-btn
  - toggle-green-gray
  - toggle-off
  - toggle-on
  - tn-lighting-box
- Generic adjacent classes such as meta-group, toggle-btn, toggle-off, toggle-on, and
  tn-lighting-box are documented as context only and are not standalone detector triggers.

Implemented:
- Added detector label: Xiumi meta panel control residue
- Added reduced three-platform regression fixture:
  MARKET_EDITOR_XIUMI_META_PANEL_CONTROL_RESIDUE_HTML

Behavior:
- WeChat, Xiaohongshu, and Zhihu quality reports now fail when Xiumi metadata-panel editor chrome
  appears in publishable output.
- The reduced fixture intentionally keeps only toggle-green-gray meta-panel controls plus readable
  cover/music/custom-title labels.
- The reduced fixture avoids sidebar/tab controls, right-toolbar controls, text-toolbar controls,
  font-family menus, font-size skimmer attributes, color-selector controls, operation-bar controls,
  UI Bootstrap directives, top operation classes, broad Angular ng-* markers, paper auxiliary tree
  controls, selection overlays, crop/worker controls, operator-dock parents, operator depot items,
  op-loader, broad tn-* markers, opera-tn-* markers, contenteditable, hosted media, SVG
  content-layer, ui-slider, ui-sortable, touch-action, and user-select markers.
- The detector stays anchored to Xiumi-specific meta-panel class/id markers and does not block
  ordinary prose containing cover, music, video, custom title, spacing, tag, ratio, metadata,
  toggle, or image wording by itself.

Verification:
- pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "meta panel controls" --reporter=default
  RED: failed before detector update; no market-editor-residue issue was emitted.
- pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "meta panel controls" --reporter=default
  PASS: 1 selected test / 239 skipped tests after detector update.
- pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts --reporter=default
  PASS: 1 file / 240 tests.
- pnpm -C inkforge exec vitest run src/services/export --reporter=default --maxWorkers=1 --no-file-parallelism
  PASS: 36 files / 1217 tests.
- pnpm -C inkforge exec eslint src/services/export/quality-detector.ts src/services/export/platform-export-rendering.test.ts --quiet
  PASS.
- pnpm -C inkforge exec vue-tsc --noEmit --pretty false
  PASS.
- NODE_OPTIONS=--max-old-space-size=4096 pnpm -C inkforge build
  PASS: 4653 modules transformed; Vite built in 28.75s.
- pnpm -C inkforge style-proof:release-preflight --json
  EXPECTED BLOCK: exit 1, status=blocked-by-external, canClaimComplete=false,
  externalHandoffRows=18, safeExternalRows=0, actionableLocalRows=0, nextRowRefs=5,
  uniqueNextRows=3.

Boundary:
- This proves local static publishability protection only.
- It does not prove WeChat PC paste, phone preview, mobile SMIL/click interaction, mobile Dark
  Mode, cover thumbnail acceptance, credentialed sync, scheduled send, platform preview, public
  article rendering, public-host acceptance, Xiaohongshu/Zhihu account upload, or publish success.
