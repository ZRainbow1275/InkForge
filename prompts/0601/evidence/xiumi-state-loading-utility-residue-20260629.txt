Xiumi State Loading Utility Residue - 2026-06-29

Scope:
- Local static publishability protection for copied or partially cleaned Xiumi v5 editor HTML.
- No platform action, no phone preview, no sync, no upload, no scheduled send, no publish.

Observed source family:
- Live Xiumi v5 paper-editor DOM readback exposed editor-side state/loading utility markers:
  - tn-state-transition-animation
  - tn-input-sortable-group
  - tn-bar-btn-active-color
  - tn-loading-overlap
  - tn-top-loading-block

Implemented:
- Added the detector label:
  Xiumi state loading utility residue
- Added a reduced regression fixture containing only the supported state/loading utility markers.
- The rule is anchored to Xiumi-specific class/id tokens and does not block ordinary state,
  loading, transition, sortable, active-color, CSS transition, CSS animation, or article text by
  itself.

Regression coverage:
- WeChat, Xiaohongshu, and Zhihu quality reports now emit their platform-specific
  market-editor-residue issue ids for the reduced state/loading utility fixture.
- Adjacent regressions keep quick input, state toggle, sortable pin, group sortable,
  operation-panel component, and background/transparency operation residue labels independent.

Verification:
- Red:
  pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "state loading utility controls" --reporter=default
  FAIL: 1 selected test failed because no wechat-market-editor-residue issue was emitted.
- Green:
  pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "state loading utility controls|state toggles|sortable pin|group sortable|operation panel component controls|background transparency operation controls" --reporter=default
  PASS: 1 file / 6 selected tests passed / 328 skipped tests.
- Full platform-rendering regression:
  pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts --reporter=default --testTimeout=90000
  PASS: 1 file / 334 tests.
- Full export service regression:
  pnpm -C inkforge exec vitest run src/services/export --reporter=default --maxWorkers=1 --no-file-parallelism --testTimeout=90000
  PASS: 36 files / 1311 tests.
- Lint:
  pnpm -C inkforge exec eslint src/services/export/quality-detector.ts src/services/export/platform-export-rendering.test.ts --quiet
  PASS.
- Type check:
  pnpm -C inkforge exec vue-tsc --noEmit --pretty false
  PASS.
- Production build:
  NODE_OPTIONS=--max-old-space-size=4096 pnpm -C inkforge build
  PASS: 4653 modules transformed; Vite built in 36.21s.
- Release preflight:
  pnpm -C inkforge style-proof:release-preflight --json
  EXPECTED BLOCK: exit 1 with canClaimComplete=false, status=blocked-by-external, blockerKinds
  phone-preview, external-dependency, unsafe-to-automate, and mutating-platform.

Boundary:
- This evidence proves local static publishability protection and diagnostic precision only.
- It does not prove WeChat PC paste, WeChat phone preview, state/loading animation fidelity,
  mobile SMIL/click interaction, H5 behavior, Dark Mode, cover thumbnail acceptance,
  credentialed sync, scheduled send, platform preview, public article rendering,
  public-host acceptance, XHS/Zhihu account upload, or publish success.
