Xiumi Attribute Context Menu Host Residue - 2026-06-29

Scope:
- Static publishability gate for copied or partially cleaned Xiumi v5 paper-editor attribute
  context-menu host nodes.
- Applies to WeChat, Xiaohongshu, and Zhihu quality reports.
- No platform action, phone preview, sync, upload, schedule, or publish action was performed.

Live source:
- CloakBrowser reviewed the logged-in Xiumi v5 paper editor DOM.
- The editor exposed empty attribute context-menu host nodes with source-specific class markers:
  `attr-bar-context-menu-host-for-comp-insert`,
  `attr-bar-context-menu-host-for-comp-modify`, and
  `attr-bar-context-menu-host-for-cell`.
- The nodes appeared under Xiumi's attribute-bar host around operator depot and attribute-board
  context, but the reduced rule is intentionally anchored to the host markers themselves.

Implementation:
- Added the `Xiumi attribute context menu host residue` detector label.
- Added a reduced regression fixture containing only the three `attr-bar-context-menu-host-for-*`
  markers, without `op-gl-dc-attr-bars`, operator depot parents, attribute-board controls,
  operation-bar menus, operator-dock controls, Angular runtime attributes, hosted media, sidebar
  controls, or meta panels.
- Kept the rule anchored to class/id attributes, not prose.
- Kept adjacent detector labels independent from `Xiumi operator depot item residue`,
  `Xiumi attribute board control residue`, and `Xiumi operation bar dropdown residue`.

TDD:
- Red:
  `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "attribute context menu host controls" --reporter=default`
  failed before the detector update because no `*-market-editor-residue` issue was emitted.
- Green:
  `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "attribute context menu host controls|operator depot item controls|attribute board controls|operation bar dropdown controls" --reporter=default`
  passed with 4 selected tests and 297 skipped tests after the detector update.

Full verification:
- `pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts --reporter=default --testTimeout=60000`
  passed with 1 file and 301 tests.
- `pnpm -C inkforge exec vitest run src/services/export --reporter=default --maxWorkers=1 --no-file-parallelism --testTimeout=60000`
  passed with 36 files and 1278 tests.
- `pnpm -C inkforge exec eslint src/services/export/quality-detector.ts src/services/export/platform-export-rendering.test.ts --quiet`
  passed.
- `pnpm -C inkforge exec vue-tsc --noEmit --pretty false` passed.
- `NODE_OPTIONS=--max-old-space-size=4096 pnpm -C inkforge build` passed with 4653 modules
  transformed and Vite build completed in 40.70s.
- `pnpm -C inkforge style-proof:release-preflight --json` exited 1 as expected with
  `status=blocked-by-external`, `canClaimComplete=false`,
  `blockerKinds=["phone-preview","external-dependency","unsafe-to-automate","mutating-platform"]`,
  `externalHandoffRows=18`, `safeExternalRows=0`, `actionableLocalRows=0`, `nextRowRefs=5`,
  and `uniqueNextRows=3`.

Boundary:
- This is static publishability protection only.
- It does not prove WeChat PC paste, phone preview, mobile SMIL/click interaction, mobile Dark
  Mode, cover thumbnail acceptance, credentialed sync, scheduled send, platform preview, public
  article rendering, public-host acceptance, XHS/Zhihu account upload, or publish success.
