Xiumi Document Selection Shell Residue - 2026-06-29

Scope:
- Local static publishability protection for copied or partially cleaned Xiumi v5 paper-editor
  document selection and dock shell markers.
- No platform action, no phone preview, no sync, no upload, no publish.

Live DOM source:
- CloakBrowser reviewed the live Xiumi v5 paper editor DOM.
- The page exposed `multi-comp-select-panel`, `tn-fly-away-workaround-ios13`, and `dock-loader`
  around the editor-side document selection / dock shell.
- Minimal local probing showed each single class token did not emit a market-editor hard-block
  before this slice.

Implemented:
- Added the `Xiumi document selection shell residue` quality detector for
  `multi-comp-select-panel`, `tn-fly-away-workaround-ios13`, and `dock-loader`.
- Added a reduced regression fixture that keeps only these source-specific class tokens, without
  `tn-paper-document-root`, group/ground markers, editing dock markers, component tree classes,
  Angular runtime attributes, hosted media, or operation-panel controls.

Regression coverage:
- WeChat, Xiaohongshu, and Zhihu all report the precise
  `Xiumi document selection shell residue` market-editor issue.
- The WeChat assertion verifies the reduced fixture does not fall through to
  `Xiumi paper document root residue`, `Xiumi group ground marker residue`, or
  `Xiumi editing dock residue`.

Verification:
- pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "document selection shell" --reporter=default
  RED: 1 selected test failed before the detector because no market-editor-residue issue was emitted.
- pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "document selection shell|paper document root|group and ground markers|editing dock classes|component template binding" --reporter=default
  PASS: 5 selected tests / 289 skipped tests.
- pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts --reporter=default --test-timeout=60000
  PASS: 1 file / 294 tests.
- pnpm -C inkforge exec vitest run src/services/export --reporter=default --maxWorkers=1 --no-file-parallelism --test-timeout=60000
  PASS: 36 files / 1271 tests.
- pnpm -C inkforge exec eslint src/services/export/quality-detector.ts src/services/export/platform-export-rendering.test.ts --quiet
  PASS.
- pnpm -C inkforge exec vue-tsc --noEmit --pretty false
  PASS.
- NODE_OPTIONS=--max-old-space-size=4096 pnpm -C inkforge build
  PASS: 4653 modules transformed; Vite build completed in 38.77s.
- pnpm -C inkforge style-proof:release-preflight --json
  EXPECTED BLOCKED: status=blocked-by-external; canClaimComplete=false; blockerKinds=phone-preview/external-dependency/unsafe-to-automate/mutating-platform; actionableLocalRows=0.

Boundary:
- This proves local static publishability protection only.
- It does not prove WeChat paste, phone preview, mobile SMIL/click interaction, mobile Dark Mode,
  cover thumbnail acceptance, credentialed sync, scheduled send, platform preview, public article
  rendering, XHS/Zhihu account upload, public host acceptance, or publish success.
