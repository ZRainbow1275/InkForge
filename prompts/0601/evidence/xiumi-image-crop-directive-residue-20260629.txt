Xiumi Image Crop Directive Residue - 2026-06-29

Scope:
- Local static publishability protection for copied or partially cleaned Xiumi v5 editor HTML.
- Source observation came from a live CloakBrowser Xiumi v5 paper-editor DOM readback.
- No account-state material, browser runtime material, capture-file reference, platform publish
  artifact, export artifact, copy artifact, sync artifact, preview artifact, QR artifact,
  credential secret, or profile-directory reference is part of this evidence.

Observed marker:
- tn-img-crop

Implemented rule:
- Label: Xiumi image crop directive residue
- Trigger: source-specific Xiumi image crop directive attributes.
- Placement: before the broad Xiumi tn-* attribute fallback.

Reduced regression:
- The fixture keeps only the tn-img-crop directive attribute.
- It intentionally avoids worker-surface crop classes, crop-panel child classes, image-enhancement
  controls, Angular runtime classes, hosted media, sidebar controls, and meta panels.
- Before the detector update, the selected test failed because the fixture only emitted
  Xiumi tn-* attribute.
- After the detector update, the selected test reports the precise label for WeChat,
  Xiaohongshu, and Zhihu.

Adjacent safety:
- Xiumi worker surface crop control residue remains separate.
- Xiumi crop panel child control residue remains separate.
- Xiumi image enhancement crop control residue remains separate.
- The broad Xiumi tn-* attribute fallback remains available for unclassified tn-* directives.

Boundary:
- This is local static publishability protection only.
- It does not prove WeChat PC paste, phone preview, mobile SMIL/click interaction, image crop
  fidelity, public-host acceptance, Dark Mode, cover thumbnail acceptance, credentialed sync,
  scheduled send, platform preview, public article rendering, Xiaohongshu/Zhihu account upload,
  or publish success.
