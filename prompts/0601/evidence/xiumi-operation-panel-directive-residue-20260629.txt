Xiumi Operation Panel Directive Residue - 2026-06-29

Scope:
- Local static publishability protection for copied or partially cleaned Xiumi v5 editor HTML.
- No platform action, no phone preview, no sync, no upload, no scheduled send, no publish.

Observed source family:
- Live Xiumi v5 paper-editor DOM readback exposed editor-side operation panel/menu directive
  attributes:
  - tn-panel-move
  - tn-op-back-mask
  - tn-op-menu

Implemented:
- Added detector label:
  Xiumi operation panel directive residue
- Added a reduced regression fixture containing only tn-panel-move, tn-op-back-mask, and
  tn-op-menu directive attributes.
- Rule remains anchored to Xiumi operation panel/menu directive attribute names and does not block
  ordinary panel/menu/move/mask wording, CSS positioning properties, article text, or non-Xiumi
  class names by itself.

Regression coverage:
- WeChat, Xiaohongshu, and Zhihu quality reports now emit platform-specific market-editor-residue
  ids with the precise operation panel directive label.
- Adjacent regressions keep operation panel loader, operator dock control, operation panel
  component control, and broad Xiumi tn-* fallback behavior independent.

Verification:
- Red:
  pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "operation panel directives" --reporter=default
  FAIL: 1 selected failing test before detector update because the fixture only emitted Xiumi tn-* attribute.
- Green:
  pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts -t "operation panel directives|operation panel loader|operator dock child|operation panel component controls|Xiumi tn\\* attribute" --reporter=default
  PASS: 1 file / 4 selected tests / 336 skipped tests.
- Full platform-rendering regression:
  pnpm -C inkforge exec vitest run src/services/export/platform-export-rendering.test.ts --reporter=default --testTimeout=90000
  PASS: 1 file / 340 tests.
- Full export service regression:
  pnpm -C inkforge exec vitest run src/services/export --reporter=default --maxWorkers=1 --no-file-parallelism --testTimeout=90000
  PASS: 36 files / 1317 tests.
- Targeted lint:
  pnpm -C inkforge exec eslint src/services/export/quality-detector.ts src/services/export/platform-export-rendering.test.ts --quiet
  PASS.
- Type check:
  pnpm -C inkforge exec vue-tsc --noEmit --pretty false
  PASS.
- Production build:
  NODE_OPTIONS=--max-old-space-size=4096 pnpm -C inkforge build
  PASS: 4653 modules transformed / built in 40.49s.
- Release preflight:
  pnpm -C inkforge style-proof:release-preflight --json
  EXPECTED BLOCKED: exit 1 with canClaimComplete=false, status=blocked-by-external,
  blockerKinds=phone-preview/external-dependency/unsafe-to-automate/mutating-platform, and
  actionableLocalRows=0.

Boundary:
- This proves local static publishability protection and diagnostic precision only.
- It does not prove WeChat PC paste, phone preview, mobile Dark Mode, H5 panel/menu interaction,
  credentialed sync, scheduled send, platform preview, public article rendering,
  public-host acceptance, XHS/Zhihu account upload, or publish success.
